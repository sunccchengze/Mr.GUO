# 燃气轮机智能设计与前沿算法自学白皮书：从高中物理到顶刊前沿

> **👑 著者前言与自学导读**

本书面向**零基础初入燃气轮机、航空发动机与科学机器学习（AI for Science / AI4Turbomachinery）领域的本科生与初学者**，基于郭振东老师及其合作团队的学术成果编写。

## 本书覆盖的文献范围（口径统一说明）

全书涉及 **20 篇**论文，统一采用 **01–20** 的规范编号（见仓库 README「论文全景索引表」）：

| 类别 | 数量 | 说明 |
| :--- | :---: | :--- |
| **全集** | **20 篇** | 期刊与会议论文总览 |
| 其中**本地有原文** | **15 篇** | 13 篇 PDF + 2 篇 ScienceDirect HTML，正文已程序化抽取入 `corpus/` |
| 其中**仅公开线索** | **5 篇** | 论文 04、05、13、14、20，受订阅墙限制未获全文 |

每讲开头都有元数据表，标明该讲的**证据等级**：

- **A 级**：本地有原文，全部论述可逐句回溯到 `corpus/txt/` 中的原文字符；
- **B 级**：无本地原文，依据出版商页面与摘要全文（论文 04、05、13）；
- **C 级**：仅有学者主页等公开线索（论文 14、20），**正文会明确标注"原文未获取"**。

## 三条写作铁律

旧版白皮书最大的问题，是"宣称"与"内容"对不上——号称"篇篇俱全、绝不略写"，实则有 9 讲只有一段话；号称"完整代码库"，实则一个可运行文件都没有；号称"全景对比矩阵"，里面的数字却查无实据。

因此重建版立下三条铁律，**并由 `tools/verify_all.py` 自动验收**：

1. **没有出处的话不写。** 正文中每一个百分比、倍数、样本量后面都跟着 `［来源］` 标注；查不到出处的，一律标 `［待核实］` 或不写。
2. **跑不通的代码不交。** 第五篇的代码由 `code/` 目录下的真实文件自动嵌入，每个文件都能 `python3 code/xxx.py` 直接运行并输出自检结果。
3. **对不上的数字不填。** 若原文结论与通俗说法冲突，以原文为准，并在讲稿中明确指出旧版表述的错处（例如第 03 讲对 GSDE 与 DE 结果的辨析）。

## 怎么读这本书

- **完全零基础**：先读第零章，它只用初高中物理讲清燃气轮机、CFD 与"高维昂贵黑箱"三件事；
- **有数学基础**：每讲的「数学公式从 0 到 1 推导」小节都是自足的，不依赖前置论文；
- **要做研究**：每讲最后的「审稿人批判」小节列出了可以攻击的软肋，这些正是下一步选题的富矿；
- **要复现**：直接进 `code/` 目录，按 `code/README.md` 跑通全部算法。

> 📌 **本文件由 `tools/build_whitepaper.py` 自动装配生成，请勿直接编辑。**
> 修改内容请改 `docs/lectures/` 下的讲稿源文件后重新运行装配脚本。
> 最近生成：2026-09-05 13:45 UTC

---

## 目录

- [第零章 极简前置铺垫：初高中物理/数学如何托起航空燃气轮机？](#第零章-极简前置铺垫初高中物理数学如何托起航空燃气轮机)
  - [0.1 燃气轮机到底是个啥？——吸气、压缩、点火、喷气四部曲](#0-1-燃气轮机到底是个啥？-吸气、压缩、点火、喷气四部曲)
  - [0.2 什么是 CFD？——把牛顿定律和微积分扔给超算](#0-2-什么是-cfd？-把牛顿定律和微积分扔给超算)
  - [0.3 为什么设计叶片这么难？——高维昂贵黑箱（HEB）与"算不起的悲剧"](#0-3-为什么设计叶片这么难？-高维昂贵黑箱-heb-与"算不起的悲剧")
- [【贝叶斯优化与高维代理模型篇】](#贝叶斯优化与高维代理模型篇)
  - [【第01讲】CR-EI：贝叶斯优化为什么会"瞎逛"——过度探索的发现与校准（**论文 01**）](#第01讲-cr-ei：贝叶斯优化为什么会"瞎逛"-过度探索的发现与校准-论文-01)
  - [【第02讲】Filter-GEI：老专家与实习生——多保真并行优化怎样把钱花在刀刃上（**论文 02**）](#第02讲-filter-gei：老专家与实习生-多保真并行优化怎样把钱花在刀刃上-论文-02)
  - [【第03讲】GSDE：126 维的奇迹——为什么贝叶斯优化在高维叶栅面前必须让位（**论文 07**）](#第03讲-gsde：126-维的奇迹-为什么贝叶斯优化在高维叶栅面前必须让位-论文-07)
  - [【第04讲】DA-EGO：把 126 维切成小块——动态分解与聚合如何救活贝叶斯优化（**论文 11**）](#第04讲-da-ego：把-126-维切成小块-动态分解与聚合如何救活贝叶斯优化-论文-11)
  - [【第05讲】EMFS / MSFO：当"便宜的粗网格"开始骗人——多保真优化的暗面（**论文 13**）](#第05讲-emfs-msfo：当"便宜的粗网格"开始骗人-多保真优化的暗面-论文-13)
- [【生成式模型与跨任务知识迁移篇】](#生成式模型与跨任务知识迁移篇)
  - [【第06讲】GMFoO：一个潜空间不够，那就同时用好几个（**论文 06**）](#第06讲-gmfoo：一个潜空间不够，那就同时用好几个-论文-06)
  - [【第07讲】SW-VAE / KT-ASO：把上一个型号的设计经验"搬"到新型号上（**论文 08**）](#第07讲-sw-vae-kt-aso：把上一个型号的设计经验"搬"到新型号上-论文-08)
  - [【第08讲】GTO：GAN 不会"反推"，那就用梯度硬算出来（**论文 17**）](#第08讲-gto：gan-不会"反推"，那就用梯度硬算出来-论文-17)
  - [【第09讲】GAN-Endwall：把生成式参数化从叶型推广到非轴对称端壁（**论文 14**）](#第09讲-gan-endwall：把生成式参数化从叶型推广到非轴对称端壁-论文-14)
- [【物理增强神经算子与 AI4Turbomachinery 篇】](#物理增强神经算子与-ai4turbomachinery-篇)
  - [【第10讲】TNO：不优化，而是让"一次评估"变得几乎免费（**论文 16**）](#第10讲-tno：不优化，而是让"一次评估"变得几乎免费-论文-16)
  - [【第11讲】SDNO：让网络"照着物理公式"长——把叠加原理写进神经算子（**论文 12**）](#第11讲-sdno：让网络"照着物理公式"长-把叠加原理写进神经算子-论文-12)
  - [【第12讲】P-ResUNet：把"相似原理"写进输入，把"载荷分布"写进输出（**论文 18**）](#第12讲-p-resunet：把"相似原理"写进输入，把"载荷分布"写进输出-论文-18)
  - [【第13讲】SHAP：优化跑完之后，那一堆数据里到底藏着什么？（**论文 19**）](#第13讲-shap：优化跑完之后，那一堆数据里到底藏着什么？-论文-19)
  - [第14讲](#第14讲)
  - [第15讲](#第15讲)
- [【流动控制、气动热耦合与风洞实验篇】](#流动控制、气动热耦合与风洞实验篇)
  - [第16讲](#第16讲)
  - [第17讲](#第17讲)
  - [第18讲](#第18讲)
  - [第19讲](#第19讲)
  - [第20讲](#第20讲)

---

# 第零章 极简前置铺垫：初高中物理/数学如何托起航空燃气轮机？

很多刚进大学的同学，一看到"跨声速多级压气机""非轴对称端壁二次流""纳维-斯托克斯方程""高斯过程回归"就心里发怵。别怕——**所有看似高不可攀的工程黑科技，其物理地基都在你初中和高中学过的知识里。** 本章只用初高中水平的物理与数学，把后面 20 讲要用到的三件事讲清楚。

---

### 0.1 燃气轮机到底是个啥？——吸气、压缩、点火、喷气四部曲

把航空发动机简化到极致，它就是一根管子，干四件事：**吸气 → 压缩 → 点火 → 喷气**。

**① 吸气（进气道）**
初中物理的**连续性方程**：管子细的地方流速快、粗的地方流速慢。进气道做成先粗后细的形状，就是为了把迎面高速气流"收拾"成适合压气机入口的速度。

**② 压缩（压气机）**
这是全书的主角。压气机由一排排**叶片**组成，每一排都在给空气"做功"——就像你用扇子扇风时给空气加了能量。
- **转子（Rotor）**：转动的叶片排，通过旋转把机械能传给气体，气体的**压力升高、速度升高**；
- **静子（Stator）**：不动的叶片排，把气体的**速度"收拾"成压力**（扩压），并为下一级整理好进气角度。

一对"转子+静子"就是一个**级（stage）**。本书第 03 讲要优化的，是一台 **3.5 级压气机**（1 排进口导叶 + 3 个级，共 7 排叶片）［原文 P07 §Geometry definition］。

这里用到的高中物理是**能量守恒**：压气机耗功 = 气体的焓升（压力与温度同时上升）。工程上衡量压缩"划不划算"的指标叫**等熵效率**：

$$\eta_{is}=\frac{\text{理想（等熵）压缩所需功}}{\text{实际消耗的功}}$$

**这个数每提高 1 个百分点，都是发动机行业的重大胜利。** 记住这个量级感：本书第 03 讲里，一次成功的优化把 3.5 级压气机的总对总效率从 85.825% 提到 86.929%，增量 **1.104%**，就已经是一篇 *Aerospace Science and Technology* 论文的核心成果［原文 P07 Table 3］。

**③ 点火（燃烧室）**
燃油与高压空气混合燃烧，化学能变成热能，气体温度骤升（现代发动机涡轮前温度已远超金属熔点，必须靠气膜冷却保护叶片——这就是第 19、20 讲要解决的问题）。

**④ 喷气（涡轮 + 尾喷管）**
高温高压气体冲过**涡轮**，推动涡轮叶片旋转；涡轮通过一根轴**反过来带动压气机**（这正是燃气轮机最精妙的自持设计）。剩下的能量从尾喷管高速喷出，按**动量守恒**（高中物理）产生反作用推力。

> **一句话串起来**：压气机是"花钱的"（耗功），涡轮是"赚钱的"（出功），燃烧室是"本钱"。整机设计的全部艺术，就是让这笔账越算越划算。

---

### 0.2 什么是 CFD？——把牛顿定律和微积分扔给超算

叶片形状好不好，不能靠猜，得算流场。所谓 CFD（计算流体力学），就是把**牛顿第二定律 $F=ma$** 写成流体的形式——**纳维-斯托克斯方程（N-S 方程）**——然后在计算机上解它。

对本书而言，你只需要理解三件事：

**① 控制方程（不必会解，但要知道它在说什么）**

- **质量守恒（连续性方程）**：流进多少，流出多少，不能凭空产生；
- **动量守恒（N-S 方程）**：$F=ma$ 的流体版，左边是惯性，右边是压力、粘性、体积力；
- **能量守恒**：热力学第一定律，决定了温度场与换热。

**② 湍流模型：必须"造假"的一层**

工程上直接解 N-S 方程（DNS）的代价大到不可想象，于是工业界普遍解**雷诺平均 N-S 方程（RANS）**，把湍流的脉动用一个模型"糊"上去。
本书第 03 讲的算例采用 **Spalart-Allmaras 一方程湍流模型**，用 **NUMECA FINE/Turbo** 求解器，网格为 O4H 拓扑的六面体结构网格、**322 万节点**，第一层网格高度 0.001 mm 以保证 $y^+<1$［原文 P07 §Numerical method］。

> **这里的工程直觉**：网格越细越准，但计算量暴涨。本书第 02 讲的核心思想恰恰就是建立在这个矛盾之上——**粗网格（低保真）便宜，细网格（高保真）昂贵，怎么搭配着用？**

**③ CFD 的代价：一切优化的起点**

第 03 讲报告了真实数字：在上述配置下，**单次 3.5 级压气机 CFD 评估需要 1~1.5 小时**（双路 AMD EPYC 7502 32 核服务器）［原文 P07 §Result analysis］。

**请记住这个数量级：一次评估 = 一小时。** 后面所有算法（贝叶斯优化、多保真、代理辅助进化、神经算子）存在的唯一理由，都是为了让"一小时的评估"少做几次。

---

### 0.3 为什么设计叶片这么难？——高维昂贵黑箱（HEB）与"算不起的悲剧"

现在把前面两节合起来，看看叶栅设计到底难在哪。

**难点一：维数灾难（Curse of Dimensionality）**

一个叶片形状要用多少个数字描述？第 03 讲的做法是：每排叶片用 **18 个变量**（根/中/尖三个截面的型线控制点 + 周向平移 + 轴向平移），7 排叶片就是 **126 维**［原文 P07 Table 2］。

为什么必须这么多？因为**叶片排之间是相互影响的**（0.1 节说过：第 2 排吃的是被第 1 排搅过的气流）。原文说得明确：**为充分考虑叶片排与级之间的相互作用，两级以上的叶栅必须同时优化**［原文 P07 摘要］——单独优化每一排在物理上就是错的。

做个量级感受：在 126 维空间里撒 1000 个样本点，平均到每个维度上只能分成

$$1000^{1/126}\approx 1.055$$

份——**连"每个方向一分为二"都做不到**。这就是维数灾难。

**难点二：每次评估贵得要命（Expensive）**

一次 CFD 一小时（0.2 节）。一个设计周期能负担多少次？原文给出的现实约束是：**几百到几千次**［原文 P07 摘要］。

**难点三：没有导数可用（Black-box）**

CFD 求解器是一个"扔进去几何、吐出来一个数"的黑箱。你拿不到 $\partial \eta_{is}/\partial x_i$ 这样的梯度信息，因此**基于梯度的优化方法（最速下降、SQP 等）通通用不上**，只能用无导数/进化类方法。

**难点四：一个点都不许浪费**

综合以上三点，就得到了本书要解决的**高维昂贵黑箱（High-dimensional Expensive Black-box, HEB）**问题。原文对它的定义性描述是：

> 基于 CFD 的叶栅设计优化是典型的高维昂贵黑箱问题……变量数可达 100 以上，而一个设计周期内可负担的 CFD 次数只有几百到几千次。［原文 P07 摘要］

**于是全书 20 讲，本质上都是在回答同一个问题：**

> **在 126 维、每次评估一小时、总共只有一两千次机会的约束下，下一个点应该算在哪里？**

| 主线 | 策略 | 对应讲次 / 论文 |
| :--- | :--- | :--- |
| 让每一次采样更聪明 | 改造采集函数（EI → CEI/REI → 滤波 GEI） | 第 01、02 讲（论文 01、02） |
| 让便宜的粗网格多干活 | 多保真 + 并行 + 失真区侦测 | 第 02、05 讲（论文 02、13） |
| 换掉撑不住高维的代理 | 代理辅助进化、子空间分解 | 第 03、04 讲（论文 07、11） |
| 不每次从零开始 | 生成式参数化 + 知识迁移 | 第 07、08 讲（论文 08、17） |
| 干脆不优化，直接预测流场 | 物理增强神经算子 | 第 10–12 讲（论文 16、12、18） |
| 回到物理本体 | 流动控制 + 实验与不确定性量化 | 第 16–19 讲（论文 03、09、10、04） |

---

> 📖 **读完第零章，你已经具备了读懂全部 20 讲的物理基础。** 接下来每一讲都会遵循统一的六节结构：
> **① 生活与物理直觉 → ② 工程死穴 → ③ 数学推导 → ④ 算法控制流 → ⑤ 实验验证（带出处）→ ⑥ 审稿人批判与承前启后。**

---

# 第一篇 【贝叶斯优化与高维代理模型篇】

本篇覆盖**论文 01、02、07、11、13**。这一条主线回答一个问题：**当一次 CFD 贵得要命时，下一个点该算在哪里？** 从校准采集函数（CR-EI），到多保真并行分配（Filter-GEI），再到 126 维下被迫改换门庭的代理进化（GSDE）与子空间聚合（DA-EGO），最后以「多保真也会骗人」这一反直觉发现（EMFS / MSFO）收尾。

---

### 【第01讲】CR-EI：贝叶斯优化为什么会"瞎逛"——过度探索的发现与校准（**论文 01**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **01** |
> | 原始文献 | *Structural and Multidisciplinary Optimization*, 2021, **64: 3549–3567** |
> | DOI | `10.1007/s00158-021-03038-3` |
> | 作者 | Zhendong Guo, Yew-Soon Ong, Haitao Liu |
> | 本地原文 | `s00158-021-03038-3.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

---

#### 1. 生活与物理直觉引子：三种"找最佳餐厅"的策略

假设你搬到一个新城市，想在 30 天内找到"全城最好吃的一家面馆"。每去一家吃饭，都要付出一顿饭的钱和时间——这顿饭就是一次**昂贵实验**，对应叶片设计中动辄几小时的一次 CFD 计算。

你只有三种策略：

- **策略 A（纯开发 exploitation）**：找到一家还不错的，从此天天去它家，只在这家附近的小巷子里换着吃。风险是：全市最好吃的那家在河对岸，你这辈子都不会知道。
- **策略 B（纯探索 exploration）**：专挑地图上空白的地方去。风险是：空白区往往是没人去的**难吃的工业区**，你只是在反复验证"这里确实难吃"。
- **策略 C（聪明折中）**：用一张"口味地图"——已知好吃的区域，猜测附近也可能好吃；空白区域则代表"不确定"，不确定性越大，越值得去试试。

贝叶斯优化的期望改善（Expected Improvement, EI）就是策略 C 的数学版本。而本篇论文要讲的，正是策略 C 里一个**藏了二十年**的陷阱。

#### 2. 叶轮机械中的真实工程死穴：算不起，但停不下

涡轮叶片的气动设计，本质是一个**昂贵黑箱优化**问题：

- 给一组几何参数 $\mathbf x$（叶型厚度分布、安装角、端壁型线……）， CFD 求解器吐回一个性能值 $y$（如总压损失系数、等熵效率）；
- 一次高保真 CFD 可能要**几小时到几十小时**；
- 一个设计周期里能负担的 CFD 次数**只有几百到几千次**［原文 P07 §1］；
- 设计变量维度轻易超过 100 个［原文 P07 摘要］。

在这种"一次评估贵得要命"的场景下，**每一次采样点的选择都必须物有所值**。EI 采集函数之所以成为贝叶斯优化的标准配置，就是因为它给出了"这个点值得花一次 CFD 吗"的量化答案。

但 EI 有个久治不愈的毛病：**采样次数已经很多了，优化却卡在次优解上不来**。此前学界的共识解释是"过度开发"（over-exploitation）——算法太贪，只在当前最优点附近打转。本篇论文的第一作者郭振东则指出：**这个解释只对了一半**。

#### 3. 数学公式从 0 到 1 推导：一个参数 z，两种病

**第一步：EI 的解析式**

设代理模型（高斯过程）在点 $\mathbf x$ 处给出预测均值 $\hat y(\mathbf x)$ 与预测标准差 $\sigma(\mathbf x)$，当前已观测到的最优值为 $\xi$。定义

$$z=\frac{\xi-\hat y(\mathbf x)}{\sigma(\mathbf x)}$$

则 EI 可解析地写成（原文式 5、式 11）：

$$\mathrm{EI}(\mathbf x)=\int_{-\infty}^{\xi}(\xi-Y)\,\mathrm dF_Y
=\big(\xi-\hat y\big)\Phi(z)+\sigma\,\varphi(z)
=\sigma(\mathbf x)\cdot\underbrace{\big(z\Phi(z)+\varphi(z)\big)}_{\displaystyle \mathrm{EI}^*(z)}$$

其中 $\Phi$ 与 $\varphi$ 分别是标准正态的累积分布函数与概率密度函数。**这个 $z$ 就是全文的题眼。**

**第二步：看 EI 对什么更敏感——求偏导**

$$\frac{\partial \mathrm{EI}}{\partial \hat y}=-\Phi(z),\qquad \frac{\partial \mathrm{EI}}{\partial \sigma}=\varphi(z)$$

于是 EI 的负梯度（即"往哪个方向走 EI 涨得最快"）为

$$-\nabla \mathrm{EI}(\mathbf x)=\Phi(z)\frac{\partial \hat y}{\partial \mathbf x}-\varphi(z)\frac{\partial \sigma}{\partial \mathbf x}$$

第一项是**开发项**（跟着预测均值走），第二项是**探索项**（跟着不确定性走），权重分别是 $\Phi(z)$ 与 $\varphi(z)$。

**第三步：z 的两个极端，对应两种病**

- 当 $z>3$ 时，$\Phi(z)\gg\varphi(z)$，负梯度近似为（原文式 8）
 $$-\nabla\mathrm{EI}\big|_{\,z>3}\approx \Phi(z>3)\frac{\partial \hat y}{\partial \mathbf x}$$
 采样几乎完全由代理模型的预测最优主导 → **过度开发**（只在已知好点附近打转）。
- 当 $z<-3$ 时，反过来 $\varphi(z)\gg\Phi(z)$（原文式 9）
 $$-\nabla\mathrm{EI}\big|_{\,z<-3}\approx -\varphi(z<-3)\frac{\partial \sigma}{\partial \mathbf x}$$
 采样几乎完全追着 $\sigma$ 的最大值跑 → **过度探索**。

**第四步：为什么"过度探索"更致命**

注意 $z$ 的符号带来的约束差异（原文式 10）：

$$z>0\ \Rightarrow\ \hat y(\mathbf x^*_{\rm EI})<\xi\quad(\text{采样被约束在有希望的区域})$$
$$z\le 0\ \Rightarrow\ \text{对}\ \hat y(\mathbf x^*_{\rm EI})\ \text{无任何约束}$$

关键在于：**$\sigma(\mathbf x)$ 的大小只反映该处样本有多稀疏，与该处目标值好坏毫无关系**。而优化早期 $z>0$ 阶段，采样不断向好区域聚集，导致**稀疏区（σ 大）恰好集中在差区域**。于是当算法转入 $z\le0$ 阶段时，EI 会被系统性地、持续地引向那些"最不确定、但也最差"的点——而且可以**永远出不来**。

论文给了个触目惊心的例子：在 6 维 Hartman6 函数（6 个局部最优）上，EI 用 LHS 生成 36 个初始样本后，很快陷入过度探索（z 在 −5 附近震荡，$\sigma^2/\sigma^2_{\max}\approx1$，maxEI 的量级从 $10^{-5}$ 一路掉到 $10^{-20}$）；此时若按"maxEI 小于阈值就停机"的常见做法提前终止，得到的最优值是 **−2.7890，而真实全局最优是 −3.0424**［原文 P01 §3.1］。

第五步：**一个附带结论**——$\mathrm{EI}^*(z)$ 在 $z>0$ 时近似线性增长，$z\le0$ 时指数衰减，数值上极难优化。所以**保持 $z>0$ 是明智的**，但简单加阈值停机又会早熟收敛。这就是 CR-EI 要解的死结。

#### 4. 算法控制流：EI / CEI / REI 三兄弟如何轮班

核心思想：**别再动 $\sigma$ 了（前人都在这儿做文章），改 incumbent $\xi$**。

**(a) CEI（Calibrated EI，校准 EI）——治过度探索，强化局部开发**

把 incumbent 从 $y_{\rm PBS}$ 抬高到 $\xi_{\rm Calibrated}>y_{\rm PBS}$，重定义

$$\tilde z=\frac{\xi_{\rm Calibrated}-\hat y(\mathbf x)}{\sigma(\mathbf x)},\qquad
\mathrm{CEI}(\mathbf x)=\sigma(\mathbf x)\big(\tilde z\Phi(\tilde z)+\varphi(\tilde z)\big)$$

这样原来大量 $z\le0$ 的点变成 $\tilde z>0$，采样区重新被约束为 $\hat y(\mathbf x^*)<\xi_{\rm Calibrated}$。再叠加**距离约束**（只接受当前最优点邻域内的建议点）：

$$\mathbf x^*_{\rm CEI}=\arg\max_{\mathbf x}\ \mathrm{CEI}(\mathbf x)\cdot\delta_1\big(\|\mathbf x-\mathbf x_{\rm PBS}\|\big),\qquad
\delta_1=\begin{cases}1,&\varepsilon<\|\mathbf x-\mathbf x_{\rm PBS}\|\le d_T\\[2pt] 0,&\|\mathbf x-\mathbf x_{\rm PBS}\|> d_T\end{cases}$$

$\varepsilon$ 常取 0.001 以防重复采样。

**(b) REI（Recalibrated EI，重校准 EI）——治过度开发，强化全局探索**

$$\mathrm{REI}(\mathbf x)=\mathrm{CEI}(\mathbf x)\cdot\delta_2\big(\|\mathbf x-\mathbf x_{\rm PBS}\|\big),\qquad
\delta_2=1-\delta_1\ \text{的互补分区}$$

即 REI **只**在 $x_{\rm PBS}$ 邻域之外取点。$\delta_1$ 与 $\delta_2$ 互补 → **CEI 管近处、REI 管远处，合起来覆盖整个设计空间**。

**(c) CR-EI 的调度规则（原文式 17–18）**

以已标注样本目标值的**中位数**作为预警阈值 $y_T=\mathrm{median}\big(Y(X)\big)$，并用一个计数器 $n_{\rm Breakthrough}$ 监测 EI 是否还在取得突破：

$$\mathbf x^*_{\rm new}=\begin{cases}
\{\mathbf x^*_{\rm EI}\}, & n_{\rm Breakthrough}=0 \quad(\text{EI 正常工作，就让它继续})\\[2pt]
\{\mathbf x^*_{\rm EI},\ \mathbf x^*_{\rm REI}\}, & n_{\rm Breakthrough}>0 \quad(\text{EI 有进展，REI 并行去别处找})\\[2pt]
\{\mathbf x^*_{\rm CEI},\ \mathbf x^*_{\rm REI}\}, & y(\mathbf x^*_{\rm EI})\ge y_T \quad(\text{EI 疑似过度探索，换 CEI+REI})
\end{cases}$$

> **一句话记住**：EI 跑得顺就让它跑，同时派 REI 去远处看看；EI 一旦开始"瞎逛"（建议点比一半已有点还差），立刻换成 CEI 收紧到近处开发 + REI 保持远眺。

#### 5. 实验验证与量化突破

**算例一：7 个基准函数**，每算法独立运行 20 次，对手是 EGO、PEI、GEI、E3I 四个主流 EI 变体：

| 函数 | EGO | PEI | GEI | E3I | **CR-EI** |
| :--- | :---: | :---: | :---: | :---: | :---: |
| Shekel4 成功率 | 17/20 | 20/20 | 6/20 | 14/20 | **20/20** |
| Trid10 成功率 | 14/20 | 10/20 | 13/20 | 20/20 | **20/20** |

> 原文明确结论：**除 10 维 Trid10 函数外，CR-EI 在其余 6 个基准上均以更快的收敛速率取得最优解**［原文 P01 Table 1 / §4.3］。

**算例二：低速翼型气动优化（工程级）**

- 参数化：用 GAN 在 UIUC 翼型数据库上训练，生成器输入即设计变量，**13 维**；
- 求解器：XFOIL；工况 $Re=1.8\times10^6$、$Ma=0.01$、$AoA=0^\circ$；
- 目标：最大化升阻比 $L/D$（实现为最小化 $-L/D$）；每组 10 次运行。

| 指标 | EGO | PEI | GEI | E3I | **CR-EI** |
| :--- | :---: | :---: | :---: | :---: | :---: |
| 均值 ± 标准差 | −239.34 ± 8.60 | −240.66 ± 6.41 | −240.70 ± 4.57 | −242.20 ± 4.48 | **−246.34 ± 3.56** |
| 中位数 | −241.44 | −242.74 | −240.82 | −241.92 | **−248.09** |

- 相对 EGO，CR-EI 把平均 $L/D$ 提升了 **2.92%**［由 Table 2 计算：$(246.34-239.34)/239.34$］；
- 结果的标准差从 8.60 降到 3.56，**离散度降低 58.6%**［由 Table 2 计算］——意味着**不只是平均更好，而且十次里次次都更好**，这对工程设计的可信度至关重要。

**参数敏感性**：CR-EI 引入距离参数 $\alpha$ 与 incumbent 参数 $\beta$。原文在 Camel6、Hartman6 上的扫描显示，$\beta=0.1$ 时综合最优；$\beta$ 越大越抗过度探索，但搜索区也随之膨胀，$\beta$ 过大时几乎覆盖全空间，反而失去约束意义［原文 P01 §4.5］。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **新增了两个需要调的超参**（$\alpha,\ d_T,\ \beta$）。推荐值 $\beta=0.1$ 是在本文测试集上试出来的，**换一个工程问题不保证最优**；$d_T$ 是绝对距离，未随维数做归一化，高维下欧氏距离集中效应会削弱 CEI/REI 的"近处/远处"分区。
2. **没有理论保证**。全文是机理分析 + 实验验证，未给出收敛性证明或 regret bound。
3. **维数天花板只有 10 维**。而真实叶栅优化是 100+ 维；高维下高斯过程本身就开始失真，"校准 incumbent"这套机制能否延续，本文没有回答。
4. **切换逻辑依赖代理模型质量**。$z$ 的符号完全由 GP 给出的 $\hat y,\sigma$ 决定，若 GP 拟合失真，预警与切换都会被误导。

**承前：** 建立在 EGO 的 EI 框架之上，把 GEI（调 σ 权重）、E3I（用 Thompson 样本最优做 incumbent）、PEI（伪 EI）三条改良路线统一到"调 incumbent"这个新视角下审视。

**启后：**
- **论文 02（Filter-GEI）**：把"用指标动态切换加点准则"的思想搬进多保真并行场景；
- **论文 07（GSDE）与论文 11（DA-EGO）**：本篇暴露的"高维下 GP 自身失效"这一天花板，正是它们转向代理辅助差分进化与动态子空间分解的直接动因；
- **论文 17（GTO）**：本篇用 GAN 做 13 维翼型参数化，后续在 GTO 中升级为带梯度重参数化的生成式迁移设计。

---

### 【第02讲】Filter-GEI：老专家与实习生——多保真并行优化怎样把钱花在刀刃上（**论文 02**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **02** |
> | 原始文献 | *Structural and Multidisciplinary Optimization*, 2021, **64: 1457–1468** |
> | DOI | `10.1007/s00158-021-02931-1` |
> | 作者 | Zhendong Guo, Qineng Wang, Liming Song, Jun Li（西安交通大学） |
> | 本地原文 | `s00158-021-02931-1.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

---

#### 1. 生活与物理直觉引子：老专家与实习生

想象你经营一家设计公司，手下有两类人：

- **老专家**：画图精准、结论可靠，但**时薪 3000 元，一天只能出 1 张图**；
- **实习生**：速度快、一天能出 10 张图，**时薪 300 元**，但图纸常有偏差，只能看个大概趋势。

现在要做一个设计招标。聪明的做法显然不是"所有图都让老专家画"（太贵），也不是"全让实习生画"（不可靠）。你会让**实习生先铺开画一批，把大致方向摸出来；老专家只在最关键的几张图上出手**。

这就是**多保真优化（Multi-Fidelity Optimization, MFO）**的全部思想。在叶轮机械里：

- **低保真（LF）** = 粗网格 CFD（1.2 万单元）、无粘 Euler、二维简化模型；
- **高保真（HF）** = 细网格 RANS（10 万单元）、三维全环、带转捩模型。

本文的工程算例中，**LF 与 HF 的成本比是 0.1**——也就是说，一次高保真的钱能做十次低保真［原文 P02 §4.3］。

**但这里有个二十年的老问题**：每轮迭代里，到底哪些点该让老专家（HF）看，哪些点交给实习生（LF）？这就是本文所说的 **sample assignment（样本分配）问题**。

#### 2. 叶轮机械中的真实工程死穴：串行加点浪费了整个机房

传统的多保真 EGO 类算法（如 augmented-EI、VF-EI）有一个共同特征：**每轮迭代只加一个样本**。

问题在两方面：

1. **贵**：一次 CFD 几小时，一个设计周期几百次调用，串行下来是几个月；
2. **浪费**：现代计算集群一晚上能同时跑几十个 CFD 作业，单点串行加点意味着**大部分核在空转**。

同时，已有算法在决定"这个点用 HF 还是 LF"时，往往只看相似性与成本比，**没有利用"LF 模型到底靠不靠谱"这一关键信息**。本文要解决的，正是这两个痛点：**把保真度分配做对，并且一次分配一批**。

#### 3. 数学公式从 0 到 1 推导

**第一步：多保真代理模型 Co-kriging**

把 LF 与 HF 都当成随机过程：

$$Y_{LF}(\mathbf x)=\mu_{LF}+Z_{LF}(\mathbf x),\qquad Y_{HF}(\mathbf x)=\mu_{HF}+Z_{HF}(\mathbf x)$$

关键假设是 HF 可以写成"LF 的缩放 + 一个差异函数"：

$$y_{HF}(\mathbf x)=\rho\cdot y_{LF}(\mathbf x)+Z_{DF}(\mathbf x)$$

其中 $\rho$ 是缩放因子，$Z_{DF}$ 是差异函数（discrepancy function），专门负责刻画"LF 修正后仍与 HF 差多少"。这样，**LF 负责大趋势，DF 负责补差距**，两套高斯过程拼成一个联合协方差矩阵（原文式 4–6）。

**第二步：基线采集函数 GEI（广义期望改善）**

经典 EI 有个毛病：太贪恋当前最优附近。Schonlau（1997）把它推广为带幂次 $g$ 的 GEI：

$$E\big[I(\mathbf x)^g\big]=s^g\sum_{k=0}^{g}(-1)^k\binom{g}{k}u^{\,g-k}T_k,\qquad u=\frac{f_{\min}-\hat y(\mathbf x)}{s(\mathbf x)}$$

$$T_0=\Phi(u),\quad T_1=-\varphi(u),\quad T_k=-u^{k-1}\varphi(u)+(k-1)T_{k-2}$$

**$g$ 越大，越偏向全局探索；$g=1$ 时退化为经典 EI。** 取若干个不同的 $g$，就同时得到一批"有的爱开发、有的爱探索"的候选点——这天然适配并行加点。

**第三步：核心创新——给 GEI 装一个"滤波器"（原文式 10–12）**

定义一个阈值：

$$T=\omega\cdot y_{HF\text{-}min}+(1-\omega)\cdot y_{HF\text{-}mean}$$

其中 $y_{HF\text{-}min}$ 是当前最优，$y_{HF\text{-}mean}$ 是历史加权平均：

$$y_{HF\text{-}mean}=\frac{1}{iter}\sum_{i=0}^{iter}\left(\frac{1}{n_i}\sum_{j=1}^{n_i}y_{HF-ij}\right)$$

而权重 $\omega$ **由 HF 与 LF 的相关性自适应决定**——这是全文最漂亮的一笔：

$$\boxed{\ \omega=\frac{1}{1+\left(\hat\sigma^2_{DF}\big/\hat\sigma^2_{LF}\right)^{0.5}}\ }$$

**物理含义请务必记住：**

- 当 **LF 与 HF 高度相关**时，差异函数 $Z_{DF}$ 的方差 $\hat\sigma^2_{DF}$ 相对很小 → 分母趋近 1 → **$\omega\to1$** → 阈值 T 贴近当前最优值 $y_{HF\text{-}min}$ → **只有非常好（优于 T）的点才配用 HF**，采样极度聚焦于局部开发。
- 当 **LF 与 HF 相关性差**时，$\hat\sigma^2_{DF}$ 大 → $\omega$ 变小 → 阈值 T 贴近历史均值 → **HF 的投放区域被放大**，避免在不可信的 LF 引导下钻牛角尖，转而加强全局探索。

**第四步：候选点去重叠**

不同 $g$ 给出的候选点可能重合，直接一起投进 Co-kriging 会让协方差矩阵病态。本文用层次聚类（逐个合并距离最小的一类，直到类间最小距离大于阈值 $d_T$）把重叠点聚掉。

#### 4. 算法控制流

```
每轮迭代：
  1. 用现有 HF + LF 样本拟合 Co-kriging 代理
  2. 用多个不同 g 的 GEI，生成一批候选点（既含开发点，也含探索点）
  3. 层次聚类去掉重叠候选点
  4. 计算自适应权重 ω = 1/(1+ (σ²_DF/σ²_LF)^0.5 )
  5. 计算阈值 T = ω·y_HF-min + (1-ω)·y_HF-mean
  6. 分流：
       预测值优于 T 的候选点  → 送 HF 仿真（贵，但值得）
       其余候选点            → 送 LF 仿真（便宜，用于全局探索+降不确定性）
  7. 一批 HF 与一批 LF 样本同时提交，并行计算
```

> **一句话记住**：**GEI 负责"去哪儿采"，滤波器负责"花多少钱采"。**

#### 5. 实验验证与量化突破

**算例一：5 个基准函数**（Forrester1a/1b、Hartman3+MA3、Ackley5+MA5、Hartmann6+MA6，最高 6 维；初始样本 HF=3×维数、LF=6×维数；统一用开源 ooDACE 工具箱）

| 观察点 | 结果 |
| :--- | :--- |
| HF 样本消耗 | Filter-GEI 在 5 个算例上**始终用最少的 HF 样本**取得有竞争力的最优解；无滤波的 GEI-MFO 需要显著更多的 HF 样本 |
| 迭代次数 | 由于每轮并行加多样本，Filter-GEI 与 GEI-MFO 的迭代次数**远少于**单点的 EI-MFO |
| 成功率 | Filter-GEI **5 个算例全部成功**；VF-EI 在 5 维 Ackley5 上仅 **12/20**；augmented-EI 在 6 维 Hartman6 上仅 **12/20** |
| 稳定性 | Filter-GEI 迭代次数波动**明显小于** VF-EI 与 augmented-EI |

［以上均出自原文 P02 §4.2 与 Table 4］

**算例二：GE-E3 涡轮静子叶片气动优化（工程级）**

- 设计变量：**7 个**几何参数；HF 网格 100,701 单元，LF 网格 12,141 单元；
- 初始样本 42 LF + 21 HF（最近邻采样），终止条件为**再消耗 35 个 HF 样本**；独立运行 5 次；
- 代理模型交叉验证 CV-R² **全部 > 0.85，多数 > 0.95**［原文 Table 7］，说明 Co-kriging 拟合可信。

| 算法 | 达到最优所需迭代数 | 最优值(%) 均值±标准差 | LF 样本消耗 |
| :--- | :---: | :---: | :---: |
| **Filter-GEI** | **13 ± 1** | **2.720 ± 0.0245** | 91 ± 10 |
| GEI（无滤波） | 8 ± 0 | 2.760 ± 0.0224 | 79 ± 2 |
| EI | 36 ± 0 | 2.732 ± 0.0192 | 77 ± 0 |
| VF-EI | 45 ± 3 | 2.748 ± 0.0239 | 51 ± 3 |
| augmented-EI | 51 ± 6 | 2.726 ± 0.0358 | 59 ± 9 |

［原文 P02 Table 8］

**怎么读这张表（很关键，别读错）：**

- **GEI 只用了 8 次迭代就"收敛"了，但收敛到的是最差的最优值 2.760**——没有滤波器时，HF 被挥霍在不太有希望的点上，看似快，实则跑偏。
- **Filter-GEI 用 13 次迭代拿到全场最好的 2.720**，而且**Min 值 2.70 是全场最低**。
- 相比串行的 EI / VF-EI / augmented-EI（36 / 45 / 51 次迭代），Filter-GEI 的迭代次数**只有它们的 1/3 左右**——这正是并行加点的直接收益。
- 代价是：**用了更多 LF 样本（91 vs 79）**。但按 0.1 的成本比折算，多花 12 次 LF ≈ 1.2 次 HF 的钱，换来最优值从 2.760 降到 2.720，这笔账是划算的。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **"并行"是算法层面的并行，实验是串行模拟的**。真拿到工程现场，收益取决于你有多少个 CFD 许可证/核；很多单位恰恰卡在许可证数量上。
2. **成本比没有进入公式**。$\omega$ 只用了方差比 $\hat\sigma^2_{DF}/\hat\sigma^2_{LF}$，**没有把 LF/HF 的实际成本比纳入**。本文工程算例的成本比恰好是 0.1；若 LF 也很贵（成本比接近 1），多投 LF 的滤波策略就不一定划算了。
3. **阈值与权重的启发式本质**。T 取"最优值与加权均值的凸组合"缺乏最优性证明；$\omega$ 的自适应也只有定性论证。
4. **维数量级有限**：基准最高 6 维，工程算例 7 维。真正的多级叶栅是 100+ 维（见**论文 07**），Co-kriging 在高维下的建模能力本身就是瓶颈。
5. **隐含前提"LF 大体能捕捉 HF 趋势"**。一旦这个前提在某块区域失效，滤波器反而会把 HF 集中投到被 LF 误导的区域——这正是**论文 13（EMFS / MSFO）**要处理的问题。

**承前：** 建立在 Co-kriging（Forrester et al. 2007）与 GEI（Schonlau 1997）之上，思想直接继承自同组前作 **论文 01（CR-EI）**——"用一个自适应指标来决定这一轮怎么采样"。

**启后：**
- **论文 13（EMFS / MSFO）**：正面处理"LF 在某些区域反而误导优化"的场景，用 DBSCAN 找出多保真代理建不准的区域，在那里改建单保真代理；
- **论文 07（GSDE）**：把优化从"低维多保真 + 贝叶斯"推进到"126 维 + 代理辅助进化"；
- **论文 11（DA-EGO）**：把"自适应分配"从**保真度维度**迁移到**子空间维度**。

---

### 【第03讲】GSDE：126 维的奇迹——为什么贝叶斯优化在高维叶栅面前必须让位（**论文 07**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **07** |
> | 原始文献 | *Aerospace Science and Technology*, 2023, **142: 108675** |
> | DOI | `10.1016/j.ast.2023.108675` |
> | 作者 | Zhendong Guo, Zijun Zhang, Yun Chen, Guangjian Ma, Liming Song\*, Jun Li, Zhenping Feng |
> | 本地原文 | `1-s2.0-S1270963823005710-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

---

#### 1. 生活与物理直觉引子：为什么不能"一排叶片一排叶片地优化"

先建立一个物理直觉。**压气机不是一个叶片在战斗，而是一支队伍在战斗。**

空气从第 1 排叶片流过时，会在出口留下"尾迹"和"旋流"；第 2 排叶片吃到的，正是被第 1 排搅过的气流。如果你把第 1 排单独优化到完美，第 2 排的进口条件就变了，它的最优形状也随之改变——反过来又影响第 1 排。

这就是为什么原文强调：**为充分考虑叶片排与级之间的相互作用，两级以上的叶栅必须同时优化**［原文 P07 摘要］。

代价是变量数的爆炸：本文的 3.5 级压气机有 **7 排叶片**（S0 导叶 + R1/S1、R2/S2、R3/S3 共 3 级），每排叶片用 **18 个变量**参数化，于是

$$18\times 7=126\ \text{维}$$

而一个设计周期里，你**只负担得起几百到几千次 CFD**［原文 P07 摘要］。126 维空间里撒几千个点是什么概念？做个类比：如果空间是 1 维，1000 个点能把区间切成 1000 份，密密麻麻；126 维空间里 1000 个点，平均每个维度上只能分成约 $1000^{1/126}\approx1.055$ 份——**比"把每个方向一分为二"还要稀疏得多**。

这就是所谓**高维昂贵黑箱（High-dimensional Expensive Black-box, HEB）**问题。

#### 2. 叶轮机械中的真实工程死穴：高斯过程在高维下会"失明"

第 01 讲的 CR-EI、第 02 讲的 Filter-GEI 都建立在**高斯过程（Kriging）**之上。它们很优雅，但有个致命的软肋：

| 问题 | 后果 |
| :--- | :--- |
| GP 的协方差矩阵随样本数 $n$ 呈 $O(n^3)$ 增长 | 样本上千就非常慢 |
| 高维下核函数的长度尺度难以估计 | 代理模型拟合失真 |
| 高维下"距离"失去区分度（距离集中效应） | "哪里不确定"这件事本身就不确定了 |
| EI 类采集函数需要在整个空间上全局最大化 | 高维下这个子优化本身就很难 |

所以本文做了一个**路线选择**：**放弃贝叶斯优化框架，改用进化算法（差分进化 DE）打底，把代理模型作为"局部搜索加速器"嵌入进去。** 这就是 GSDE（Generalized Surrogate-assisted Differential Evolution）。

#### 3. 数学公式从 0 到 1 推导

**第一步：为什么代理模型换成 RBF**

原文指出：在高维设计空间，RBF 的预测精度强于其他常见代理，且训练时间随变量数增长仍可接受。GSDE 采用**三次 RBF + 线性多项式尾项**：

$$\hat f(\mathbf x_{new})=\sum_{i=1}^{n}\lambda_i\,\varphi\big(\|\mathbf x_{new}-\mathbf x_i\|\big)+p(\mathbf x_{new})$$

$$\varphi(r)=r^3,\qquad p(\mathbf x)=\sum_{i=1}^{D}b_i x_i+a$$

待定系数共 $n+D+1$ 个（$\lambda_1..\lambda_n,\ b_1..b_D,\ a$），由一个线性方程组**一次性解出**（不需要迭代优化超参数，这正是 RBF 在高维工程场景的实用之处）：

$$\begin{pmatrix}\Phi & P\\ P^{T} & 0\end{pmatrix}\begin{pmatrix}\boldsymbol\lambda\\ \mathbf c\end{pmatrix}=\begin{pmatrix}\mathbf F\\ \mathbf 0\end{pmatrix},\qquad \Phi_{ij}=\varphi\big(\|\mathbf x_i-\mathbf x_j\|\big)$$

**第二步：策略一——代理引导的差分进化（GSDE 的核心）**

经典 DE 的 best/1 变异是：

$$\mathbf v_i=\mathbf x_{best}+F\big(\mathbf x_{r_1}-\mathbf x_{r_2}\big),\qquad N=50,\ F=0.3\ \text{（默认）}$$

GSDE 的关键改动是**给每个父代点 $\mathbf x_i$ 建一个局部 RBF**：

- 训练点取距 $\mathbf x_i$ 最近的 $5\times D$ 个样本（126 维时即 630 个）；
- 局部区域半径**自适应**：
$$r_i=0.5\,D_{i,\max}\sqrt[D]{\frac{N-1}{D}}$$
其中 $D_{i,\max}$ 是 $\mathbf x_i$ 到其他父代点的最大距离——**维数 D 越高，这个根式越接近 1，半径越大**，这正是对高维稀疏性的补偿；
- 用 PSO（种群 50、迭代 100 次）在这个局部 RBF 上找出**预测父点** $\mathbf x_i^*$。

然后做**指数交叉**，把变异点与预测父点缝合成候选个体：

$$\mathbf u_{i,j}=\begin{cases}\mathbf v_{i,j}, & \mathrm{rand}[0,1]\le CR\ \text{从}\ j=j_{rand}\ \text{起}\\ \mathbf x^{*}_{i,j}, & \text{否则}\end{cases}\qquad CR=0.9\ \text{（默认）}$$

> **请特别注意这里的设计哲学**：在传统"DE + 代理"的松散耦合里，代理模型只用来**筛选**已经生成的候选点；而在 GSDE 里，代理模型**直接参与生成子代的基因**——$\mathbf x_i^*$ 的某些维度会直接进入下一代。原文说得很明确：这种"紧耦合（tight form）"正是 GSDE 在高维下仍能表现良好的特殊之处［原文 P07 §Proposed algorithm］。

**第三步：策略二——基于父代的选择（省下真金白银）**

每轮生成的候选点如果全做 CFD，开销受不了。GSDE 的办法是：**按父代适应度给候选点排序，只挑前 $T=9$ 个去做真实 CFD**。理由是"优秀父代产生的子代更可能携带好基因"。这 9 个子代随后替换父代中最差的 9 个点。

**第四步：策略三——基于代理的局部搜索（并行进行）**

在当前最优点 $\mathbf x_{best}$ 附近再建一个局部 RBF，用 PSO 找局部最优：

$$\mathbf x^*=\arg\min \hat f_{RBF}(\mathbf x),\qquad \mathbf x\in[\mathbf x_{best}-\Delta,\ \mathbf x_{best}+\Delta]\cap[lb,ub]$$

其中 $\Delta$ 取"$\mathbf x_{best}$ 到训练点中适应度最差者距离"的一半。若 $\mathbf x^*$ 优于 $\mathbf x_{best}$ 就替换它。

**这三条策略是并行执行的**——这是 GSDE 适配真实计算集群的工程设计。

#### 4. 算法控制流

```
初始化：采样 → 建 RBF 代理
每轮迭代（三路并行）：
  ① 代理引导差分进化
       对每个父点 xi：
          局部 RBF（5D 个最近邻点，半径 ri 自适应）
          PSO 在局部 RBF 上求预测父点 xi*
          best/1 变异 vi = x_best + F(x_r1 - x_r2)
          指数交叉 vi 与 xi* → 候选 ui
  ② 基于父代的选择
          按父代适应度给所有 ui 排序，只取前 T=9 个送真实 CFD
          用这 9 个子代替换父代中最差的 9 个
  ③ 基于代理的局部搜索（并行）
          在 x_best 附近建局部 RBF，PSO 求 x*，优于 x_best 则替换
```

#### 5. 实验验证与量化突破

**算例一：4 个经典数值函数（50–100 维）**

设计空间归一化到 $[0,1]$，**最大函数调用 1000 次**，每算法独立运行 10 次；基线为 DE、GA 与当时的先进代理辅助进化算法 **GSGA**。结论：**GSDE 在 1000 次调用内找到真实最优解，收敛速率远优于对比算法**［原文 P07 摘要与 Performance validation 节］。

**算例二：3.5 级压气机，126 维（工程级）**［以下设置均出自原文 P07 §Geometry definition / §Numerical method / Table 2］

- 几何：进口导叶 S0 + 3 个低压压气机级（R1/S1、R2/S2、R3/S3），共 7 排叶片；NURBS 参数化，每排 18 变量（根/中/尖各 5 个型线控制点 + 周向平移 + 轴向平移）；型线控制点变化幅度为轴向弦长的 ±0.6% / ±1.2% / ±1.5% / ±1.2% / ±0.6%，周向平移 ±15%，轴向平移 ±5%；
- 求解器：NUMECA FINE/Turbo，三维定常 RANS + Spalart-Allmaras；O4H 六面体结构网格 **322 万节点**，第一层 0.001 mm 保证 $y^+<1$；
- 工况：进口总温 288.15 K、总压 101,325 Pa，出口平均静压 164,000 Pa，转速 8297.7 RPM，壁面绝热无滑移；
- 目标与约束：最大化 $\eta_{is}/p(\mathbf x)$（$p$ 为罚函数），约束总压比在基准的 $[0.98,1.05]$ 倍、流量在基准的 $[0.98,1.02]$ 倍内。
  ［以上几何、求解器、工况与约束设置均出自原文 P07 §Geometry definition、§Numerical method、Table 2 与式 9］

| 参数 | 参考设计 | GSDE(1000) | **GSDE(1500)** | DE |
| :--- | :---: | :---: | :---: | :---: |
| 等熵总对总效率 $\eta_{is}$ (%) | 85.825 | 86.867 | **86.929** | 86.940 |
| 效率增量 (%) | — | +1.042 | **+1.104** | +1.115 |
| 总压比相对变化 (%) | — | −0.068 | −0.074 | −5.7×10⁻³ |
| 流量相对变化 (%) | — | +0.30 | +0.33 | +0.66 |
| **CFD 次数** | — | 1000 | **1500** | **20,000** |

［原文 P07 Table 3］

**怎么读这张表——这里必须诚实，不能夸大：**

- **DE 的最终效率增量 +1.115% 其实略高于 GSDE 的 +1.104%**。所以正确的结论**不是**"GSDE 结果更好"，而是：
 **GSDE 用 1500/20000 = 7.5% 的 CFD 预算，拿到了 DE 约 99% 的效率增益（1.104/1.115 ≈ 99.0%）。**
- 在硬件上（双路 AMD EPYC 7502 32 核 + 256 GB 内存的微服务器，单次 CFD 1~1.5 小时，每轮并行 10 个样本）：GSDE 完成 126 维优化**约 1 周**，DE 完成同等任务**超过 2 个月**［原文 P07 Result analysis］。
- 换算成原文的表述：GSDE 的计算开销约为常用进化算法的 **1/13**。

> ⚠️ **旧版白皮书把这里写成"GSDE 优于 DE"，是不严谨的。请以本表为准。**

**气动机理（后验观察）**：优化后 S0 向吸力面倾斜并后掠；其余三级叶片均向压力面倾斜；三个转子（R1/R2/R3）前掠、三个静子（S1/S2/S3）后掠；除 S0 外，六个叶片的吸力面型线普遍变薄［原文 P07 §Aerodynamic analysis］。

#### 6. 审稿人批判与承前启后

**可以攻击的点**（以下为本文方法层面的合理质疑，涉及的设置参数均出自原文 P07 §Proposed algorithm）：

1. **只在设计点优化**。现代压气机要求在整个工作线（近失速到近堵塞）上都达标，单点优化得到的叶片很可能在非设计点恶化——本文未做多点优化。
2. **超参数多且缺乏敏感性分析**。$N=50,\ F=0.3,\ CR=0.9,\ T=9,\ 5D$ 训练集、PSO(50×100) 基本都是默认值，原文没有报告它们对结果的影响。
3. **局部 RBF 在高维下的"局部性"存疑**。取 $5D=630$ 个"最近邻"点，在 126 维空间里，最近邻与最远邻的距离差距会因距离集中效应而急剧缩小，"局部"可能已经不再局部。
4. **约束处理用了罚函数**，而非更严谨的可行性保持策略；最终方案的总压比相对基准下降 0.074%，虽在约束带内，但确是"贴着下边界"过约束。
5. **气动机理是后验解释**，没有提炼成可迁移的设计准则（例如"转子前掠多少度能改善多少"）。

   ［以上第 1–5 点均基于原文 P07 报告的方法设置与结果所作的合理质疑；其中 $N=50$、$F=0.3$、$CR=0.9$、$T=9$、$5D$ 训练集、PSO(50×100) 等参数出自 §Proposed algorithm］

**承前：** 直接回应了**论文 01（CR-EI）**遗留的天花板——CR-EI 最高只测到 10 维，而 GSDE 把可用维数推到 **126 维**。路线选择上，它放弃了贝叶斯优化的优雅，换取了工程可用性。

**启后：**
- **论文 11（DA-EGO）**：GSDE 走的是"放弃贝叶斯、拥抱进化"；DA-EGO 反过来问"那能不能改造贝叶斯框架本身来扛高维"，答案是用动态子空间分解；
- **论文 08（SW-VAE）与论文 17（GTO）**：GSDE 仍需 1500 次 CFD，这两篇试图用**生成式先验 + 知识迁移**把样本量再降一个量级；
- **论文 19（SHAP-Turbine）**：126 维优化做完之后，自然会追问"这 126 个变量里到底谁在起决定性作用"——这正是 SHAP 可解释性挖掘的出发点。

---

### 【第04讲】DA-EGO：把 126 维切成小块——动态分解与聚合如何救活贝叶斯优化（**论文 11**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **11** |
> | 原始文献 | *Engineering Optimization*, **2025, 57(2): 514–542**（线上发表于 2024-04-18） |
> | DOI | `10.1080/0305215X.2024.2325651` |
> | 作者 | Qineng Wang, Zhendong Guo, Yun Chen, Guangjian Ma, Liming Song, Jun Li |
> | 本地原文 | `A dynamic aggregation strategy enhanced efficient global optimization algorithm...pdf` |
> | 开源代码 | https://github.com/zhet1997/DA_EGO_publish |
> | 证据等级 | **A（本地原文逐句可查）** |

---

#### 1. 生活与物理直觉引子：为什么"分而治之"在优化里这么难

先讲一个生活场景。你要装修一套 126 个房间的超大房子，预算只够请设计师做 **1000 次**方案评审。

**笨办法**：每次都让设计师把整套房子从头到尾重新设计一遍。1000 次下来，每个房间平均只被认真推敲过 8 次——大概率烂尾。

**聪明办法**：把房子按功能分区（客厅区、卧室区、厨卫区……），**每次集中精力改一个区**，其他区保持现状；改完一个区就把它的最优方案"固化"下来，再改下一个区；改完一轮后重新审视：**哪些区之间其实互相影响**（比如开放式厨房和客厅必须一起改），把它们合并后重新规划。

这就是 DA-EGO 的核心思路。但这里有个真正的难点，也是本文的贡献所在：

> **你事先并不知道哪些房间之间互相影响。**

在叶轮机械里，这个"影响"是物理上真实存在的：改动第 1 排叶片，会改变第 2 排的进口气流角，于是第 1 排和第 2 排的变量之间存在**交互作用（interaction）**。把它们拆进不同子空间各自优化，就会得到互相矛盾的结果。

所以 DA-EGO 的关键不是"分解"，而是"**动态**分解"——**每轮迭代都重新判断谁和谁该分在一起**。

#### 2. 叶轮机械中的真实工程死穴：GSDE 的代价与贝叶斯框架的"不甘心"

第 03 讲的 GSDE 用进化算法硬扛 126 维，效果很好，但它放弃了贝叶斯优化（EGO）这套东西。站在贝叶斯阵营看，这是有遗憾的：

- EGO/Kriging 的**样本效率**在**低维**问题上其实远高于进化算法——几十次评估就能逼近最优；
- 它失效的地方是**高维**：Kriging 建模失真、采集函数全局最优化本身变成难题。

于是问题变成：**能不能不动 EGO 的骨架，只把"高维"这件事解决掉？**

本文给出的答案是：**把高维问题分解成若干低维子问题，在每个子问题上用 EGO 好好优化，再把各子空间学到的知识聚合回全空间。** 而且——**分解方案每轮迭代都要更新**。

原文定义的问题规模是 **$d\ge 30$ 的高维昂贵黑箱问题**［原文 P11 摘要］。

#### 3. 数学公式从 0 到 1 推导

**第一步：什么是"变量交互"（interaction）**

设目标函数 $f(\mathbf x)$。若变量 $x_i$ 的取值会**改变** $x_j$ 对 $f$ 的影响，则称 $x_i$ 与 $x_j$ 存在交互。

用方差分析（ANOVA）的语言说：把 $f$ 分解成主效应项与交互效应项

$$f(\mathbf x)=f_0+\sum_i f_i(x_i)+\sum_{i<j}f_{ij}(x_i,x_j)+\cdots$$

若 $f_{ij}\equiv 0$，则 $x_i$ 与 $x_j$ **可分离**，可以安全地分进不同子空间；反之必须放在同一个子空间里一起优化。

**第二步：怎么在只有稀疏样本时判断交互？——PCE 灵敏度系数**

本文用**多项式混沌展开（PCE）**的灵敏度系数 $SPCE_{ij}$ 给每一对变量 $(x_i,x_j)$ 打分。$D$ 个变量共有

$$\frac{D(D-1)}{2}$$

个变量对（126 维时是 7875 对）。DA-EGO 把它们分成三类［原文 P11 §3.2.1］：

| 类别 | 记号 | 含义 |
| :--- | :--- | :--- |
| 确认交互 | $P_C$ | 已由低维代理模型验证确认有交互的 $t$ 对 |
| 疑似交互 | $P_S$ | 在剩余变量对中 $SPCE_{ij}$ 最高的一批，**待验证** |
| 非交互 | — | 其余（可能交互很弱） |

> **原文自己给出的警告（务必记住）**："由于维数灾难的存在，**高维 PCE 代理的精度有限，这会导致交互变量对的选取出现误差**。" 正因如此，$P_S$ 中的疑似交互还要**再用低维代理模型验证确认**。

**第三步：精英点（elite-point）——子空间与全空间之间的知识桥梁**

取全部已评估样本中的**当前最优样本**作为精英点。各子空间优化得到的最优解被**组合进精英点**，再用于全空间的全局搜索。

这一步的意义是：子空间是低维的、好优化的，但它只是"局部视野"；精英点把各局部视野的最优解**拼装成一个全局可行解**，保证子空间优化不会跑偏到全空间的劣解上去。

**第四步：三类知识构成闭环**

原文把"设计空间的知识"明确定义为三类数据［原文 P11 §3］：

1. **采样数据**：所有已评估样本的坐标与函数值；
2. **变量交互关系**：已发现（确认/疑似）的交互变量对；
3. **搜索边界**：每个变量当前的搜索范围。

由此形成闭环：

```
[知识] → 构造子任务（分解方案 + 搜索范围）
            ↓
     各子空间独立做基于代理的优化
            ↓
   子任务数据挖掘（摄动法 + ANOVA 检测交互）
            ↓
[知识更新] → 下一轮的分解方案与搜索范围
```

**第五步：搜索范围收缩**

若此前求解过**相似子任务**（变量相同、精英点不同），则可依据累积经验缩小子问题的搜索范围，从而同时提升优化效率与交互分析的精度［原文 P11 §3.2.2］。

#### 4. 算法控制流

```
① 初始化
     LHS 采样 50 个点并评估
     默认：任意变量间无交互
     默认：每个变量搜索范围 [0,1]

② 循环直到满足停止条件：
   (a) 高维分解
        计算所有变量对的 PCE 灵敏度系数 SPCE_ij
        分为：确认交互 PC / 疑似交互 PS / 非交互
        用低维代理模型验证 PS → 更新 PC
        据 PC 把 D 维空间分解为若干低维子空间
        取当前最优样本作为 elite-point
        若历史上有相似子任务 → 收缩该子问题的搜索范围
   (b) 子问题优化与知识挖掘
        在每个子空间上用 Kriging + EGO 采集函数优化
        用摄动法 + ANOVA 在子空间/全空间中检测变量交互
        把新发现的交互关系写回知识库
        把各子空间最优解组合进 elite-point
```

#### 5. 实验验证与量化突破

**算例一：21 个基准函数，30–90 维**［原文 P11 摘要与 §4］，用于证明算法的效率与鲁棒性。

**算例二：NASA Rotor 37 跨声速压气机转子（28 变量）**

- 参数化：5 个展向截面（0/25/50/75/100% 叶高）× 各 5 个吸力面控制点 = 25 个变量，加 1 个周向平移（x26）与 2 个轴向平移（x27、x28），共 **28 维**［原文 Table 3］；
- 设计条件：转速 17,188.7 rpm，36 个叶片，展弦比 1.19，叶尖间隙 0.356 mm，进口 288.15 K / 101,325 Pa，出口静压 115,000 Pa［原文 Table 4］；
- 网格与求解：约 **400 万节点**，H–O–I 拓扑（NUMECA AutoGrid5），第一层网格 3×10⁻⁶ m，SA 湍流模型，NUMECA FINE/Turbo；**单样本约 600 秒**（Intel i5-9400F）［原文 §5.1.2］；
- 约束：流量与压比均限制在基准值的 **[0.98, 1.02]** 倍内，以罚函数实现；
- 目标：最大化 $\eta_{is}/p(\mathbf x)$，其中 $\eta_{is}=\dfrac{(p^{T}_{outlet}/p^{T}_{inlet})^{(\gamma-1)/\gamma}-1}{T^{T}_{outlet}/T^{T}_{inlet}-1}$。

| 算法 | 效率 | 压比 | 流量 (kg/s) | 效率提升 |
| :--- | :---: | :---: | :---: | :---: |
| 基准设计 | 0.8539 | 2.040 | 20.62 | — |
| **DA-EGO** | **0.8707** | 2.055 | 20.96 | **+1.65%** |
| GSGA | 0.8696 | 2.051 | 20.89 | +1.57% |
| GA | 0.8673 | 2.049 | 20.88 | +1.34% |
| IKAEA | 0.8665 | 2.050 | 20.88 | +1.26% |
| DE | 0.8645 | 2.049 | 20.83 | +1.06% |

［原文 P11 Table 5；每算法独立运行 5 次，每次 1000 次 CFD］

- DA-EGO 相对最强的对手 GSGA 高 **0.08 个百分点**，相对 DE 高 **0.59 个百分点**［由 Table 5 计算］；
- 原文指出：DA-EGO、GA、DE 的 5 次重复运行**方差很小**，说明**受初始样本分布影响小**——这对工程应用的可复现性很关键。

**气动机理**：基准设计的吸力面存在明显的**激波–附面层干扰**现象，尾缘附近发生严重流动分离，尾缘叶尖处还伴有回流；DA-EGO 优化后的分离线显著改善，分离区面积与回流强度都减小，这正是效率提升的流动根源［原文 §5.1.3］。

**算例三：多级轴流压气机（60 变量）**

- 构型：IGV + 2 级（R1/S1、R2/S2）共 5 排叶片，**IGV 不参与优化**；转子每排 18 变量、静子每排 12 变量，合计 **60 维**；
- 网格约 **2300 万节点**，**单样本约 4200 秒**［原文 §5.2.1］。

| 算法 | 效率 | 压比 | 流量 (kg/s) | 效率提升 |
| :--- | :---: | :---: | :---: | :---: |
| 基准设计 | 88.072% | 2.4604 | 53.6465 | — |
| **DA-EGO** | **89.142%** | 2.4644 | 53.9005 | **+1.070%** |
| GSGA | 88.973% | 2.4630 | 53.7780 | +0.901% |
| IKAEA | 88.905% | 2.4623 | 53.8325 | +0.833% |
| GA | 88.804% | 2.4634 | 53.8110 | +0.732% |
| DE | 88.495% | 2.4575 | 53.5025 | +0.423% |

［原文 P11 Table 7；同样 1000 次 CFD 预算］

- 在 60 维这一更接近真实工业规模的算例上，DA-EGO 的优势**比 28 维时更明显**：相对 DE 的效率提升从 +0.59 个百分点扩大到 **+0.647 个百分点**（1.070% vs 0.423%）［由 Table 7 计算］。
- **气动机理**：沿叶高看，优化后 **90% 叶高以上区域效率变化不大、中段叶高明显提升、近端壁处略有下降**；分级看，第一级各叶高均提升约 1%，第二级则是根部（<10% 叶高）与顶部（>85% 叶高）下降、中 span 显著提升。熵函数云图显示流道内三个高熵区（A、B、C）熵值均明显下降，其中 A、B 区下降最显著——原因是叶型改变**削弱了转子 R1 通道内的激波**；中截面的周向静压分布均匀性大幅提高，也佐证了激波的削弱［原文 §5.2.2］。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **作者自己承认的时间复杂度问题**。原文结论原话：该方法"提高了算法的时间复杂度，导致算法自身消耗更多计算资源。虽然这对昂贵样本问题不构成阻碍，但在**样本廉价**的场景下会削弱算法性能；样本的主要计算发生在 Kriging 模型构建，可用增强 Kriging 方法加以解决"［原文 §6 Conclusions］。翻译成人话：**这套方法只在"评估很贵"时才划算**——而这恰恰是叶轮机械的场景，所以问题不大，但必须有这个前提意识。
2. **交互检测的可靠性是方法的阿喀琉斯之踵**。原文明确说高维 PCE 精度有限会导致交互对选取出错。一旦分解错了，子空间优化就是在错误的子空间里做无用功。
3. **搜索范围收缩依赖历史经验，且没有回退机制**。若早期收缩把最优区域提前排除出去了，原文未说明如何恢复。
4. **超参与停止条件未做敏感性分析**：初始 50 个 LHS 样本、1000 次 CFD 预算均为设定值。
5. **仍是单点工况优化**：与 GSDE 相同，只优化设计点，未考虑非设计点性能与失速裕度。
6. **维数仍低于 GSDE**：本文工程算例最高 60 维，而 GSDE 做到了 126 维。两条路线各有适用范围。

**承前：**
- 直接回应了**论文 07 GSDE** 所代表的分歧：高维 HEB 到底该"放弃贝叶斯走进化"还是"改造贝叶斯扛高维"。DA-EGO 选了后者。
- 方法论上继承自 EGO/Kriging 与变量交互分析（PCE、ANOVA）。

**启后：**
- **论文 19（SHAP-Turbine）**：DA-EGO 里的变量交互分析是**为优化服务的副产品**；SHAP 那篇把"变量重要性与交互"本身作为研究目标，做成了可解释的设计知识。
- **论文 13（EMFS / MSFO）**：同样关心"代理模型在哪些区域不可信"，但切入点在保真度而非子空间。

---

### 【第05讲】EMFS / MSFO：当"便宜的粗网格"开始骗人——多保真优化的暗面（**论文 13**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **13** |
> | 原始文献 | *ASME Journal of Turbomachinery*, **2024, 146(4): 041011**（10 页） |
> | DOI | `10.1115/1.4064228` |
> | 论文号 | TURBO-23-1281（线上 2023-12-21） |
> | 作者 | Qineng Wang, Liming Song, Zhendong Guo（通讯）, Jun Li, Zhenping Feng |
> | 本地原文 | **无（ASME 订阅墙）** |
> | 证据等级 | **B（出版商页面 + 摘要全文）** |

> ⚠️ **本讲的写作边界**：本文无本地全文，以下内容**仅取自出版商页面的标题/作者/卷期/摘要原文**，以及可核验的著录信息。
> **凡摘要未给出的数字（具体提升幅度、样本量、DBSCAN 参数、权重公式），本讲一律不写，并明确标注「原文未获取」。**

---

#### 1. 生活与物理直觉引子：老专家带的实习生，也会把你带沟里

第 02 讲（Filter-GEI）讲过"老专家 + 实习生"的比喻：实习生（低保真 LF）便宜、出活快，负责摸清大方向；老专家（高保真 HF）贵，只在关键点出手。

但那个比喻里藏着一个**没被追问的前提**：

> **实习生的判断，大体上是对的。**

如果这个前提不成立呢？想象一下：实习生看图纸有个**系统性偏差**——他总把某种特定结构画得比实际更结实。在设计的早期，这个偏差无所谓，因为大方向是对的；但当设计推进到精细阶段、你已经逼近最优解时，**这个系统性偏差会把你往错误的方向一直带**。

这时候，继续让实习生参与决策，不但不省钱，反而是**负资产**——你花的每一分钱都在加固一个错误。

这就是本文的出发点，而且它是**反直觉**的：

> 多保真优化（MFO）**并不总是**优于单保真优化。在优化的后期，随着高保真样本不断积累，低保真样本会产生**负面效果，误导优化搜索**。［原文 P13 摘要］

#### 2. 叶轮机械中的真实工程死穴：一个被"MFO 一定更好"掩盖的问题

多保真方法（Co-kriging、多保真 EGO 等）在 2010 年代后成为叶轮机械优化的主流工具，因为它确实省钱。行业内逐渐形成一种默认认知："**只要能用多保真，就该用多保真。**"

本文要戳破的正是这个默认认知。原文描述的观察是：

> 基于多保真代理（MFS）的优化，**有时只能在优化早期取得更好的收敛速率，但最终解反而比"只用高保真样本的单保真代理（SFS）优化"更差**。［原文 P13 摘要］

为什么会这样？原文给出的机理是：

> 随着优化过程中 HF 样本的增加，**LF 样本会产生负面效果，从而误导优化搜索**。［原文 P13 摘要］

用物理语言翻译一遍：优化前期，LF 的"大趋势"信息价值极高，因为 HF 样本太少、全靠 LF 撑着全局形状；但到了后期，HF 样本已经足够刻画局部细节，此时 LF 与 HF 之间的**系统性差异**（discrepancy）就从"可容忍的噪声"变成了"**系统性的偏差**"——它会持续把代理模型的最优位置拉偏。

#### 3. 数学公式从 0 到 1 推导

**第一步：把"多保真为什么失效"写清楚**

多保真代理（以 Co-kriging 为例，参见第 02 讲）把 HF 建模为

$$y_{HF}(\mathbf x)=\rho\cdot y_{LF}(\mathbf x)+Z_{DF}(\mathbf x)$$

其中 $Z_{DF}$ 是差异函数（discrepancy function），负责修正 LF 的系统性偏差。

问题在于：**$Z_{DF}$ 本身也是用有限样本拟合出来的高斯过程**。当 HF 样本在优化后期**高度聚集于当前最优附近**时，$Z_{DF}$ 在"你真正关心的那块区域"里的估计反而可能很不准——因为 LF 在那里从来就没被 HF 校验过。

于是出现了本文说的现象：**在 MFS 建不准的那块区域，MFS 反而不如一个纯用 HF 建的单保真代理（SFS）。**

**第二步：核心思想——不是"全用 MFS"或"全用 SFS"，而是"哪里 MFS 不行，哪里就换 SFS"**

本文的做法可以概括成一句话：

> 用 **DBSCAN 密度聚类**找出"MFS 建不准"的区域，在该区域**局部改建一个单保真代理（local SFS）**，再用**自适应权重**把 MFS 与这个局部 SFS 融合成一个集成代理 **EMFS**（Ensemble weighted Multi-Fidelity Surrogate）。［原文 P13 摘要］

**第三步：为什么用 DBSCAN 而不是别的聚类？**

DBSCAN（Density-Based Spatial Clustering of Applications with Noise）的特点是：

- **不需要预先指定簇的个数**——你事先并不知道有几个"失真区"；
- **能识别噪声点**——稀疏的离群样本不会被硬塞进某个簇；
- **能发现任意形状的簇**——失真区的几何形状未必是凸的。

这三点恰好契合"找出代理模型失真区域"这个任务：失真区可能不止一个、形状不规则、且边界处样本稀疏。

> 📌 **DBSCAN 的具体参数（邻域半径 $\varepsilon$、最小样本数 MinPts）与自适应权重的具体公式，摘要未给出，原文未获取，本讲不作推测。**

**第四步：命名规范（旧版白皮书在这里错了，必须纠正）**

| 名称 | 全称 | 指代对象 |
| :--- | :--- | :--- |
| **EMFS** | Ensemble weighted Multi-Fidelity Surrogate | **代理模型**：MFS 与局部 SFS 的自适应加权集成体 |
| **MSFO** | Multi- and Single-fidelity surrogate fused Optimization | **优化算法**：用 EMFS 引导搜索的完整算法 |

> ❌ 旧版白皮书第 05 讲的标题写作"**EMFS 算法**：DBSCAN 空间聚类识别失真区的多保真局部融合"，把**代理模型的名字当成了算法名**。
> ✅ 正确表述：**EMFS 是代理模型，MSFO 才是优化算法。**

#### 4. 算法控制流

依据摘要可确认的流程骨架（**细节参数原文未获取**）：

```
每轮迭代：
  1. 用现有 HF + LF 样本建立多保真代理 MFS（如 Co-kriging）
  2. 用 DBSCAN 对样本/设计空间做密度聚类，
     侦测出 "MFS 在该区域建不准" 的区域
  3. 在这些失真区域内，仅用 HF 样本建立一个局部单保真代理 local SFS
  4. 用自适应权重把 MFS 与 local SFS 融合为集成代理 EMFS
  5. 用 EMFS 引导下一轮加点（这就是 MSFO 算法）
```

> 一句话记住：**不是"多保真 vs 单保真"的二选一，而是"哪里可信就用哪套"的自适应切换。**

#### 5. 实验验证与量化突破

**可确认的验证算例**［原文 P13 摘要］：

1. **GE-E3 叶片优化**（与第 02 讲 Filter-GEI 用的是同一个公开标杆几何）；
2. **涡轮端壁气膜孔布局设计**（the film cooling layout design of a turbine endwall）。

摘要的原话是："Through tests on GE-E3 blade optimization and the film cooling layout design of a turbine endwall, the effectiveness of proposed MSFO is well demonstrated."

> ⚠️ **重要提醒**：摘要**没有给出**任何量化对比数字（提升了多少、节省了多少次 CFD、成功率如何）。
> 因此本讲**不列出任何性能数字**。
>
> 🗑️ **旧版白皮书 6.1 矩阵中写的「250 次 CFD」「15D~30D」「消除低保真模型失真误导现象」等条目，均无出处，属填充内容，已在重建版中删除或标注待补。**

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **"失真区"的判定标准本身依赖代理模型**。用 MFS 去判断"MFS 在哪不准"，存在自我指涉的风险；判定失准时会连锁出错。
2. **DBSCAN 的参数敏感性**。密度聚类对邻域半径与最小样本数非常敏感，而这两个参数与"代理模型误差阈值"之间并无直接的物理换算关系——调参很大程度上靠试。
3. **局部 SFS 的样本从哪里来**？失真区之所以失真，恰恰是因为那里 HF 样本稀少；在 HF 样本稀少的区域建单保真代理，可能同样不可靠。原文未提供此处的处理细节（原文未获取）。
4. **权重的自适应规则缺少理论依据**（原文未获取，无法评价）。
5. **仅两个算例**，且都为该课题组的常规算例，未在更广泛的基准上验证泛化性。

**承前：**
- 直接**修正了第 02 讲（Filter-GEI）的隐含前提**。Filter-GEI 假定"LF 大体能捕捉 HF 趋势"，并据此设计滤波阈值；本文则处理**这个前提在局部失效**的情形。两篇论文构成"多保真方法"的一体两面。
- 方法上继承了 Co-kriging 与多保真 EGO 的体系。

**启后：**
- **论文 07（GSDE）与论文 11（DA-EGO）**：如果说 EMFS 处理的是"**保真度维度**上的模型不可信"，那么 GSDE 与 DA-EGO 处理的是"**维度灾难**带来的模型不可信"。三者共享同一个哲学——**先识别代理模型在哪里不可信，再在那里换一种策略**。
- **论文 19（SHAP-Turbine）**：同样是对"模型/数据不可信"的反思，但转向可解释性视角。

---

> 📎 **如果想把本讲升级为 A 级证据**，需要取得 ASME 全文（DOI `10.1115/1.4064228`）。届时可在 `corpus/facts/P13.md` 中补全：DBSCAN 参数、自适应权重公式、Table 中的量化对比结果。

---

# 第二篇 【生成式模型与跨任务知识迁移篇】

本篇覆盖**论文 06、08、17、14**。核心追问是：**能不能不每次都从零开始设计？** 把叶型、端壁这类高维几何压进一个低维潜空间（GMFoO、GAN 参数化），再把上一个型号学到的设计知识迁移到新型号（SW-VAE、GTO），从而把昂贵的优化样本量再降一个量级。

---

### 【第06讲】GMFoO：一个潜空间不够，那就同时用好几个（**论文 06**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **06** |
> | 原始文献 | *IEEE Transactions on Cybernetics*, **2023, 53(7): 4347–4360** |
> | DOI | **`10.1109/TCYB.2022.3165044`** |
> | 作者 | Zhendong Guo, Haitao Liu, Yew-Soon Ong, Xinghua Qu, Yuzhe Zhang, Jianmin Zheng |
> | 本地原文 | `Generative_Multiform_Bayesian_Optimization.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

> ❗ **著录更正**：仓库旧文档（README、全集汇总、论文链接整理）给出的 DOI 为 `10.1109/TCYB.2022.3168744`，**尾号错误**。原文首页印刷及第三方期刊的参考文献著录均为 **`10.1109/TCYB.2022.3165044`**。详见 `corpus/doi_verification.md`。

---

#### 1. 生活与物理直觉引子：描述"形状"，比描述"数字"难得多

前面几讲处理的设计变量，本质上都是**一串数字**：叶型控制点的偏移量、叶片的周向平移量……变量再多，改变一个变量，形状就"连续地"变一点点。数学上这叫**欧氏空间**。

但真实工程里有一大类问题不是这样。比如：

- 一个翼型，用 **192 个点**离散描述，共 **384 个坐标变量**［原文 P06 §IV-C］。你随便挪动其中一个点，大概率得到一个**锯齿状、根本飞不起来**的怪物形状。
- 一个分子结构、一段程序代码、一个拓扑结构——这些是**离散空间**或**非欧空间**。

这类空间有两个特点：① 维度极高；② **变量之间高度耦合**（原文用词是 highly interacted），必须"成组地"改变才有意义。在这上面直接做贝叶斯优化，几乎必然失败——高斯过程会认为"每个点都可以独立乱动"。

**生成模型（GAN / VAE）给出的解法**：先学一个从"低维潜空间"到"合法形状"的映射。潜空间里每个点都对应一个**看起来像真翼型**的形状，于是在潜空间里优化就安全多了。这套两阶段流程叫做 **GMO（Generative Model-based Optimization）**。

#### 2. 叶轮机械中的真实工程死穴：潜空间维度，选大还是选小？

GMO 好用，但有一个绕不开的抉择：**潜空间取多少维？**

这是一个**两难**：

| 潜空间维度 | 优点 | 缺点 |
| :--- | :--- | :--- |
| **低维**（如 3–4 维） | 搜索空间小，**收敛极快** | 表达能力不足，很多好形状**表示不出来** → 最终解精度差 |
| **高维**（如 13–23 维） | 表达能力强，**最终解精度高** | 搜索空间大，贝叶斯优化**收敛慢** |

原文把这个矛盾概括为：**潜空间维度难以确定，会引发"解的精度（solution accuracy）"与"收敛速率（convergence rate）"之间的冲突**［原文 P06 摘要］。

而且这个冲突在叶轮机械上格外要命：因为每次评估是 CFD，**"收敛慢"直接等于"多烧几个月的机时"**。

本文的答案很巧妙：**既然选大选小都有道理，那就同时用几个不同维度的潜空间，让它们互相帮忙。**

#### 3. 数学公式从 0 到 1 推导

**第一步：把"多空间协同"写成优化框架**

$$\min_{\mathbf x\in\mathcal X} f(\mathbf x)=\min\{T_0,\ T_1,\ \dots,\ T_m\ |\ \mathcal M(t)\}$$

- $\mathcal X$：结构化的原始输入空间（384 维翼型坐标）；
- $T_0$：在**高维潜空间** $Z$ 上做的优化子任务；
- $T_i$：在**低维潜空间** $C_i$ 上做的优化子任务；
- $\mathcal M(t)$：**知识库**，负责在子任务之间交换信息。

拆开写就是：

$$T_0=\min_{\mathbf z\in Z} y_0(\mathbf z),\qquad T_i=\min_{\mathbf c_i\in C_i} y_i(\mathbf c_i),\quad i=1,\dots,m$$

**第二步：关键设计——让潜空间之间"正相关"**

如果各个潜空间彼此独立，那"同时优化"就退化成"各自乱跑"。本文借 InfoGAN 的思路，在 GAN 的训练损失里加入**最大化互信息**的正则项：

$$\min_G\max_D V_M(D,G)=V(D,G)-\sum_{i=1}^{m}\lambda_i\,I\big(c_i;\,G(z)\big)$$

互信息 $I(c;G(z))=H(c)-H(c|G(z))$ 不可直接算，用因子化高斯 $Q(c|x)$ 做变分近似，得到下界：

$$L_I(c;G(z))=-\sum_{j=1}^{|c|}\left[\frac{(c_j-\mu_j)^2}{2\sigma_j^2}+\log\big(\sigma_j\sqrt{2\pi}\big)\right]+H(c)$$

训练时令 $\mu_j=c'_j$、把 $H(c)$ 视为常数，于是优化目标变为**最小化 $c$ 与逆推断 $c'$ 之间的差距**：

$$\min\ -L_I\simeq\min\ \sum_{j=1}^{|c|}\left[\frac{(c_j-c'_j)^2}{2\sigma_j^2}+\log\big(\sigma_j\sqrt{2\pi}\big)\right]$$

**物理含义**：逼着低维潜空间 $C$ 去"复述"高维潜空间 $Z$ 生成出来的物体样貌。这样一来，$C$ 的每一维都对上了物体的某个**主要变化特征**，两个空间就**正相关**了——知识才能在它们之间传递。

这个生成模型被命名为 **MFoO-GAN**。

**第三步：两个方向上的样本交换**

- **低维 → 高维（$C\to Z$）**：把 $z=[c,\ z^*]$ 里的"剩余变量" $z^*$ **固定为常数**（例如置零）。
 三个好处：① 搜索**优先沿物体的主要变化方向**推进；② 避免 $z^*$ 随机化引入噪声，使 $C$ 中的样本在下一轮**可以复用**；③ 从 $C$ 到 $Z$ 的变换变得非常简单。
- **高维 → 低维（$Z\to C$）**：走**判别器的逆推断结构**，取后验 $Q(c'|x)$ 的均值 $c'$。
 相比作为先验的 $c$，$c'$ 通过 $\sigma_j^2$ 把 $z^*$ 带来的变化也计入了，**逆推断更稳健**。

**第四步：以贝叶斯优化实例化时的两个策略**

**策略一：用低维空间的最优解，去收窄高维空间的搜索范围**

低维空间 $C$ 捕捉的是物体的主要变化，因此它的当前最优解 $c_{min}$ 很可能就落在真实最优解附近。于是用 $c_{min}$ 在高维空间 $Z$ 里划出一块**收窄区域**集中搜索，区域大小由参数 $\Delta$ 控制（**默认 0.15**）［原文 P06 §III-B-1、§IV-B］。

> 这一步的直觉：**先用 4 维空间快速锁定大致方位，再让 23 维空间在那个方位附近精雕细琢。**

**策略二：把"跨空间变换来的样本"当低保真样本，用多保真 GP 消化**

从 $Z$ 变换到 $C$、或从 $C$ 变换到 $Z$ 的样本，都伴随**精度损失**（毕竟经过了压缩/逆推断）。本文的处理很聪明：**不丢掉它们，而是把它们当作低保真样本**，构建多保真高斯过程（MFGP），用协方差矩阵作为知识库 $\mathcal M(t)$：

$$k_M(c,C^*)=\big[\rho_{11}k(c,C),\ \rho_{12}k(c,C')\big]$$

$$K_M(C^*)=\begin{pmatrix}\rho_{11}K(C) & \rho_{12}K(C,C')\\ \rho_{21}K(C',C) & \rho_{22}K(C')\end{pmatrix}+D$$

其中 $\rho_{ij}$ 建模高保真与低保真样本的相关性，$D$ 为噪声对角阵。

> 这一步与第 02 讲（Filter-GEI）的多保真思路一脉相承，但"低保真"的来源从**粗网格 CFD** 变成了**跨潜空间的变换**——这是个很漂亮的迁移。

#### 4. 算法控制流（原文 Algorithm 3）

```
输入：目标优化问题、结构化输入空间的数据集 X
 1. 训练 MFoO-GAN，得到高维潜空间 Z 与若干个正相关的低维潜空间 C
 2. 用 LHS 在各潜空间生成初始样本并评估，得到 {Z, Y(Z)}、{C, Y(C)}
 3. while 未满足终止条件：
 4.   for 每个潜空间 Zi ∈ {Z, C}:
 5.       建立/更新多保真 GP 代理（MFGP）
 6.       用 EI 采集函数选出下一个查询点 z*_i = arg max EI(z_i)
 7.       评估新样本的真实目标值，更新该空间的训练集
 8.   end for
 9.   高维空间的强化开发：
        a. 用低维空间当前最优 c_min 更新 Z 的收窄区域（尺寸参数 Δ）
        b. 在收窄区域内用 EI 继续搜索
        c. 在 Z 与 C 之间做样本交换（图 3 的两个方向）
10. end while
```

#### 5. 实验验证与量化突破

**算例一：低速翼型优化**

- 原始空间：**192 个点 → 384 个设计变量**，变量高度耦合；
- 目标：最大化升阻比 $L/D$，用 **XFOIL** 计算；$Re=1.8\times10^6$、$Ma_\infty=0.01$、$AoA=0^\circ$；
- MFoO-GAN：**高维潜空间 Z = 13，低维潜空间 C = 3**；SVD 基线潜空间维度同为 13；
- 每组实验重复 **10 次**［原文 P06 §IV-C-1］。

**算例二：亚声速翼型优化**

- 工况改为 $Ma_\infty=\mathbf{0.45}$（其余同低速算例）；
- MFoO-GAN：**Z = 23，C = 4**；SVD 潜空间维度 23［原文 P06 §IV-C-2］。

**算例三**：corbel（牛腿结构）设计；**算例四**：面积最大化问题［原文 P06 摘要］。

**基线共六类**［原文 P06 §IV-A］：GMO-High、GMO-Low（单潜空间 + 标准 BO）、GMO-NashEGO（多形式 BO，交替子空间**随机**生成）、SVD-BO（SVD 线性降维 + BO）、GMO-CMAES、GMO-IKEA。

**核心结论（原文明确表述，均为定性结论）**：

1. **GMFoO 的收敛速率与最终解都优于 SVD-BO。** 原因：SVD 本质上是**线性**降维模型，其潜空间的解精度不如 MFoO-GAN 这类非线性深度模型［原文 §IV-C］。
2. **GMO-Low 收敛最快、但最终解较差；GMO-High 最终解更好、但收敛更慢**——这正是第 2 节说的两难。原文原话：在 23 维设计空间中的 GMO-High，其收敛速率明显慢于在 4 维空间中的 GMO-Low［原文 §IV-C］。
3. **GMFoO 同时在两个潜空间上优化、互相补足，因而既拿到最好的解，又有更快的收敛速率。**
4. **一个重要的反例**：同样做"多形式优化"的 **GMO-NashEGO 反而差于**单潜空间的 GMO-High / GMO-Low。原因是它的交替子空间是**随机生成**的，缺少 GMFoO 那种"正相关"设计来保证知识有效传递［原文 §IV-C］。

> 📌 **这条反例特别有价值**：它证明"多空间并行"本身不带来收益，**真正带来收益的是"空间之间建立了可用 correspondence（对应关系）"**。

> ⚠️ **数字说明**：原文以收敛曲线图（10 次运行的阴影区）与最终解箱线图呈现结果，**摘要与正文未给出统一的数值表格**。本讲因此不引用任何具体提升百分比——**凡原文未给出的数字，本讲不写**。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **核心问题只被缓解、未被根除**。本文的动机是"潜空间维度难定"，解法是"高低搭配"，但高维维度（13 / 23）仍是按算例人为给定，低维维度 $d_L$ 仍要在 [3,5] 区间里试。
2. **新增两个超参**：收窄尺寸 $\Delta$（默认 0.15）与低维维度 $d_L$。原文做了敏感性分析（§IV-F），但默认值仍是经验值。
3. **算例的"昂贵"程度有限**：翼型用 XFOIL（准二维、秒级求解），**并非真实的三维 RANS 叶栅**。本文没有在叶轮机械的昂贵 CFD 场景上验证——这一点必须诚实，因为"昂贵黑箱"的全部前提在本文算例中是被弱化的。
4. **生成模型依赖训练数据分布**：MFoO-GAN 在 UIUC 翼型数据库上训练；若目标形状显著偏离训练集（例如全新的超临界叶型、涡轮端壁），潜空间的表达能力会打折。
5. **工程链路复杂**：Python（GAN）+ MATLAB（BO）+ PERL（胶水脚本）三语言串联，复现与部署成本高。

**承前：**
- 直接延续**论文 01（CR-EI）**：CR-EI 用 GAN 做翼型参数化（13 维），本文则把"潜空间维度该怎么定"本身变成研究问题。
- 方法论上继承 GMO 两阶段框架、InfoGAN、贝叶斯优化与多保真 GP。

**启后：**
- **论文 08（SW-VAE）**：把"多潜空间 + 知识迁移"从**同一任务内的并行**推进到**跨任务迁移**——上一个型号学到的设计知识，迁移到新型号上；
- **论文 17（GTO）**：进一步引入**梯度重参数化**，让生成式潜空间能直接支撑梯度式迁移优化；
- **论文 14（GAN-Endwall）**：把 GAN 参数化从翼型推广到非轴对称端壁。

---

### 【第07讲】SW-VAE / KT-ASO：把上一个型号的设计经验"搬"到新型号上（**论文 08**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **08** |
> | 原始文献 | *Aerospace Science and Technology*, **2024, 147: 108998** |
> | DOI | `10.1016/j.ast.2024.108998` |
> | 作者 | Zhendong Guo, Cunxi Li, Yun Chen, Liming Song\*, Jun Li, Zhenping Feng |
> | 本地原文 | `1-s2.0-S1270963824001317-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

> 📌 **命名规范**：完整流程叫 **KT-ASO**（Knowledge Transfer accelerated ASO，知识迁移加速的气动形状优化）；**SW-VAE**（sample-weighted VAE）是其中的**参数化组件**。说"SW-VAE 算法"是不准确的。

---

#### 1. 生活与物理直觉引子：为什么"相关经验"能帮你，也可能坑你

先看一个生活场景。你是一名厨师，已经把"红烧肉"做到了极致。现在老板让你做一道**从没做过**的"红烧牛腩"。

你会怎么做？显然不会从零开始瞎试——你会把红烧肉的经验搬过来：先煸炒、再上糖色、小火慢炖……**因为这两道菜的物理过程高度相似**。这就是**迁移学习（transfer learning）**。

但这里有个陷阱：如果你把"猪肉要炖 90 分钟"这条经验**原封不动**地套到牛腩上，可能就炖过头了。**经验有价值，但不能照搬**——用得不好，反而比不用更糟。在优化领域，这叫**负迁移（negative transfer）**。

把这个比喻映射到涡轮叶片设计：

- **源任务（source task）**：某个已经优化完成的型号（比如 GE-E3 低压涡轮叶片在原负荷下）；
- **目标任务（target task）**：新的设计要求（比如把叶片数从 120 减到 100，成为超高负荷叶片）；
- **要迁移的知识**：源任务优化过程中积累的**样本**（几何 + 对应的 CFD 评估结果）。

本文要做的事：**让这些"上一个型号"的样本，帮新型号的优化少烧几百次 CFD。**

#### 2. 叶轮机械中的真实工程死穴：高保真 CFD 越用越贵，预算却越来越少

原文开头的逻辑链很清晰：

> 涡轮叶片气动形状优化（ASO）是典型的昂贵黑箱问题。**随着设计要求日益严格，高保真仿真的需求上升，仿真成本急剧增加，这意味着一个优化周期内允许的仿真调用次数越来越少。**［原文 P08 摘要］

前几讲的所有方法（CR-EI、Filter-GEI、GSDE、DA-EGO）都在回答"**下一个点算在哪里**"。本文换了一个更根本的问法：

> **能不能干脆不"从零开始"？**

如果上一个型号的优化过程已经产生了成百上千个样本，而这些样本与新型号共享大量气动规律，那么**把这些历史样本当"免费的知识"用起来**，是不是就能大幅减少新任务需要的 CFD 次数？

本文给出的答案：是的，而且效果显著——**取得相近最优解时，KT-ASO 至少减少 50% 的函数调用**［原文 P08 摘要］。

#### 3. 数学公式从 0 到 1 推导

**第一步：VAE 参数化与它的损失函数**

用 VAE 把叶型（**202 个二维散点**）压进 **10 维潜空间**［原文 Table 3、§5.2］。损失函数是三项加权：

$$L(x)=KL(x)+\lambda_1\,MSE(x)+\lambda_2\,L_P(x)$$

- $KL$：KL 散度，约束潜空间的分布；
- $MSE$：输入叶型与重构叶型的均方误差；
- $L_P$：**叶片几何专属的正则项**——约束尾缘不薄于基准，防止生成畸形叶型：

$$L_P=\mathbb E\left[\left(\max\left\{0,\ \frac{r^{ref}_{min}-r_{min}}{r^{ref}_{min}}\right\}\right)^2\right]$$

实验取 $\lambda_1=1,\ \lambda_2=500000$。

此外，解码器末端嵌入了一个 **FFD 层**（6×5 控制点网格，共 **50** 个主动变量），以保证生成叶型的光顺性。

**第二步：核心难题——为什么源样本会被"淹没"，进而引发负迁移**

原文引用 Tripp et al. 的观察：

> VAE 所学设计空间中的样本分布，**大致正比于其在训练集中的出现频率**。

这带来两个连锁问题：

1. VAE 有效训练需要**上千个**样本（本文用了 **1010** 个无标签叶型），而源样本只有**几十个**（本文每轮用 **40** 个）。直接混在一起训练，源样本的独特特征会被彻底淹没。
2. 更糟的是，源样本是"已完成任务的优化解"，它们往往**落在训练集分布之外**。于是用基础 VAE 把源样本重参数化进潜空间时，**重构出的叶型和原始叶型差得很远**——迁移过来的"知识"本身就是错的，用它引导优化只会拖后腿。这就是**负迁移**。

**第三步：样本加权训练策略（SW-VAE 的核心）**

解法分两层。第一层：**把源样本纳入训练集**（让它不再位于分布之外）。

$$\mathcal L_w=\sum_{i=1}^{n^{(S)}}L\big(x^{(S)}_i\big)+\sum_{j=1}^{n^{(T)}}L\big(x^{(T)}_j\big)$$

关键在于：**在每个 epoch 的每个 batch 中，固定源样本数 $n^{(S)}$ 与无标签样本数 $n^{(T)}$ 的比例**（实验中取 $n^{(S)}/n^{(T)}=1$），分别从源样本集 $X_S$ 与无标签集 $X_U$ 中随机抽取。

这样一来，哪怕源样本只占全集的极小一部分，它们的特征也能被稳定地学进去。

**第四步：为什么"纳入源样本"还有第二个好处？**

因为**源任务与目标任务的最优解，特征上是相似的**（本文的 t-SNE 可视化证实了这一点：两者的最优解落在潜空间的相近区域）。

所以，把源样本学进潜空间，等价于**人为提高了潜空间中"高分候选解"的比例**——优化器一上手就站在离好解更近的地方。

**第五步：多保真代理——把源样本当低保真数据**

源任务与目标任务的趋势"相似但不完全相同"，因此最合理的处理是：

> **源样本 = 低保真样本；目标样本 = 高保真样本。**

多保真高斯过程的协方差结构：

$$K_M(Z^*)=\begin{bmatrix}K_T & \rho\,K_{ST}\\ \rho\,K_{TS} & K_S\end{bmatrix}+D,\qquad k_M(z,Z^*)=\big[k(z,Z_T),\ \rho\,k(z,Z_S)\big]$$

$$\hat y_M(z)=k_M(z,Z^*)K_M^{-1}[Y_T,\ Y_S]^{\top},\qquad
\sigma_M^{2}(z)=k_M(z)-k_M(z,Z^*)K_M^{-1}k_M(Z^*,z)+\sigma^{2}_{n,1}$$

其中 $\rho$ 建模源与目标的相关性，$D$ 是噪声对角阵。随后用多保真版 EI 引导搜索：

$$EI_M(z)=\big(y^{(t)}_{min}-\hat y_M(z)\big)\Phi(u_M)+\sigma_M(z)\,\varphi(u_M),\qquad u_M=\frac{y^{(t)}_{min}-\hat y_M(z)}{\sigma_M(z)}$$

> 这一步与第 02 讲（Filter-GEI）的多保真思想同源，但"低保真"的来源从**粗网格 CFD** 换成了**历史任务的样本**——这是本讲最值得记住的迁移。

#### 4. 算法控制流（KT-ASO）

```
(1) 训练样本加权 VAE
      无标签叶型集 XU（1010 个，由基准叶型做 ±10% 几何扰动生成，不经 CFD）
    + 源样本集 {xS, yS}m（已完成任务的高分样本）
    → 按固定 n(S)/n(T) 比例分 batch 训练

(2) 用 LHS 在 VAE 潜空间生成目标任务的初始样本，做 CFD 评估 → {zT, yT}

(3) 用 VAE 编码器把源样本 {xS, yS}m 重参数化进潜空间 → {zS, yS}m

(4) 合并 {zT, yT}（高保真）与 {zS, yS}（低保真），建立多保真 GP

(5) 用多保真 EI 选下一个查询点，评估后加入 {zT, yT}

(6) 重复 (4)(5) 直至满足终止条件
```

#### 5. 实验验证与量化突破

**数值验证（先验证多保真迁移本身）**［原文 Table 1、Fig. 8］

- 源任务：$0.9\times$Ackley6d $+0.1\times$Rosenbrock6d；目标任务：Ackley6d（均为 6 维，$x_i\in[-2,2]$）；
- 源样本先在源任务上跑标准 BO 获得；初始样本数与源样本数均设为 6 倍维度；10 次独立运行；
- 结论：多保真版（MFO）**收敛更快、最终解明显更好**。

**工程算例：GE-E3 低压涡轮第二级转子叶片（rb23）的超高负荷改型**

- 做法：把叶片数从 **120 减到 100**（栅距由 25.006 mm 增大到 **30.0072 mm**），进出口边界条件保持不变；
- 代价：尾缘出现明显分离泡，**总压损失系数 $C_{pt}$ 从 4.22% 恶化到 12.99%**［原文 Table 2、§5.1］；
- 优化目标：最小化 $C_{pt}$，约束流量在基准的 $[0.98,1.02]$ 倍内、尾缘最小半径不薄于基准；
- 边界条件：进口气流角 49.3°、进口总压 319,339.2 Pa、进口总温 1161 K、出口静压 245,618 Pa；轴向弦长 26.6 mm；
- 源任务：同一叶型在**原负荷**下的优化（假设已完成）；先在源任务上跑 10 次 BO 收集 **1400** 个中间样本，取 $C_{pt}$ 最低的 **400** 个为候选，每次 KT-ASO 运行随机抽取 **40** 个作为源样本；
- 样本预算：所有对比流程统一 **140 次 CFD**，各运行 10 次。

**先看重参数化精度——这是迁移能否成立的前提**［原文 Table 4，归一化 MSE］

| 模型 | 重构误差 |
| :--- | :--- |
| 基础 VAE | $(8.610\pm3.168)\times10^{-3}$ |
| **样本加权 VAE** | $(1.986\pm1.572)\times10^{-4}$ |

> **误差降低一个数量级以上。** 原文的可视化也显示：基础 VAE 的潜空间里，40 个源样本落在**边缘区域**（几乎不被覆盖），重参数化后叶型与原叶型有明显偏差；而样本加权 VAE 的潜空间很好地覆盖了源样本，重参数化后**几乎完全重合**。

**再看最终优化结果（10 次运行的中位数）**［原文 Table 5］

| 方法 | $C_{pt}$ (%) | 流量 $\dot m$ | 出口气流角 (°) |
| :--- | :---: | :---: | :---: |
| 基准（超高负荷 rb23） | 12.99 | 475.02 | −60.86 |
| FFD-BO（传统参数化） | 4.96 | 472.45 | −61.04 |
| GAN-BO | 4.86 | 474.47 | −60.95 |
| VAE-BO（基础 VAE） | 4.85 | 470.18 | −61.25 |
| **VAE-W-BO**（样本加权 VAE） | 4.75 | 468.78 | −61.35 |
| **KT-ASO**（加权 + 多保真迁移） | **4.52** | 473.32 | −61.06 |

**这张表设计得非常干净，可以做两步"消融"：**

1. **FFD-BO → VAE-BO**（4.96 → 4.85）：验证**生成式参数化**优于传统 FFD。原因是 FFD 的设计空间是围绕**单个基准叶型**扰动出来的，而 VAE 的设计空间是从 **1010 个不同叶型**中学出来的，形状多样性（高分候选解比例）高得多。
2. **VAE-BO → VAE-W-BO**（4.85 → 4.75）：单独验证**样本加权**的收益。
3. **VAE-W-BO → KT-ASO**（4.75 → 4.52）：单独验证**多保真知识迁移**的收益。

- KT-ASO 相对基准把 $C_{pt}$ 从 **12.99% 降到 4.52%**；
- 相对最好的非迁移基线 VAE-BO（4.85%），再降 **0.33 个百分点**［由 Table 5 计算］；
- 摘要给出的核心结论：**与无知识迁移的 ASO 流程相比，KT-ASO 在取得相近最优解时至少减少 50% 的函数调用**；在相同样本预算下则取得远优于对比流程的解。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **源任务与目标任务是"人为构造"的同构问题**。目标是同一 rb23 叶型减叶片数得到的超高负荷改型，源任务是同一叶型在原负荷下——两者**过于相似**。真实工程中"相关任务"未必这么规整，**源与目标差多远时迁移会失效，本文没有给出判据**。
2. **负迁移的边界未被量化**。本文用样本加权**规避**了负迁移，但没有系统扫描"源目标相似度 vs 迁移收益"的曲线。
3. **训练样本 $X_U$ 不含气动信息**。1010 个叶型是纯几何扰动生成的、未经 CFD，只用于学形状流形——潜空间本身没有气动偏好。
4. **只是二维中截面优化**。作者本人在结论中也承认框架"可进一步扩展到三维多学科优化"，即**本文尚未做到三维**。
5. **超参偏经验**：$\lambda_2=500000$、$n^{(S)}/n^{(T)}=1$、源样本 40 个、潜空间 10 维，均未见系统的敏感性分析。
6. **GAN-BO 基线的公平性存疑**。原文自己也说 GAN 训练更困难，且其"样本重参数化与样本加权训练"尚需研究——也就是说，GAN-BO 是个"没吃饱"的对手。

**承前：**
- 继承 VAE/GAN 参数化（**论文 06 GMFoO**）与多保真 GP（**论文 02 Filter-GEI**、**论文 06**）。
- 关键跃迁：**GMFoO 是"同一任务内多潜空间并行"，本文是"跨任务知识迁移"**——后者才是真正能把 CFD 次数降一个量级的方向。

**启后：**
- **论文 17（GTO）**：本文的迁移是"把源样本当低保真喂给代理模型"；GTO 更进一步，用 **GAN + 梯度重参数化**让源域知识能直接以梯度方式参与目标优化；
- **论文 13（EMFS / MSFO）**：两者处理的是同一个隐患——"低保真（源）样本会不会误导优化"。本文靠固定的多保真核处理，EMFS 则用 DBSCAN 主动侦测失真区，是同一问题的两种解法。

---

### 【第08讲】GTO：GAN 不会"反推"，那就用梯度硬算出来（**论文 17**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **17** |
> | 原始文献 | *Aerospace Science and Technology*, **2026, 177: 112324** |
> | DOI | `10.1016/j.ast.2026.112324` |
> | 作者 | Fei Zeng（中国航发湖南动力机械研究所）, Cunxi Li, Liming Song, Zhendong Guo（通讯） |
> | 本地原文 | `1-s2.0-S1270963826007042-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

> 📌 **命名**：整体框架叫 **GTO**（Generative Transfer Optimization）；实验中把具体实现标为 **GAN-TO**，又分 **GAN-TO-MFS**（用 Co-kriging 多保真）与 **GAN-TO-EMFS**（用集成加权多保真）两个版本。

---

#### 1. 生活与物理直觉引子：别人给的图纸，只有一堆坐标点

第 07 讲讲过"把上一个型号的经验搬过来"。那里有个隐含的便利条件：**源样本是我们自己用同一套参数化方法生成的**，所以能通过 VAE 的编码器顺利映射进潜空间。

现在把场景换成更真实的工程处境：

> 你接手一个新叶型的设计任务。前辈留给你的，是**几百个优化过的叶型，每个都由上百个坐标点描述**——但**当时用的是什么参数化方法（NURBS？FFD？控制点怎么定义的？），并没有留下**（甚至这些结果来自别的单位）。

这时候，VAE 那套"用编码器逆推断"的便利没有了。更麻烦的是，如果你改用生成质量更强的 **GAN**，问题会更尖锐：**GAN 根本没有逆推断能力**——它只有生成器 $G: z\to x$，没有编码器 $x\to z$。

原文把这一点说得很直白：

> 当我们把源样本与目标样本一起用坐标散点表示时，可以用 GAN 学出一个**共享潜设计空间**……但**如何把已完成任务的解仿射到待求解问题的设计空间，并不是一件平凡的工作**。这可能解释了为什么迁移优化在以往的涡轮设计研究中鲜有涉及。［原文 P17 §3.1］

本文的第一个贡献，就是**给 GAN 补上这个"反推"能力**。

#### 2. 叶轮机械中的真实工程死穴：二维叶型优化的 CFD 预算

本文的任务场景是**高负荷低压涡轮（LPT）叶片**设计。为什么高负荷叶片重要？

> 为提升航空发动机推重比、降低油耗，人们倾向于**减少叶栅中的叶片数量**。但**高负荷 LPT 叶片的气动损失会显著增加**。［原文 P17 §5.1 同类论述］

具体到本文：以 GE-E3 低压涡轮第二级转子叶片中截面（rb23）为参考，把**栅距从 25.006 mm 增大到 30.0072 mm**（等价于叶片数从 120 减到 100），进出口边界条件不变。代价是：**尾缘出现明显分离泡，总压损失系数从 4.22% 恶化到 12.99%**。

所以目标是：在这个高负荷条件下把总压损失重新压下去——而 CFD 预算只有**每次运行不到 140 次评估**［原文 §4.3］。

#### 3. 数学公式从 0 到 1 推导

**第一步：样本加权 GAN（沿用第 07 讲的思想，换到 GAN 上）**

GAN 损失加一项几何正则：

$$L(x)=\min_G\max_D V(D,G)+\lambda\,L_P(x)$$

$$\min_G\max_D V(D,G)=\mathbb E_{x\sim P_{data}}\big[\log D(x)\big]+\mathbb E_{z\sim P_z}\big[\log(1-D(G(z)))\big]$$

正则项 $L_P$ 用**叶型最大曲率**来抑制尖角与自交（这是叶型生成的关键工程细节）：

$$\kappa_{max}=\max\frac{|\dot x\ddot y-\ddot x\dot y|}{\big[\dot x^{2}+\dot y^{2}\big]^{1.5}}$$

（$\dot x,\ddot x,\dot y,\ddot y$ 用中心差分计算。）

训练数据两部分的**数量极不平衡**：无标签叶型 $X_U$ 有 **1030** 个，源样本只有 **40** 个。直接混训会淹没源样本特征，于是沿用第 07 讲的**样本加权**策略：

$$\mathcal L_w=\sum_{i=1}^{n^{(S)}}L\big(x^{(S)}_i\big)+\sum_{j=1}^{n^{(T)}}L\big(x^{(T)}_j\big)$$

在每个 batch 中固定 $n^{(S)}/n^{(T)}=1$——**每批一半来自源样本、一半来自无标签样本**。

**第二步：本文的核心创新——梯度重参数化**

把"把一个坐标叶型 $\mathbf x$ 映射回潜向量 $\mathbf z$"写成一个优化问题：

$$\boxed{\ \mathbf z^{*}=\arg\min_{\mathbf z}\ \big\|G(\mathbf z)-\mathbf x\big\|_2^{2}\ }$$

然后用 PyTorch 的自动微分做梯度下降：

$$\mathbf z^{(n+1)}=\mathbf z^{(n)}-\eta\cdot\frac{\partial\big\|G(\mathbf z^{(n)})-\mathbf x\big\|_2^{2}}{\partial \mathbf z^{(n)}}$$

> **一句话理解**：GAN 没有编码器？那就**用生成器本身做"反向搜索"**——不断调整 $z$，直到生成出来的叶型和目标叶型重合。

原文的可视化验证：把源样本先经梯度重参数化得到 $z$，再用 $G(z)$ 重构出叶型 $\hat x$；**原叶型与重构叶型几乎完全重合**［原文 Fig. 11、§4.2.2］。

**第三步：多保真迁移——源样本当低保真**

$$\hat y_{MFS}(\mathbf x)=\rho\cdot\hat y_{LF}(\mathbf x)+d(\mathbf x)$$

其中 $\rho$ 为缩放因子、$d(\cdot)$ 为差异函数。

**第四步：关键增强——用 EMFS 而不是普通 MFS**

原文明确指出多保真的隐患：

> 当 HF 与 LF 的函数趋势相关性不好时，只用 HF 建的单保真代理（SFS）可能比 MFS 更准；随着 HF 样本增多，SFS 也可能反超 MFS。**这时就会发生负迁移。**［原文 §3.3］

这正是第 05 讲（**论文 13**）处理的问题。本文**直接采用了论文 13 提出的 EMFS**（集成加权多保真代理，GTO 的参考文献 [36] 即该文）：

$$\hat y_{EMFS}=w_G\,\hat y_{MFS}+w_L\,\hat y_{SFS},\qquad s^{2}_{EMFS}=w_G\,s^{2}_{MFS}+w_L\,s^{2}_{SFS}$$

$$w(\mathbf x)=\frac{s_L^{2}(\mathbf x)}{s_L^{2}(\mathbf x)+s_G^{2}(\mathbf x)},\qquad
w_L=\mathrm{sigmoid}\big(m\cdot w-b\big)=\frac{1}{1+e^{-(m\cdot w-b)}},\qquad w_G=1-w_L$$

其中：
- $s_G^2$：用**全部 HF 样本**建的单保真 GP 的预测不确定性；
- $s_L^2$：用 **DBSCAN 检出的 HF 样本密集区**内的局部样本建的 GP 的预测不确定性。

直观理解：MFS 能抓住全局趋势，局部 SFS 在 HF 样本密集区更准，**EMFS 按不确定性自适应地加权融合**，两者兼得。

采集函数相应改为多保真 EI：

$$EI_M(z)=\big(f^{(t)}_{min}-\hat y_M(z)\big)\Phi(u_M)+\sigma_M(z)\varphi(u_M),\qquad u_M=\frac{f^{(t)}_{min}-\hat y_M(z)}{\sigma_M(z)}$$

#### 4. 算法控制流（GTO / GAN-TO）

```
① 收集数据集 X = 无标签叶型 XU（1030 个）+ 源样本 {xS, yS}m（40 个，坐标点表示）
② 用样本加权策略训练 GAN（每 batch 中源/无标签各半）→ 共享潜设计空间 𝒵
③ 用梯度重参数化把源样本 xS 编码进潜空间 → {zS, yS}m
④ 目标侧：LHS 生成 40 个初始样本，做 CFD 评估 → HF 样本
⑤ 以源样本为低保真、目标样本为高保真，建立 EMFS（或 MFS）
⑥ 用多保真 EI 选点 → CFD 评估 → 更新样本集
⑦ 重复 ⑤⑥ 直到预算耗尽（< 140 次评估）
```

#### 5. 实验验证与量化突破

**算例**：GE-E3 低压涡轮叶片高负荷改型（栅距 25.006 → 30.0072 mm）。

| 设置项 | 数值 |
| :--- | :--- |
| 边界条件 | 进口气流角 49.3°；进口总压 319,339.2 Pa；进口总温 1161 K；出口静压 245,618 Pa |
| 叶型 | 轴向弦长 26.6 mm；栅距 25.006 → **30.0072 mm**；出口气流角 −64.19° → −60.86° |
| 参考设计损失 | **12.99%** |
| 求解器 | RANS + **SST $\gamma$-$\theta$ 转捩模型**；对称边界 + 转捩周期；网格 **492,700** 单元 |
| 约束 | 流量在基准 $[0.98,1.02]$ 倍内；尾缘最小半径 ≥ 0.2 mm（保证强度） |
| 预算 | 初始样本 40（LHS），每次运行总评估 **< 140**；各方法 **10 次**运行 |

**主要结果**［原文 Table 3］

| 设计 | 总压损失 $\varepsilon_P$ (%) | 流量 $\dot m$ | 出口气流角 (°) |
| :--- | :---: | :---: | :---: |
| 参考（高负荷） | 12.99 | 475.02 | −60.86 |
| FFD-EGO | 4.72 | 471.50 | −61.30 |
| GAN-EGO | 4.56 | 474.08 | −61.05 |
| GAN-TO-MFS | 4.35 | 473.64 | −61.10 |
| **GAN-TO-EMFS** | **4.29** | 467.70 | −61.69 |

**原文明确给出的三条结论：**

1. **为达到与 GAN-EGO 相近的优化结果，GAN-TO 所需的函数调用可减少 50% 甚至更多**［摘要与 §4.4］；
2. **GAN-TO 的结果鲁棒性明显更好**——其最优解的变动范围远小于 GAN-EGO；
3. **GAN-TO-EMFS 比 GAN-TO-MFS 收敛更快**。原因是 MFS 抓住全局趋势、局部 SFS 抓住 HF 密集区的局部趋势，EMFS 的自适应融合兼得二者。

**气动机理**：优化后尾缘附近的分离泡几乎消失，吸力面尾缘的"压力平台区"也一并消失。相比 FFD-EGO，GAN-EGO 的叶片负荷更靠前、尾缘逆压梯度更平缓；而 GAN-TO-MFS 与 GAN-TO-EMFS 的设计还出现了**二次加速**，显著削弱了吸力面沿程（尤其是逆压段）的压力梯度，因而总压损失更低［原文 §4.4］。

**潜空间维度的敏感性分析（§4.5，很有价值）**

- 维度增大 → GAN 更能捕捉叶片的精细局部几何 → **重参数化精度提高**、设计空间中的真实最优更好；
- 但维度增大 → 建准代理需要**更多训练样本**；
- 在相同初始样本数与预算下：**$d=3$ 收敛最快但最终解较差，$d=30$ 结果最差，$d=5$ 是最佳折中**。

**跨论文对比（与第 07 讲是同一个基准问题）**

两篇论文的 Table 1 / Table 2 数值**完全一致**（进口 49.3°、319,339.2 Pa、1161 K、245,618 Pa、26.6 mm、25.006→30.0072 mm、参考损失 12.99%）［对照原文 P08 Table 2 与 P17 Table 1］，因此可以在同一基准上排一条演进线：

| 方法 | 论文 | 总压损失 (%) |
| :--- | :--- | :---: |
| FFD-BO / FFD-EGO | 08 / 17 | 4.96 / 4.72 |
| GAN-BO / GAN-EGO | 08 / 17 | 4.86 / 4.56 |
| VAE-W-BO（样本加权，无迁移） | 08 | 4.75 |
| KT-ASO（VAE + 多保真迁移） | 08 | 4.52 |
| **GAN-TO-EMFS（GAN + 梯度重参数化 + EMFS）** | **17** | **4.29** |

> ⚠️ **对比须谨慎**：论文 08 通篇用 **BO** 作优化器、论文 17 通篇用 **EGO**，且源样本构成略有差异，**上表只能作为趋势参考，不是严格同条件对照**。

#### 6. 审稿人批判与承前启后

**可以攻击的点**（其中涉及的设置与结论均出自原文 P17 §4–§5）：

1. **源任务与目标任务仍是"人为构造"的同构对**（同一叶型、靠增大栅距制造高负荷），与论文 08 的构造完全相同。**"源与目标差多远时迁移失效"这个最关键的问题，两篇都没有量化。**
2. **最优潜空间维度依赖预算**。原文自己在结论里承认"如何高效找到生成模型的最优潜空间维度"仍需更多研究——$d=5$ 只是本文预算下的结论。
3. **梯度重参数化是逐样本的局部优化**。$\arg\min\|G(z)-x\|^2$ 对非凸的 $G$ 没有全局最优保证，且每个源样本都要单独跑一次梯度下降。
4. **GAN 的训练成本未被计入"节省的 50%"**：4000 epochs、学习率调度、MMD 监控、模式崩溃风险——这些是实打实的工程开销。
5. **仍是二维叶型优化**，不涉及三维叶片与多学科约束。

   ［以上第 1–5 点均基于原文 P17 报告的方法设置与结果所作的合理质疑；其中"减少 50% 甚至更多"出自 P17 摘要与 §4.4，$d=5$ 的敏感性结论出自 §4.5 与 §5 结论(3)］

**承前（本文是同组工作的集大成者）：**

| 继承自 | 继承的内容 |
| :--- | :--- |
| **论文 08（KT-ASO / SW-VAE）** | 样本加权训练策略、多保真迁移的整体框架（把 VAE 换成 GAN） |
| **论文 13（EMFS / MSFO）** | 多保真代理直接采用该文提出的 EMFS（含 DBSCAN 局部 SFS），见 GTO 参考文献 [36] |
| **论文 06（GMFoO）** | GAN 用于叶型参数化、潜空间优化的路线 |

**本文的独特贡献**：用**梯度重参数化**补上了 GAN 无法逆推断这一环，从而让生成质量更强的 GAN 也能用于迁移优化。

**顺带成果**：本文参考文献 [17] 提供了**论文 14（GAN-Endwall）**的准确著录——C. Li, L. Song, Z. Guo, Z. Yang, J. Li, Z. Feng，ASME Turbo Expo 2024（London），论文号 **V12DT34A025**。这解决了第 09 讲那篇文献"查无此号"的问题。

---

### 【第09讲】GAN-Endwall：把生成式参数化从叶型推广到非轴对称端壁（**论文 14**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **14** |
> | 原始文献 | **ASME Turbo Expo 2024（London, UK）**，论文号 **V12DT34A025** |
> | 作者 | Cunxi Li, Liming Song, Zhendong Guo, Zhao Yang, Jun Li, Zhenping Feng |
> | DOI | **未检索到**（ASME 页面需订阅） |
> | 本地原文 | **无** |
> | 证据等级 | **B（由论文 17 的参考文献 [17] 提供完整著录；正文方法细节无法获取）** |

> ⚠️ **本讲的写作边界（务必先读）**
> 本文**没有本地全文**，公开渠道只能确认**标题、会议、论文号、作者**，**无法获取摘要与正文**。
> 因此本讲**只做"方法定位 + 与相邻工作的关系"**，**不写任何量化指标、网络结构、算法流程**——因为那些都查无实据。
>
> 🗑️ **旧版白皮书的错误**：旧版把本文称作 "**GAN**-Endwall"，并写入"**消除九成以上的无效几何生成**"。
> 核查结果：① 公开著录中**只出现 "Generative Model Based Parameterization"，没有 GAN 字样**——旧版很可能是把第 08 讲的 GTO（确为 GAN）混淆过来了；② 该数字**无任何来源**，已在重建版中删除。

---

#### 1. 生活与物理直觉引子：为什么端壁比叶型更难"捏形状"

前面几讲一直在处理**叶型**（叶片的横截面）。现在换一个更麻烦的对象：**端壁（endwall）**——也就是叶片根部所依附的那个环形壁面。

传统端壁是**轴对称**的：绕发动机轴线旋转一圈，壁面形状完全一样，像一张被旋转扫掠出来的圆盘面。它便宜、好加工，但气动上并不好——因为流道里的压力分布是**周向不均匀**的：叶片压力面一侧压力高、吸力面一侧压力低，气流会本能地从高压侧"横着"往低压侧跑，形成**横向二次流**，卷出一堆旋涡（马蹄涡、通道涡、角涡），造成显著损失。

**非轴对称端壁（Non-Axisymmetric Endwall, NAE）**的做法是：把端壁做成**周向起伏的三维曲面**——在压力高的一侧"鼓起来"一点，在压力低的一侧"凹下去"一点，用几何本身去抵消横向压力梯度。

听起来很美，但工程上有两个硬骨头：

1. **参数化维度爆炸**：一个光滑的三维曲面需要很多控制点（B 样条/NURBS 网格），维度一高，优化就难；
2. **容易生成"畸形几何"**：高阶曲面参数化经常在局部产生凹凸畸变，生成的形状**根本加工不出来**，白白浪费一次次 CFD。

这正是第 06、07、08 讲那套**生成式参数化**要解决的同类问题——只不过对象从二维叶型换成了三维端壁。

#### 2. 叶轮机械中的真实工程死穴：设计空间里绝大部分形状是"废品"

这一节必须在证据允许的范围内讲，所以我们把它讲成"**问题是什么**"，而不声称"**本文怎么解决、效果如何**"。

非轴对称端壁优化的经典困难可以概括为三句话：

1. **维度高**：三维曲面若用 $6\times5$ 甚至更密的 B 样条控制网格描述，设计变量轻易几十上百个；
2. **流形窄**：在如此高维的空间里，"看起来像真实、可加工端壁"的形状只占极小一部分，绝大部分随机采样都是废品；
3. **评估贵**：每个候选形状都要跑一次三维 RANS 才能知道好坏。

于是问题变成：**能不能先学出一个"只生成合法端壁"的低维潜空间，再在这个潜空间里优化？**

这就是**生成模型参数化（Generative Model Based Parameterization）**的思路，也正是本文标题所指向的方法定位。

#### 3. 数学公式：本讲**不推导**。原因与替代方案

> 🚫 **本文无全文，任何公式、网络结构、损失函数、训练策略我们都无从得知，因此绝不做推测性推导。**
> 这是重建版白皮书与旧版最本质的区别之一——**宁可留白，不可编造**。

如果你想理解这类方法的数学骨架，**请直接阅读相邻两讲**，它们有完整原文支撑：

| 想理解的内容 | 请读 |
| :--- | :--- |
| 潜空间优化的基本框架、GAN/VAE 如何学出设计流形 | **第 06 讲（论文 06 GMFoO）** |
| 样本加权训练（防止高分样本被淹没）、多保真迁移 | **第 07 讲（论文 08 SW-VAE / KT-ASO）** |
| 梯度重参数化（把坐标点形状映射回潜空间） | **第 08 讲（论文 17 GTO）** |
| 非轴对称端壁的气动原理与流场机理 | **第 16 讲（论文 03 NAE-Purge）**、**第 17 讲（论文 09 NAE-Exp）** |

一旦取得本文全文，本讲应立即补齐方法细节并**把证据等级从 B 升到 A**。

#### 4. 算法控制流

> 🚫 同样无法给出。理由同上：**无全文，不做推测。**

#### 5. 实验验证与量化突破

> 🚫 **本文无任何可核实的量化结果。**

- 公开渠道可确认的只有：**该文发表于 ASME Turbo Expo 2024（London），论文号 V12DT34A025，作者为 Cunxi Li, Liming Song, Zhendong Guo, Zhao Yang, Jun Li, Zhenping Feng**。
- **摘要、图表、数值结果均未获取。**
- 因此本讲**不列出任何百分比、降幅、CFD 次数**。

> 🗑️ 再次明确：旧版白皮书 6.1 对比矩阵中"GAN-Endwall（2024）……**消除九成畸变几何，设计空间极度紧凑**"这一行属于**无源填充**，已从重建版删除。

#### 6. 审稿人批判与承前启后

**可以攻击的点（这些是"方法层面"的合理质疑，不依赖全文）：**

1. **"生成模型参数化"的收益缺乏公开量化**。与第 07、08 讲（有 Table 4/Table 5/Table 3 这样的硬数据）相比，本文目前**无法公开验证其收益幅度**。
2. **会议论文篇幅有限**。ASME Turbo Expo 会议论文通常较短，方法细节与消融实验往往不完整——这本身也是"证据等级 B"的成因。
3. **三维端壁比二维叶型难得多**：叶型可以用 202 个散点描述，端壁是真正的三维曲面，其参数化、网格变形、光顺性约束都更复杂；生成模型能否稳定胜任，需要全文数据支撑。
4. **与相邻工作的差异化不明**：本组已有论文 06（GMFoO）、论文 08（KT-ASO）、论文 17（GTO）在叶型上做生成式参数化，本文在端壁上的**独特贡献点**在缺少全文的情况下无法说清。

**承前：**
- 直接继承自**论文 06（GMFoO）**确立的"生成模型 + 潜空间优化"范式，以及**论文 08 / 论文 17** 的样本加权与迁移思路；
- 应用对象（非轴对称端壁）则承接**论文 03（NAE-Purge）**与**论文 09（NAE-Exp）**所研究的端壁气动问题。

**启后：**
- 与**论文 10（FFD-Tip）**共同构成"复杂三维几何的高效参数化"这一工程子方向：一个用生成模型，一个用自由变形（FFD）。

---

> 📎 **升级路径**：若能提供 ASME Turbo Expo 2024 的全文 PDF 或录用通知，本讲可立即升级为 A 级证据，并补全：网络结构、训练数据构成、潜空间维度、参数化对比（vs B 样条）、气动收益数值。
> 届时请在 `corpus/facts/P14.md` 中登记，并同步更新本讲。

---

# 第三篇 【物理增强神经算子与 AI4Turbomachinery 篇】

本篇覆盖**论文 16、12、18、19、15、20**。范式转变是：**不再预测一个标量性能，而是直接预测整个流场。*把叠加原理、流动相似性、守恒律写进网络结构（SDNO、ResUNet-Sim、TNO），再用 SHAP 把黑箱里的决策逻辑翻译给工程师看（SHAP-Turbine）。

---

### 【第10讲】TNO：不优化，而是让"一次评估"变得几乎免费（**论文 16**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **16** |
> | 原始文献 | *Chinese Journal of Aeronautics*, **2025, 38(7): 103473** |
> | DOI | `10.1016/j.cja.2025.103473`（开放获取 CC BY） |
> | 作者 | Qineng Wang, Zhendong Guo, Liming Song\*, Tianyuan Liu\* |
> | 本地原文 | `1-s2.0-S1000936125000792-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

---

#### 1. 生活与物理直觉引子：与其省着打车的钱，不如把打车变免费

前面九讲，我们一直在琢磨同一件事：**CFD 太贵，怎么把"打车的次数"省下来？**

- 第 01 讲：让每一次采样都更有价值（CR-EI）；
- 第 02 讲：便宜的粗网格先探路（Filter-GEI）；
- 第 03、04 讲：换个能扛高维的搜索策略（GSDE / DA-EGO）；
- 第 07、08 讲：把上一个型号的经验搬过来（KT-ASO / GTO）。

这些都属于**"省着花"**的路线。

本文换了一个完全不同的思路：

> **能不能干脆让"一次评估"本身变得几乎不要钱？**

这就是**代理模型（surrogate model）**最朴素的想法——但本文的做法远比"训一个效率预测网络"更进一步，它叫**全景式（panoramic）预测**。

#### 2. 叶轮机械中的真实工程死穴：一个代理模型只能服务一个目标

先说常规做法的问题。假设你在设计叶片，关心三个指标：效率、压比、失速裕度。

常规代理建模会怎么做？**每个指标各训一个模型。** 代价是：

- 每增加一个关心的指标，就要多训一个模型、多花一份数据；
- 更麻烦的是，**中途临时想看一个新指标**（比如"我想看看 70% 叶高处的熵增分布"），对不起——**模型没训过，只能回去补 CFD**。

设计师的真实工作方式是**边看边想**的：看一眼流场，觉得尾缘分离可疑，就想看尾缘的熵分布；发现激波位置不对，又想看等熵马赫数分布。**"临时起意"是设计过程的常态。**

本文的洞察是：既然这些量**本质上都能从基本流场算出来**，那为什么不——

> **先预测流场的基本物理量，再按公式算出任何你想看的性能指标？**

原文的表述是：

> 与以往大多数直接预测所关心目标函数的模型不同，该方法**首先预测 Navier–Stokes 方程的基本参数**（温度、压力、密度等），再利用这些基本物理量**预测涡轮级子午面上的关键性能参数**。如此，该框架可**像 CFD 求解器一样工作**。［原文 P16 摘要］

#### 3. 数学公式从 0 到 1 推导

**第一步：把"全景框架"写成两段映射**

$$\mathbf x\ \xrightarrow{\ \text{基本场预测器}\ f_s\ }\ f_s\ \xrightarrow{\ \text{全景预测器}\ }\ W$$

- $\mathbf x$：设计变量；
- $f_s$：基本物理场；
- $W$：性能量。

**第二步：选哪些"基本物理量"？**

原文选了 5 个［§3.3］：**静压 $p$、静温 $t$、绝对速度 $v$、相对速度 $w$、质量流 $q_{vm}$**。

为了计算方便，速度取平方，于是网络要预测的是：

$$(p,\ t,\ v^{2},\ w^{2},\ q_{vm})$$

子午面被离散成 $M=4096$ 个点。

**第三步：TNO 的网络结构（branch–trunk 框架）**

$$F(x)(r,z)=S\Big(B_{k}(x;h_b)\ \odot\ T_{k}(r,z;h_t);\ h_s\Big)$$

- **分支网络 $B$（branch）**：把设计变量 $\mathbf x$ 编码成特征，用堆叠的 MLP；
- **主干网络 $T$（trunk）**：把查询位置 $(r,z)$ 编码成特征，由 1 个 embedding block + 若干 **Galerkin 注意力块**串联而成：

$$T(r,z)=P\circ G_{t_m}\circ\cdots\circ G_{t_1}\circ E(r,z)$$

- 每个 Galerkin 注意力块 = 注意力层 + 前馈网络（FFN），**均带残差连接**；
- 注意力输出：

$$Z=\mathrm{Attn}(P^{t})=\frac{QK^{T}V}{m},\qquad Q=P^{t}W^{Q}$$

**为什么用 Galerkin 注意力而不是原生 softmax 注意力？**

原文说明：采用基于 Galerkin 的**简化自注意力**替代常规注意力，**省掉了 softmax 的计算，从而提升计算速度**［§3.1］。

**为什么自注意力在这个任务上特别有效？** 原文给了一个非常有物理感的解释：

> 叶型的**局部几何改动只影响特定的流动区域**——比如尾缘的改动会影响下游的分离涡与叶尖流动。自注意力模型能捕捉这种**复杂、非均匀的空间依赖**，把几何变化映射到对应的流动区域，从而提高整场预测精度。［§3.1］

最后，TNO 接一个**共享输出网络 $S$**，用于**同时处理多个物理场**——这正是"全景"二字的技术实现。

**第四步：从基本场算出性能量（全景预测器）**

原文 Table 6 给出 9 个常用性能指标的**显式计算公式**，全部只依赖那 5 个基本场，例如：

| 性能量 | 计算方式 |
| :--- | :--- |
| 绝对总温 | $t_v=t+\dfrac{v^2}{2c_p}$ |
| 相对总压 | $p_w=p\left(1+\dfrac{w^2}{2c_p t}\right)^{\gamma/(\gamma-1)}$ |
| 等熵效率 | $\eta_{is}=\dfrac{(p_{v2}/p_{v1})^{(\gamma-1)/\gamma}-1}{t_{v2}/t_{v1}-1}$ 形式的总参数之比 |
| 质量流 | $\dot m=\dfrac{\int_{r_h}^{r_s}\rho v_m\,z\,r\,dr}{\int_{r_h}^{r_s} z\,r\,dr}$（周向平均后再展向积分） |

整体量的计算被分解为**周向积分 + 展向积分**两步，可分别在流场预测之前或之后执行。

**训练目标**：MSE 损失，Adam 优化器，900 epochs。

#### 4. 算法控制流

```
离线（一次性投入）：
  ① 生成 2500 个叶片几何样本 + 对应 CFD 解
  ② 把每个子午面离散为 4096 个点，提取 5 个基本场
  ③ 训练 TNO：分支网络吃设计变量，主干网络吃查询坐标，输出 5 个基本场

在线（几乎免费，可反复执行）：
  ④ 给定新设计 x → TNO 预测子午面基本场
  ⑤ 按 Table 6 的显式公式算出任意性能指标
  ⑥ 接下游任务：敏感性分析 / GA 单目标优化 / NSGA-II 多目标优化
  ⑦ 关键结论仍送回 CFD 复核
```

#### 5. 实验验证与量化突破

**算例**：Rotor 37 跨声速压气机叶片，**28 个设计变量**。数据集 **2500 训练 / 400 验证**［§3.3］。

**① 基本物理场预测精度（FRE，%）**［Table 4］

| 网络 | 平均误差 |
| :--- | :---: |
| MLP | 0.820 |
| UNet | 0.792 |
| DeepONet | 1.888 |
| FNO | 0.572 |
| **TNO** | **0.083** |

> 原文结论：**TNO 相对 FNO 的平均误差降低 85.5%**［§4.1.1］。验算：$(0.572-0.083)/0.572=85.5\%$ ✅

**② 网络规模与速度**［Table 3］

| 网络 | 前向显存 (MB) | 训练时间 (min) | 预测时间 (ms) |
| :--- | :---: | :---: | :---: |
| MLP | 768.0 | 19.98 | 2.198 |
| UNet | 624.5 | 114.0 | 5.075 |
| DeepONet | 822.5 | 52.9 | 0.970 |
| FNO | 558.9 | 106.5 | 1.448 |
| **TNO** | **147.13** | **40.5** | **0.915** |

> ⚠️ **原文此处自相矛盾，必须如实指出**：§4.1.1 正文写的是"TNO 的预测时间约为 **9 ms**，比 CFD 仿真低 5 个数量级"，而 Table 3 给出的是 **0.915 ms**，相差 10 倍。可能是排版把 "0.9 ms" 印成了 "9 ms"，也可能正文另有所指。**本讲以 Table 3 为准，并标注该处待核实。**

**③ 与传统代理模型的对比（等熵效率 PRE，‰）**［Table 8］

| 训练样本量 | 500 | 1000 | 1500 | 2000 | 2500 |
| :--- | :---: | :---: | :---: | :---: | :---: |
| FNO | 1.213 | 1.188 | 1.057 | 0.938 | 0.810 |
| GPR（传统代理最佳） | 1.425 | 1.273 | 1.156 | 1.098 | 1.075 |
| ANN | 3.697 | 3.775 | 3.799 | 3.699 | 3.706 |
| **TNO** | **0.823** | **0.460** | **0.448** | **0.439** | **0.486** |

原文结论：**15 个场景中 TNO 有 13 个最优；在训练样本超过 1500 的所有场景中，TNO 全部最优且优势显著**［§4.1.3］。

**④ 单目标优化（GA，种群 20、代数 100；所有最优解都送回 CFD 复核）**［Table 9］

| 性能量 | 方向 | 参考值 | 模型预测 | CFD 复核 | 预测相对误差 | 相对改善 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **等熵效率** | max | 0.854 | 0.871 | **0.871** | 0.001% | **+1.95%** |
| 总压损失 | min | 0.124 | 0.115 | 0.103 | 11.87% | +17.24% |
| 静熵 | min | 44.83 | 41.44 | 38.94 | 6.42% | +13.13% |
| 总压比 | max | 2.046 | 2.073 | 2.071 | 0.11% | +1.21% |
| 温比 | max | 1.265 | 1.270 | 1.271 | 0.03% | +0.44% |

- 效率的实际改善为 **1.70%**，与作者先前在同一优化任务上的工作一致［§4.3］。
- **这张表最值得细看的一行是"总压损失"和"静熵"**：它们的预测相对误差高达 **11.87%** 与 **6.42%**，远高于效率的 0.001%。原因见下一节的批判。

**⑤ 多目标优化（NSGA-II，种群 30、代数 100）**［Table 10，节选］

| 指标 | 参考设计 | Opt-A | Opt-B | Opt-C |
| :--- | :---: | :---: | :---: | :---: |
| 等熵效率 | 0.854 | **0.871** | 0.869 | 0.863 |
| 多变效率 | 0.872 | 0.883 | 0.882 | 0.876 |
| 总压损失 | 0.124 | 0.116 | 0.116 | 0.119 |
| 总压比 | 2.046 | 2.069 | 2.045 | 2.040 |
| 质量流 | 20.266 | 20.615 | 20.439 | 20.262 |
| 熵 | 44.832 | 41.196 | 41.273 | 43.462 |

**⑥ 最有冲击力的一句话**［§4.3］

> 全景式预测框架把一个**原本需要 1000 组 CFD 计算**的优化任务，压缩到 **121 秒**内完成，**优化效率提升四个数量级**。

**⑦ 敏感性分析的额外发现**［§4.2］

28 个设计变量按叶高分组做方差敏感性分析（蒙特卡洛法）。结果显示：**span 25% 组与 span 50% 组对整体效率的影响幅度相近**，但借助全景流场预测进一步分析会发现——**两组对流场的作用方式完全不同**：span 25% 组主要影响叶高约 5%–50% 区域与 90% 附近区域，前者是同叶高位置的几何直接影响，后者是局部型线变化通过分离涡**传播到全场**的表现。

> 这正是"全景"框架的独有优势：**传统代理只能告诉你"哪个变量重要"，而流场预测能告诉你"它通过什么流动机制起作用"。**

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **相对误差指标在"小量纲"指标上失真**。总压损失的预测相对误差 **11.87%**、静熵 **6.42%**——也就是说，**越是接近零的损失类指标，模型越不准**。原文用同一个相对误差指标衡量不同量纲的量，这是方法论上的瑕疵。
2. **最优解落在训练集分布之外**。原文自己承认：识别出的最优解性能**显著超出训练样本对应指标的分布范围**。这意味着模型在**外推**，而外推的可靠性没有任何保证。
3. **只有一个算例**（Rotor 37，一种叶片、一个子午面），泛化性未验证。
4. **预测时间自相矛盾**（正文 9 ms vs Table 3 的 0.915 ms），见上文。
5. **训练成本没有被算进"四个数量级"**。2500 个 CFD 样本是实打实的投入；本文没有与"用同样 2500 个样本做常规代理 + 优化"做**端到端的总账对比**。
6. **优化器很朴素**：GA / NSGA-II，没有用前面各讲的贝叶斯或代理辅助方法。也就是说，**TNO 的收益来自"评估便宜"，而不是"搜索聪明"**——这两条路线其实是**可以叠加**的，本文没有做这个实验。

**承前：**
- 方法论上继承神经算子（DeepONet、FNO）与 Transformer/Galerkin 注意力。
- 与第 01–09 讲的关系是**互补而非替代**：前面九讲解决"**如何用尽量少的 CFD 找到最优点**"，本文解决"**如何让一次评估本身变得几乎免费**"。**两条路线可以叠加**——用 TNO 做"廉价评估"，再用 CR-EI/DA-EGO 做"聪明搜索"，这是很自然的下一步。

**启后：**
- **论文 12（SDNO）**：同为流场预测的深度算子方向；
- **论文 18（ResUNet-Sim）**：同为"用深度学习替代/加速仿真"的工程化落地；
- **论文 19（SHAP-Turbine）**：本文的敏感性分析只是**为设计服务的副产品**，而 SHAP 那篇把"变量重要性与可解释性"本身作为研究对象。

---

### 【第11讲】SDNO：让网络"照着物理公式"长——把叠加原理写进神经算子（**论文 12**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **12** |
> | 标题 | Enhancing the Generalization Ability of Endwall Film Cooling Prediction by Incorporating Superposition Principle into Transformer-based Neural Operator |
> | 作者 | Qineng Wang, Liming Song, Tianyuan Liu（百度 / 北京大学）, Zhendong Guo（通讯） |
> | 发表状态 | **预印本（SSRN，提交至 Elsevier，2024-06-14），未经同行评审** |
> | DOI | `10.2139/ssrn.4869789` ｜ https://ssrn.com/abstract=4869789 |
> | 本地原文 | `ssrn-4869789.pdf` |
> | 证据等级 | **A（正文全文本地可查）／著录为 B（预印本，未见正式出版版本）** |

> ⚠️ **著录纪律**：本地 PDF 每一页页脚都印着 "This preprint research paper has not been peer reviewed."
> 因此本讲**不编造任何期刊卷期页码**。若该文日后正式发表，应回到此处更新著录。

---

#### 1. 生活与物理直觉引子：为什么"背过题"的 AI 一遇到新题型就崩

先说一个所有深度学习用户都踩过的坑。

你给一个网络看成千上万张"1 个喷头"和"2 个喷头"的喷水降温照片，让它学会预测地面温度分布。现在你问它：**10 个喷头同时开会怎样？**

网络大概率答不好。不是因为它笨，而是因为**它从来没见过 10 个喷头的画面**——在它的经验里，"画面里有 10 个白点"这件事根本不存在。这就是**外推失效（extrapolation failure）**。

工程上这个问题非常现实：

> 做涡轮端壁气膜冷却设计时，你不可能为"7 个孔""8 个孔""19 个孔"每一种孔数都准备一批 CFD 训练数据——**太贵了**。

那怎么办？人是怎么解决这类问题的？

人的做法是**用物理规律**。早在几十年前，气膜冷却领域就有一个非常朴素的经验规律——**叠加原理（superposition principle）**：

> 多个孔一起开的冷却效果，可以由"每个孔单独开的效果"**叠加**出来。

本文的核心想法，就是把这条**人尽皆知的物理规律，直接写进神经网络的结构里**。

#### 2. 叶轮机械中的真实工程死穴：端壁气膜冷却的 CFD 成本

先看这个工程问题有多贵。

**研究对象**：Pak-B 叶片端壁的气膜冷却。Pak-B 是气膜冷却领域的经典公开标杆几何。

**物理过程**：高温主流从一侧流过端壁；冷气从端壁下方的**集气腔（plenum）**进入，通过端壁上的**气膜孔**喷出，与主流掺混。冷气一方面形成覆盖端壁的**气膜**隔绝高温，另一方面通过对流换热带走热量。

**数值模型**［原文 §2.1］：

| 项目 | 设置 |
| :--- | :--- |
| 求解器 | **Star-CCM+**，棱柱网格 + 有限体积法，**共轭传热（CHT）**模型 |
| 网格规模 | **280 万 – 360 万**节点 |
| 单样本成本 | **4 核并行约 3 小时** |

**边界条件**［原文 Table 1］：主流出口雷诺数 $1.98\times10^{5}$；主流进/出口马赫数 0.029 / 0.047；主流进口总温 **323 K**；主流进口湍流度 **6%**；速度/动量边界层厚度 33 mm / 2.5 mm；冷气进口总温 **286 K**。

也就是说：**一个样本 3 小时**。要生成覆盖"1 到 20 个孔 × 各种位置组合"的训练集，成本是天文数字。

于是问题被逼成一句话：**能不能只在"1–5 个孔"的数据上训练，却让模型能预测"10–20 个孔"的布局？**

#### 3. 数学公式从 0 到 1 推导

**第一步：原始的 Sellers 叠加公式**

气膜冷却领域经典的叠加原理写成：

$$T^{(AB)}(\omega)=T^{(A)}(\omega)\oplus_{seller}T^{(B)}(\omega)
=\eta_C^{-1}\Big[\eta_C\big(T^{(A)}(\omega)\big)\cdot\eta_C\big(T^{(B)}(\omega)\big)\Big]$$

其中 $\eta_C$ 是气膜冷却效率，$T^{(A)}$、$T^{(B)}$ 分别是"只开 A 孔"与"只开 B 孔"时的温度场。

**第二步：本文的两处关键改造**

$$T^{(AB)}(\Omega_p)=T^{(A)}(\Omega_p)\ \hat\oplus_{super}(\theta_S)\ T^{(B)}(\Omega_p)$$

| 改造 | 原来 | 改成 | 为什么 |
| :--- | :--- | :--- | :--- |
| **① 算子可学习** | 固定的 $\oplus_{seller}$（Sellers 公式给死） | 参数化的 $\hat\oplus_{super}(\theta_S)$ | 让**数据**来修正物理公式的偏差 |
| **② 计算对象升级** | 单点 $\omega$ | **子区域 $\Omega_p\subset\Omega$** | 把**孔与孔之间的相互作用**纳入叠加 |

> 第 ② 点尤其重要：Sellers 公式是**逐点**成立的，但真实流场里上游孔会改变下游孔的主流环境，这种**非局部干扰**必须靠"子区域"才能表达。

**第三步：把叠加原理实例化成网络**

$$\hat F\circ X^{(AB)}(\Omega_p)=\hat S\Big(\hat C\circ X^{(A)},\ \hat C\circ X^{(B)}\Big)(\Omega_p)$$

- $\hat C(\cdot)$ —— **Calculate Net**（计算网络）：基于 **Transformer** 的神经算子，负责预测"**子布局**"的温度场；
- $\hat S(\cdot)$ —— **Superposition Net**（叠加网络）：用 **FNO** 实现，负责把多个子布局的场**叠加**成完整布局的场。

Superposition Net 的具体结构［Table 3］：

```
输入 N×128×128×2
  → Patch 16            → 16N×32×32×2
  → Linear-1            → 16N×32×32×64
  → (Conv2d + GELU + SpectralConv2d) × 2
  → Linear-2 / Linear-3
  → Patch-reverse       → N×128×128×1
总参数量：17,089（0.07 MB）
```

**第四步：一个极其优雅的性质——交换律与结合律**

校准后的叠加算子满足：

$$T^{(A)}\hat\oplus_{super}T^{(B)}=T^{(B)}\hat\oplus_{super}T^{(A)}$$

$$T^{(A)}\hat\oplus_{super}T^{(BC)}=T^{(AB)}\hat\oplus_{super}T^{(C)}$$

**这意味着什么？** 意味着 $\hat S$ 网络可以**被反复递归调用，而且不在乎输入顺序**。

于是：你在**1–5 个孔**的数据上训练好"两两叠加"的能力，推理时就能**像搭积木一样**把 20 个孔的场一层层叠出来——**这就是"从 5 外推到 20"的机制根源。**

> 💡 这是本讲最值得记住的一点：**不是靠更大的网络记住更多，而是靠"结构符合物理"让外推变得合法。**

**第五步：SDF 预处理——别把孔画成黑白像素**

如果直接把气膜布局表示成二值像素（有孔=1、无孔=0），会有**像素化误差**：孔的位置稍微挪一点，像素矩阵可能完全不变，网络就"看不见"这个变化。

本文改用**符号距离函数（Signed Distance Function, SDF）**编码布局——SDF 记录"每个点到最近孔的距离（带符号）"，**对孔的位置变化连续敏感**。原文明确指出 SDF"**有效降低了像素化误差**"［§6 结论］。

**第六步：双路径训练 + 推理时的分解策略**

- **训练**：同一个样本走两条前向路径——
 - **standard mode**（黑箭头）：只用 $\hat C$ 做全监督；
 - **superposition mode**（蓝箭头）："**分解 → 预测 → 叠加**"。
 关键在于：叠加训练的反向传播不仅优化 $\hat S$，**还同步调整 $\hat C$ 的参数**，从而大幅丰富了 $\hat C$ 见到的样本排列组合，**连 $\hat C$ 自身的精度都被带高了**。
- **推理**：训练时随机分解，推理时改用固定的**"均质化（homogenization）"策略**——把气膜孔**均匀**分配到各子布局。原文用 Fig. 10 对比了不同分解策略，结论是均质化能最小化 OOD 样本误差。

#### 4. 算法控制流

```
训练阶段：
  ① CHT 仿真生成样本：Pak-B 端壁，气膜孔数从 1 到 5，位置随机（单样本约 3 小时）
  ② 用 SDF 编码每个布局（替代二值像素）
  ③ 双路径训练：
       black：L = ||Ĉ(X) − Y||²                 （全监督，只训 Ĉ）
       blue ：分解 → Ĉ 预测子场 → Ŝ 叠加 → 与全场真值比（同时更新 Ŝ 与 Ĉ）
  ④ 微调（fine-tuning）

推理阶段：
  ⑤ 把待预测的 N 孔布局，按"均质化"策略分解为若干子布局（如 5 孔 × 4 组）
  ⑥ Ĉ 预测每个子布局的温度场
  ⑦ Ŝ 递归地把这些场两两叠加（顺序无关）
  ⑧ 得到完整布局的温度场
```

#### 5. 实验验证与量化突破

**核心结果**（均相对**全监督基线网络**）［原文 §6 结论、§1 贡献点(3)］

| 阶段 | 样本类型 | 误差降低 |
| :--- | :--- | :--- |
| 叠加训练后 | **ID（分布内）样本** | **约 20%** |
| 叠加训练后 | **OOD（分布外）样本** | **约 70%** |
| **进一步微调后** | **外推到 20 个气膜孔** | **超过 90%** |

- **训练数据**：气膜孔数量 **1 – 5**、位置不同的样本；
- **外推目标**：训练时**从未见过**的 **10 – 20 个气膜孔**布局。

> 原文在 §5.1 中还坦白了一件重要的事：**只做叠加训练时，模型对训练集分布之外样本的预测精度"仍不足以满足工程应用需求"**；真正达到可用，是在**进一步微调之后**。这一点必须诚实呈现——**"外推到 20 孔、误差降 90%"是微调后的结果，不是零样本外推的结果。**

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **仍是预印本**，未经同行评审，正式卷期与 DOI 未知。
2. **叠加原理本身是近似假设**。Sellers 公式假设各孔效果可分离叠加，而真实流场中上游孔会改变下游孔的主流环境，存在**非线性干扰**。本文用"子区域 + 可学习算子"缓解，但**没有根除**——叠加越多次，误差累积的风险越大。
3. **外推到 20 孔依赖微调**。原文自己承认叠加训练后的 OOD 精度仍不够工程可用，说明**纯零样本外推能力有限**。
4. **推理时的分解策略是试出来的**。"均质化"来自 Fig. 10 的对比实验，缺乏理论依据；换一个问题可能要重新试。
5. **只验证了温度场**，未涉及流动场、压力场，也没有接入下游优化闭环。
6. **数据成本依然很高**：280–360 万节点的 CHT 仿真，单样本 4 核 3 小时。

**承前：**
- 继承神经算子（FNO）、Transformer 算子，以及气膜冷却领域经典的**叠加原理（Sellers 公式）**。
- **与论文 16（TNO）是同一作者群在同一时期的两条算子路线**：

| | 论文 16 TNO | 论文 12 SDNO |
| :--- | :--- | :--- |
| 关键词 | **全景式** | **物理增强** |
| 思路 | 先预测基本流场，再按公式算出所有性能量 | 把已知的叠加原理**编码进网络结构** |
| 靠什么 | 靠**数据**与网络容量 | 靠**物理先验** |
| 共同点 | 都属于"让深度学习像 CFD 求解器一样工作"的路线 | |

> **二者互补**：TNO 解决"一个模型服务多个指标"，SDNO 解决"模型在分布外失效"。理想形态是把两者合起来。

**启后：**
- 把物理定律（而非仅靠数据）嵌入网络架构，是解决"深度学习外推失效"的根本路径之一；这一思路也延续到**论文 18（ResUNet-Sim）**。

---

### 【第12讲】P-ResUNet：把"相似原理"写进输入，把"载荷分布"写进输出（**论文 18**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **18** |
> | 原始文献 | *Aerospace Science and Technology*, **2026, 177: 112351** |
> | DOI | `10.1016/j.ast.2026.112351` |
> | 作者 | Hui Cheng, Liming Song, Yuqing Ouyang（中国航发湖南动力机械研究所）, Fei Zeng, Zhendong Guo（通讯） |
> | 本地原文 | `1-s2.0-S1270963826007315-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |

> ❗ **两处旧文档错误，在此更正：**
> 1. 旧文档给本文填的 DOI 是 `10.1016/j.ast.2026.112440`——该 DOI 实际指向 *Accelerating Tandem-Airfoil simulations using curriculum-based GCN for initialization*（作者 Wei Xian Lim 等），**与本团队毫无关系**。正确 DOI 为 **`10.1016/j.ast.2026.112351`**。
> 2. 旧版白皮书称本文"**马赫数预测精度 98%**"——原文并无此说法。正式指标是**流场（表面等熵马赫数分布）平均相对误差 MRE = 1.16%**，且它衡量的是**分布预测**，不是"精度 98%"这种含糊表述。

---

#### 1. 生活与物理直觉引子：为什么做模型之前要先把单位"除掉"

先想一个日常问题。你要训练一个 AI 来预测"不同尺寸的纸飞机能飞多远"。

如果你直接把"翼展 15 厘米""翼展 30 厘米""飞行距离 4 米""飞行距离 9 米"这些**带单位的原始数字**喂给网络，它会学得很吃力——因为它必须自己发现"尺寸放大一倍，飞行模式其实一样"这件事。

但如果你先做一个简单的除法：把长度都除以翼展，得到**无量纲**的比例（比如"展弦比"），网络立刻就能抓住本质：**决定飞行的是形状比例，不是绝对大小。**

流体力学里，这个"先做除法"的智慧有个正式名字——**相似原理（similarity principle）**。

- 两架几何相似、但尺寸差 10 倍的飞机，只要**雷诺数**和**马赫数**相同，它们的流场就是相似的；
- 换句话说：**流动本质上不在乎绝对尺度，只在乎那几个无量纲数。**

本文要做的第一件事，就是**把这条原理用在神经网络的输入端**：先做无量纲化，再让网络学习。

#### 2. 叶轮机械中的真实工程死穴：换个工况，模型就废了

上一讲（SDNO）我们看到的问题是"**换个孔数，模型就废了**"。涡轮叶片设计里有个同类的、甚至更常见的问题：

> **换个工况（马赫数、冲角、栅距），模型就废了。**

传统数据驱动模型在训练工况附近很准，一旦几何尺度或边界条件明显变化，精度就急剧下降［原文 §2.1.1］。

这对工程是致命的，因为**真实叶片必须在宽工况范围内工作**：从起飞到巡航，从设计点到近失速、近堵塞，冲角可能在 −30° 到 +15° 之间变化，出口马赫数能从 0.2 跨到 1.2。

本文的目标是：**建一个能覆盖这整个宽工况范围的预测模型，并把它接进正向/逆向设计闭环。**

#### 3. 数学公式从 0 到 1 推导

**第一步：几何侧的无量纲化**

以弦长 $C$ 为基准，把二维叶型坐标与栅距全部归一化，并提取关键无量纲特征：

$$\text{相对栅距}\quad \frac{t}{C}$$

它表征叶栅的**稠度（solidity）**。这样一来，模型就不再依赖叶片的实际物理尺寸，**设计空间的维度也被压缩了**。

**第二步：工况侧的无量纲化**

引入两个无量纲准则参数：

- **出口等熵马赫数 $Ma_{is1}$**：建立进口总压 $P^{*}_{0}$ 与出口静压 $P_1$ 之间的映射，表征流动的**可压缩性**；
- **进口冲角 $\alpha$**：表征来流的**方向性**。

**第三步：一个反直觉的决定——把雷诺数踢出去**

通常教科书会告诉你：流动相似需要雷诺数和马赫数都相等。但本文明确**排除了 $Re$**，原文给了两条理由：

> ① 典型涡轮叶片通常工作在雷诺数的**自模化区（$Re > 5\times10^{4}$）**，此时黏性效应对流场的影响相对次要；
> ② **精度验证表明，把 $Re$ 作为输入特征反而会降低预测性能。**［原文 §2.1.1］

> 💡 这是本讲最值得记住的工程判断：**不是"物理上有这个数"就一定要喂给网络。** 特征选择本身就是一种物理建模，加错了特征比少加特征更糟。

**第四步：知识嵌入——让网络先"学会载荷分布"**

传统端到端模型直接从几何映射到损失系数，浪费了数据里丰富的空间信息。本文改为两级：

$$\text{无量纲输入}\ \xrightarrow{\ \text{ResUNet}\ }\ \underbrace{\text{表面等熵马赫数分布}\ Ma}_{\text{中间物理约束}}\ \xrightarrow{\ \ }\ \text{总压损失系数}\ \delta$$

为什么这样做有效？原文的解释是：

> 这一约束**迫使网络在逼近性能数据时遵守流体力学的演化规律**；该关联预测机制既提升了跨工况泛化能力，也显著增强了对**激波、分离区**等复杂流动结构的捕捉精度。［原文 §2.1.2］

用人话说：**你逼着网络先把"压力沿叶型怎么分布"这件事说清楚，它就不太可能靠瞎蒙得到一个数字。**

**第五步：网络主体 ResUNet = U-Net + ResNet**

- **U-Net**：对称的编码器–解码器结构，因在医学图像分割中表现出色而闻名，其"精确定位"能力同样适合预测叶片表面流场；
 - **编码器**：逐层卷积 + 下采样，提取几何与气动特征（4–5 个卷积模块，各含卷积层、最大池化层、激活层）；
 - **解码器**：把低分辨率的深度特征逐步还原到原始尺度；
 - **跳跃连接（skip connection）**：U-Net 的标志性设计，直接把**高分辨率细节**从编码器传到解码器。
- **ResNet 残差模块**作为核心单元嵌入，构成 **ResUNet**。

#### 4. 算法控制流

```
离线建库（一次性投入）：
  ① 选取 5 种工业代表叶型：GE-E3 高压涡轮、PW-E3 高/低压涡轮 + 2 种典型动力涡轮叶片
  ② 参数化：轴向弦长 Cx、进口几何角 β1、进口楔角 ε1、切向弦角 γ、出口偏转角 θ、有效出口角 β2
  ③ LHS 生成 20,000 个叶型
  ④ Python 脚本后台驱动 ANSYS：模板化自动网格 → CFX 批处理求解 → 自动后处理提取
  ⑤ 划分：训练 16,000 / 验证 4,000（4:1）

模型训练：
  ⑥ 输入无量纲化：t/C ∈ [0.3,1.0]，Mais1 ∈ [0.2,1.2]，α ∈ [−30°,15°]（不含 Re）
  ⑦ 训练 ResUNet：输入 → 表面等熵马赫数分布 Ma → 总压损失系数 δ

在线设计闭环：
  ⑧ ANOVA 变量敏感性分析 → 圈定优化空间
  ⑨ 正向设计（给定几何求性能）/ 逆向设计（给定目标载荷反求几何）
  ⑩ 单工况 → 多工况（多点）验证
```

#### 5. 实验验证与量化突破

**数值模型**［§3.2］：ANSYS CFX，二维线性叶栅，$k$-$\omega$ **SST** 湍流模型，近壁网格保证 **$y^{+}=1$**，对流项与湍流项均用 **High Resolution** 格式，稳态收敛判据为所有控制方程 RMS 残差 **$<10^{-6}$**。网格无关性研究表明约 **90,000** 单元时总压损失系数即收敛。

**模型可信度验证**：在冲角 **−20°、0°、10°** 三个典型工况下与**实验数据**对比，预测的吸力面等熵马赫数分布与实验吻合良好，尤其在吸力面加速区。

**核心结果：物理增强 vs 网络结构改进，谁的贡献大？**［Table 1］

| 模型 | 验证集 MRE of $Ma$ (%) | 验证集 MAE of $\delta$ (%) |
| :--- | :---: | :---: |
| MLP | – | 2.84 |
| P-MLP | 2.16 | 0.35 |
| UNet | – | 1.87 |
| P-UNet | 1.73 | 0.31 |
| ResUNet | – | 1.52 |
| **P-ResUNet** | **1.16** | **0.26** |

> **这张表最有价值的读法**：沿着"不加物理"的一列往下看，MLP → UNet → ResUNet 只从 2.84 降到 1.52（渐进改善）；而一旦加上物理增强，ResUNet → **P-ResUNet** 直接从 **1.52 跳到 0.26**（约 **5.8 倍**改善）。
>
> **结论：物理先验的收益，远大于堆网络结构的收益。**

**正向设计：单工况**［Table 4］

| | P-ResUNet | CFD 复核 | 绝对误差 |
| :--- | :---: | :---: | :---: |
| 基准设计 | 3.45% | 3.47% | 0.02 |
| 优化设计 | 3.14% | 3.18% | −0.04 |
| **相对改善** | **8.99%** | **8.36%** | — |

**正向设计：多工况（冲角 10° / 20° / 40°）**［Table 6］

| | 10° | 20° | 40° |
| :--- | :---: | :---: | :---: |
| 基准设计（模型 / CFD） | 3.74 / 3.82 | 3.64 / 3.60 | 3.78 / 3.69 |
| 优化设计（模型 / CFD） | 3.48 / 3.60 | 3.60 / 3.50 | 3.43 / 3.48 |
| **改善幅度（模型 / CFD）** | **6.95% / 5.76%** | **1.99% / 2.78%** | **9.26% / 5.69%** |

> ⚠️ 注意 **20° 冲角**这一列：模型预测改善 1.99%，CFD 复核却是 2.78%——**模型在这里低估了改善幅度**。三个工况中，中等冲角区的预测精度最弱。这是原文没有特别强调、但读表能发现的事实。

**计算成本：这是本文最直观的工程价值**［Table 7］

| 方法（硬件） | 单工况 | 多工况 |
| :--- | :---: | :---: |
| CFD（服务器：**128 核 AMD EPYC 7T83**） | 约 **6 小时** | 约 **18 小时** |
| **P-ResUNet**（台式机：**12 核 Intel i5-10400F**） | 约 **2 分钟** | 约 **5 分钟** |

> 也就是说：**一台普通台式机跑 2 分钟，顶得上 128 核服务器跑 6 小时。** 即便按最保守口径（6 h vs 5 min）计算，加速比也在 **两个数量级** 上下。
> 📌 说明：Table 7 在 PDF 中的列头抽取为四个 "Forward design"（应为"正向/逆向 × 单/多工况"），P-ResUNet 行给出 2/5/5/10 分钟四个值。本表按"正向设计单工况 ≈2 min、多工况 ≈5 min"引用，其余两列（推测为逆向设计）**不做断言**。

**变量敏感性分析（ANOVA）——一段很有工程味道的推理**［§5.1］

1. 在包含**全部变量**的分析中，**尾缘半径 $R_{le}$ 是最显著的全局变量**——**尽管它的采样变化范围很小**。主效应分析显示：尾缘半径越大，总压损失系数急剧上升。物理解释很直接：**尾缘厚度直接决定尾迹掺混损失的大小。**
2. 于是作者做了一个工程决策：**把尾缘半径固定为 0.3 mm**（考虑实际制造工艺的最小约束），不再作为优化变量。
3. 固定后再分析，发现**尾缘半径的极高敏感性此前"遮蔽"了其他变量**。此时**有效出口角 $\beta_2$ 与轴向弦长 $C_x$** 的影响权重最突出：
 - $\beta_2$ 直接决定叶栅出口流量，为满足严格流量匹配，其变化范围须约束在 **±0.5°** 内；
 - $C_x$ 通过改变相对栅距来显著影响叶道内的流动加速与载荷分布。
4. 其次敏感的是**吸力面第二段 Bézier 曲线的控制点**——说明吸力面中后段的曲率对边界层发展与流动分离至关重要。

> 💡 这段推理值得反复品味：**"先做敏感性分析 → 发现某个变量的敏感度高到遮蔽一切 → 用工艺约束把它钉死 → 再看其他变量"**，这才是真实工程设计的样子，而不是把 20 个变量一股脑丢给优化器。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **是二维线性叶栅**。未涉及三维叶片、端壁效应与二次流。
2. **排除了雷诺数**。虽然原文给出了自模化区的理由并做了精度验证，但这意味着**模型不宜外推到低雷诺数工况**（如高空巡航、小型动力装置）。这是一个明确的能力边界，使用时必须注意。
3. **"小量纲"指标的误差仍需警惕**：MAE 0.26% 是**绝对误差的均值**，而总压损失系数本身只有 3%–4% 量级，换算成相对量仍有约 6%–8% 的不确定性。
4. **20° 冲角下模型低估了改善幅度**（1.99% vs 2.78%），说明中等冲角区的预测精度弱于其他工况，原文未深入讨论。
5. **数据集同质**：20,000 个样本均由 5 种基准叶型经参数化扰动生成，本质是"同一族叶型"的插值，对**全新叶型族**的泛化能力未验证。
6. **优化器未做特别设计**：收益几乎全部来自"评估便宜"，而非"搜索聪明"——与第 10 讲 TNO 的批判相同，这两条路线本可叠加。

**承前：**
- 继承 U-Net / ResNet 图像式流场预测、相似原理无量纲化、叶型参数化与 ANOVA 敏感性分析。
- ANOVA 变量分析延续了**论文 11（DA-EGO）**的做法，但目的从"服务子空间分解"变成"圈定优化空间"。

**与 TNO、SDNO 共同构成"物理增强深度学习"三条路线**（这是本讲最值得总结的一点）：

| | 论文 16 TNO | 论文 12 SDNO | 论文 18 P-ResUNet |
| :--- | :--- | :--- | :--- |
| 物理先验注入位置 | **输出侧**：先预测基本场，再按公式算性能 | **结构侧**：把叠加原理写进网络 | **输入侧**（相似原理无量纲化）+ **输出侧**（载荷分布作中间约束） |
| 主要解决的问题 | 一个模型服务多个指标 | 分布外外推 | 跨工况泛化 |

**启后：** 把训练好的预测器接入**正向 + 逆向**双模式设计框架，是"AI 预测器 → 工程设计闭环"的完整示范，也是后续把"廉价评估"与"聪明搜索"结合的基础。

---

### 【第13讲】SHAP：优化跑完之后，那一堆数据里到底藏着什么？（**论文 19**）

> **本讲元数据**
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **19** |
> | 原始文献 | *Chinese Journal of Aeronautics*（**Journal Pre-proofs**，2026） |
> | DOI | `10.1016/j.cja.2026.104374` ｜ PII `S1000-9361(26)00312-2` |
> | 作者 | Zhao Yang, Yunjia Yao（太行实验室）, Liming Song, Zhendong Guo（通讯） |
> | 本地原文 | `1-s2.0-S1000936126003122-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |
> | ⚠️ 状态 | 预排版版本，**暂无卷期页码**；收稿/修回/录用日期在封面页与首页记载不一致（待正式版确认） |

---

#### 1. 生活与物理直觉引子：赢了比赛，却说不清为什么赢

想象一位厨师参加厨艺大赛，拿了冠军。记者问他："你这道菜为什么好吃？"

如果他说"因为我放了很多调料，炒了很久"——这不算回答。真正有价值的回答是：

> "关键是**糖要在下酱油之前放**，这样能裹住肉的水分；至于火候，前两分钟必须大火，之后转小火——**这两个步骤顺序反了就会柴**。"

前十二讲，我们一直在研究**"怎么更快找到冠军"**——CR-EI、Filter-GEI、GSDE、DA-EGO、KT-ASO、GTO、TNO……每一讲都在让搜索更高效。

但这一讲要问一个**被忽略的问题**：

> **找到最优解之后，我们能说清"它为什么好吗"？**

要知道，一次优化跑下来会产生几百上千个样本（几何 + CFD 结果）。这些数据的价值**绝不只是"里面有一个最优解"**——它们记录了整个设计空间的"地形"。可惜大多数时候，我们只取走最高点，然后把地图扔了。

本文要做的事：**把这张地图读出来，变成人能理解的设计准则。**

#### 2. 叶轮机械中的真实工程死穴：93 维，人脑完全无法理解

本文的设计空间有多大？**93 维**。

| 部件 | 变量构成 |
| :--- | :--- |
| **静子** | 3 个截面（hub / 中 / 尖）× 每截面 15 个控制参数 = **45**；外加 **3** 个三维积叠参数 |
| **转子** | 3 个截面 × 15 = **45**（**不调整积叠形状**——因为旋转时要保证结构强度） |
| **合计** | **93 维** |

命名规则：S1/S2/S3 = 静子 hub / 中 / 尖截面；R1/R2/R3 = 转子对应截面；D1/D2/D3 = 静子的**周向中偏移、周向尖偏移、轴向尖偏移**。

在 93 维空间里，人不可能像看二维图像那样"看出"规律。传统的敏感性分析能告诉你"第 37 号变量很重要"，但给不了三件更要紧的事：

1. 这个变量是**调大好**还是**调小好**？
2. 它影响的是**叶高的哪一段**？
3. 它背后的**物理机制**是什么？

SHAP 恰好能提供前两点，本文进一步把第三点也做出来了。

#### 3. 数学公式从 0 到 1 推导

**第一步：SHAP 的思想——公平的"分功劳"**

一场篮球赛赢了 20 分，五个球员各自贡献多少？

朴素算法会看"谁在场时球队得分高"，但这有偏——因为球员是**组合**出场的。合作博弈论给出的公平分配方案叫 **Shapley 值**，做法是：

> 枚举**所有可能的出场组合**，计算某球员加入每个组合时带来的**边际贡献**，再对所有组合做加权平均。

把"球员"换成"设计变量"、"比赛得分"换成"模型预测值"，就是 **SHAP（SHapley Additive exPlanations）**。

**第二步：SHAP 的严格定义**

$$\phi_i=\sum_{S\subseteq N\setminus\{i\}}\frac{|S|!\,\big(M-|S|-1\big)!}{M!}\Big[f_x\big(S\cup\{i\}\big)-f_x\big(S\big)\Big]$$

- $N=\{1,2,\dots,M\}$：全部特征的下标集，$M$ 为特征总数；
- $S$：不含特征 $i$ 的任意子集；
- $f_x(S\cup\{i\})-f_x(S)$：**特征 $i$ 在子集 $S$ 下的边际贡献**；
- 前面的阶乘系数：对所有子集大小做**公平加权**。

**第三步：SHAP 的可加性（这是它最实用的性质）**

$$y_i=y_{base}+f(x_{i1})+f(x_{i2})+\cdots+f(x_{ik})$$

其中 $y_{base}$ 通常是全体样本目标变量的**均值**（"平均水平"）。

- $f(x_{ij})>0$：该特征**推高**了预测值（对效率是正面作用）；
- $f(x_{ij})<0$：该特征**拉低**了预测值。

**这个性质意味着什么？** 意味着你可以把任意一次预测**精确地拆成"各个变量贡献了多少"的账本**——而且账目必须配平。这对工程师来说极其直观。

**第四步：为什么用 XGBoost 当被解释的模型？**

SHAP 需要一个"被解释的模型"。本文用 **XGBoost**：

$$\hat y_i^{(t)}=\hat y_i^{(t-1)}+f_t(\mathbf x_i),\qquad Obj=\sum_i l\big(y_i,\hat y_i\big)+\sum_k\Omega(f_k)$$

- 串行集成决策树，每轮拟合上一轮的**残差**；
- 目标函数 = **损失函数 + 正则项**（防过拟合）；
- 树模型天然支持高效的 SHAP 精确计算（TreeSHAP）。

本文 XGBoost 的精度：交叉验证 **RMSE = 0.0290%**，**相对误差 = 0.0274%**［§4.2］。

**第五步：本文的方法论创新——把 SHAP 从 0 维推到 1 维和 2 维**

这是本讲最值得记住的贡献：

| 层次 | 数据形态 | 能回答的问题 |
| :--- | :--- | :--- |
| **0 维** | 标量输出（级效率） | 哪些变量重要？**调大还是调小好**？ |
| **1 维** | 展向性能分布 | 这些变量影响**叶高的哪一段**？ |
| **2 维** | 子午面物理场云图 | 影响的**物理机制**是什么？ |
| 局部 | 典型样本归因 | 在这个具体设计里，各变量**贡献了多少**？ |

> 💡 传统敏感性分析止步于第 0 维。本文把它推进到"**定位到叶高位置**"和"**看到流场机制**"，这才是能指导设计的知识。

#### 4. 算法控制流

```
① 93 维参数化（静子 45+3，转子 45），LHS 采样，变量归一化到 (0,1)，0.5 = 基准
② NUMECA + S-A 湍流模型做 CFD，记录级效率 η 与流量 ṁ
③ 筛选：只保留流量偏离 ≤ ±1% 的样本
      1000 个 LHS 样本  →  352 个合格样本
④ 用 XGBoost 建效率代理模型（RMSE 0.0290%）
⑤ SHAP explainer 计算每个变量的 SHAP 值
⑥ 0 维：画 SHAP 条形图 → 识别显著变量
⑦ 1 维：画展向 SHAP 分布图 → 定位受影响的叶高区间
⑧ 2 维：画子午面物理场云图 → 解释物理机制
⑨ 局部：挑典型样本做归因分析
⑩ 总结设计准则 → 落到新设计 → CFD 验证
```

#### 5. 实验验证与量化突破

**研究对象**：GE-E3 高压涡轮**第一级**——静子 46 片、转子 76 片，转子叶尖间隙 0.3 mm。

**数值方法**［§3.1］：NUMECA 做网格与气动仿真，**Spalart–Allmaras** 湍流模型；进口总温 **719 K**、进口总压 **344.74 kPa**、出口静压 **139.473 kPa**、转速 **8279 r/min**；网格无关性研究表明网格超过约 **150 万**时效率与压比趋于稳定，最终选用约 **200 万**节点的结构网格；静子表面等熵马赫数分布与文献实验数据吻合良好。

**评价对象**：总对总效率

$$\eta=\frac{1-T^{*}_{2}/T^{*}_{0}}{1-\big(P^{*}_{2}/P^{*}_{0}\big)^{(k-1)/k}}$$

同时监测流量变化，以保证涡轮级在设计点附近工作。

**① 显著变量识别（对级效率）**［§4.2］

| 排名 | 变量 | 含义 |
| :---: | :--- | :--- |
| **1** | **AE** | 叶片**有效出口角**，$AE=\arcsin(o/t)$（$o$ 为喉部直径、$t$ 为栅距） |
| **2** | **D** | 静子**三维积叠参数**（周向中偏移 / 周向尖偏移 / 轴向尖偏移） |
| **3** | **PSL2** | 吸力面前缘附近的**样条控制点 2** |

补充发现：在静子与转子的 6 个截面中，**转子中截面 R2 与静子中截面 S2** 的影响程度高于其他截面。

**② 三条挖出来的设计准则**［§5 结论(1)–(3)］

**准则一（有效出口角 AE）**：
- **转子中截面（AE-R2）减小有效出口角 → 提升效率**；
- **静子中截面（AE-S2）增大有效出口角 → 提升效率**；
- 二者**配合**使用可在提升效率的同时**限制流量变化**。
- 1 维 SHAP 可视化进一步显示：减小 AE-R2 能提升**中段叶高**的效率，但会**牺牲上部端壁区**的效率——这是一个典型的**权衡**，而 0 维分析看不到这一点。

**准则二（静子三维积叠 D）**：
- 叶尖处**向压力侧**偏移 → 提升**叶尖**效率；
- 中截面处同样**向压力侧**偏移 → 提升**中段**效率；
- 两者共同形成**正倾斜（positively leaned）叶片**，有助于**减小静子叶道内的径向压力梯度**，从而提升涡轮效率。

**准则三（吸力面前缘样条控制点 PSL2）**：
- 把静子与转子中截面**吸力面前缘附近的样条控制点朝"叶片减薄"方向移动**，有利于**降低前缘加速区的压力梯度**，减少不可逆流动损失，从而在**大部分叶高范围**内提升效率。

**③ 最终验证**［§5 结论(4)］

| 指标 | 值 |
| :--- | :--- |
| 最终设计效率 | **91.09%** |
| 相对参考设计提升 | **+0.65%** |
| 与数据集的关系 | **超过数据集中的最大效率值** |

> 📌 **这一点特别关键**：最终设计**不是**数据集里挑出来的最好样本，而是**遵循准则重新设计**出来的，结果**比数据集里最好的还要好**。这说明挖出来的准则具有**外推价值**，而不只是对已有数据的拟合总结。

#### 6. 审稿人批判与承前启后

**可以攻击的点：**

1. **样本量偏小**：93 维设计空间只有 **352 个样本**（约 3.8 个样本/维）。对 XGBoost 建模尚可，但对 SHAP 的**高阶交互**估计与结论泛化都有风险。
2. **筛选引入了选择偏差**：1000 → 352 的筛选条件是"流量偏离 ≤ ±1%"。这意味着**所有结论只在这条流量约束带内成立**，不能外推到流量大幅变化的工况。
3. **SHAP 解释的是 XGBoost，不是真实 CFD**。虽然 XGBoost 交叉验证 RMSE 仅 0.0290%，但解释的可靠性上界**就是代理模型的精度**；在 93 维、352 样本的条件下，代理模型在样本稀疏区域可能失真，而 SHAP 无法自证。
4. **相关性 ≠ 因果性**。SHAP 给的是"在该数据集上、该模型内的贡献归因"，**不是物理因果**。三条准则的物理机制解释（径向压力梯度、前缘加速区）是作者的**后验解读**，需要独立的机理验证。
5. **未与 Sobol 指数、主动子空间做定量对比**，只在引言中引用了他人的对比结论。
6. **版本状态**：Journal Pre-proofs，无卷期页码，且日期记载存在两处不一致。

**承前：**
- **论文 11（DA-EGO）**：用 PCE/ANOVA 做变量交互分析，但那是**为子空间分解服务**的副产品；本文把"变量重要性"本身作为研究目标。
- **论文 18（P-ResUNet）**：用 ANOVA 圈定优化空间；本文用 SHAP，且能额外给出**影响的方向性**与**空间定位**。

**本文的独特价值**：前面十二讲都在问"**怎么找到最优解**"，本文问的是"**最优解为什么好**"——把优化留下的数据变成**可读的设计知识**。

**启后：**
- SHAP 框架可推广到多学科设计空间（原文结论明确提及）；
- 最自然的下一步是**闭环**：用 SHAP 挖出的准则**缩小设计空间**（例如把 PSL2、AE-S2 的变化范围收紧），再交给 DA-EGO / GSDE 去搜索。这样"知识发现"与"智能优化"就从两个独立环节变成一条流水线。

---

### 【第14讲】待重建（对应论文 15 · AI-PJP）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P15.md`；旧版内容保留在 git 历史中。

---

### 【第15讲】待重建（对应论文 20 · SPIE-AI）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P20.md`；旧版内容保留在 git 历史中。

---

# 第四篇 【流动控制、气动热耦合与风洞实验篇】

本篇覆盖**论文 03、09、10、04、05**。回到物理本体：**端壁二次流怎么控、叶尖怎么修、冷气怎么给、实验结果站不站得住。** 非轴对称端壁从数值优化（NAE-Purge）走到跨声速风洞实测验证（NAE-Exp），并用不确定性量化（Slot-UQ）回答一个工程上最要命的问题：**加工公差和工况波动，会让我的设计偏离标称值多远？**

---

### 【第16讲】待重建（对应论文 03 · NAE-Purge）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P03.md`；旧版内容保留在 git 历史中。

---

### 【第17讲】待重建（对应论文 09 · NAE-Exp）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P09.md`；旧版内容保留在 git 历史中。

---

### 【第18讲】待重建（对应论文 10 · FFD-Tip）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P10.md`；旧版内容保留在 git 历史中。

---

### 【第19讲】待重建（对应论文 04 · Slot-UQ）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P04.md`；旧版内容保留在 git 历史中。

---

### 【第20讲】待重建（对应论文 05 · Impingement-Film）

> 🚧 本讲尚未按重建规范重写。事实依据见 `corpus/facts/P05.md`；旧版内容保留在 git 历史中。

---

# 第五篇 【实践与代码篇】可运行算法原型库

> 🚧 代码库尚在建设中（见 `docs/重建计划.md` P3）。

---

# 第六篇 【对比、批判与前瞻篇】

> 🚧 待重建：全景对比矩阵将逐条标注数据来源，凡无出处者一律标注「待核实」，不再沿用旧版的无源数字。

---


