# P13 · EMFS / MSFO

> 规范编号 **13** ｜ 白皮书讲次 **第05讲** ｜ 证据等级 **B（出版商页面 + 摘要全文，全文未获取）**
> 建卡日期 2026-09-06 ｜ 依据 `corpus/web_evidence/P13.md`（ASME Digital Collection 页面，检索于 2026-09-05）

## 一、权威著录

| 字段 | 值 |
| :--- | :--- |
| 标题 | A Novel Multi-Fidelity Surrogate for Efficient Turbine Design Optimization |
| 作者 | Qineng Wang, Liming Song, **Zhendong Guo**（通讯）, Jun Li, Zhenping Feng |
| 期刊 | *ASME Journal of Turbomachinery* |
| 卷期页码 | **146(4): 041011**，共 10 页 |
| 论文号 / DOI | TURBO-23-1281 ｜ `10.1115/1.4064228` |
| 发表 | 正式 2024-04（线上 2023-12-21）；Received 2023-10-08 |
| 本地原文 | **无**（ASME 订阅墙，全文未获取） |

### 命名规范（旧版白皮书在此处用错，必须区分）

| 名称 | 全称 | 指代对象 |
| :--- | :--- | :--- |
| **EMFS** | Ensemble Weighted Multi-Fidelity Surrogate | **代理模型**（MFS 与局部 SFS 的自适应加权集成体） |
| **MSFO** | Multi- and Single-Fidelity surrogate fused Optimization | **优化算法**（用 EMFS 引导搜索的完整算法） |

❌ 旧版把标题写成"**EMFS 算法**"，把代理模型名当成了算法名；重建版统一写作"EMFS 代理模型 + MSFO 算法"。

## 二、摘要原文（English，逐字摘录，未改写）

> To solve the turbine design optimization problems efficiently, surrogate-based optimization algorithms are frequently used. To further reduce the cost of turbine design, the multi-fidelity surrogate (MFS)-based optimization is proposed by the researchers, who resort to augmenting the small number of expensive high-fidelity (HF) samples by a large portion of low-fidelity (LF) but cheap samples in surrogate modeling and optimization process. Nonetheless, according to our observations, the MFS-based optimization sometimes can only have better convergence rate at the early stage of optimization process, but yielding worse final solution than the single-fidelity surrogate (SFS)-based optimization that uses high-fidelity samples alone. The reason behind can be explained as follows. With the increase of HF samples in the optimization process, the LF samples can cause negative effect and therefore misleading the optimization search. To address the above issue, an ensemble weighted multi-fidelity surrogate (EMFS) is proposed. Specifically, the density-based spatial clustering of applications with noise is used to detect the region where the MFS cannot build a more accurate surrogate, and a local SFS is built there. Then, an EMFS is built by combining the MFS and SFS with adaptive weights, which is used to guide the optimization process. The related algorithm is named as multi- and single-fidelity surrogate fused optimization (MSFO). Through tests on GE-E3 blade optimization and the film cooling layout design of a turbine endwall, the effectiveness of proposed MSFO is well demonstrated.

**摘要中译**：为高效求解涡轮设计优化问题，代理模型辅助优化算法被广泛使用；为进一步降低设计成本，研究者提出基于多保真代理（MFS）的优化——在建模与优化过程中，用大量廉价低保真（LF）样本补充少量昂贵高保真（HF）样本。然而据作者观察，MFS 优化有时仅在优化早期具有更快收敛率，最终解却劣于仅用高保真样本的单保真代理（SFS）优化。原因可解释为：随着优化过程中 HF 样本增多，LF 样本会产生负面影响，从而误导优化搜索。为解决该问题，提出集成加权多保真代理（EMFS）：具体以 DBSCAN 密度聚类算法侦测"MFS 无法建立更准代理"的区域，并在该区域局部建立 SFS；随后以自适应权重把 MFS 与 SFS 组合成 EMFS，用于引导优化过程；相关算法命名为多/单保真代理融合优化（MSFO）。通过对 GE-E3 叶片优化与涡轮端壁气膜孔排布设计的测试，良好地验证了所提 MSFO 的有效性。

## 三、关键量化指标（逐条裁定后填入）

| 指标 | 数值 | 原文位置 | 证据等级 | 是否采信 |
| :--- | :--- | :--- | :---: | :---: |
| 验证算例数 | 2（GE-E3 叶片气动优化、涡轮端壁气膜孔排布） | 摘要 | B | ✅ |
| 保真层级数 | 2（HF / LF） | 摘要 | B | ✅ |
| 失真区侦测算法 | DBSCAN | 摘要 | B | ✅ |
| 融合方式 | 自适应权重（adaptive weights） | 摘要 | B | ✅ 定性采信 |
| 相对 SFS/MFS 的 CFD 节省次数 | 未获取 | 需全文 Table | X | ❌ |
| 精度提升百分比 | 未获取 | 需全文 §4 | X | ❌ |

> **必须删除的旧版填充**：6.1 矩阵中的"**250 次 CFD**""**15D~30D**""**消除低保真模型失真误导现象**"三条均无出处（见 `corpus/web_evidence/P13.md` §五），重建版已删，不得回流。

## 四、候选量化断言（已逐条裁定）

1. 摘要给出的**唯一硬事实是"机制"而非"数量"**：LF 样本在优化后期产生负面误导 → 局部改用 SFS。
2. "sometimes can only have better convergence rate at the early stage" → **采信为定性结论**，并保留原文的"有时（sometimes）"限定语；不可写成"多保真总是更差"。

## 五、方法机理

- **核心痛点**：多保真的教科书卖点"用低保真换样本量"隐含一个未验证前提——**LF 与 HF 的关系在全设计域近似一致**。叶栅流动在高负荷/分离区里 LF 系统性失真，于是"越多 LF 越好"反转为"越多 LF 越坑"。
- **传统方法为何失效**：Co-Kriging 一类的**全局**一致性假设（$y_{HF}=\rho y_{LF}+\delta$）在失真区不成立；而标准做法对全部样本一视同仁地融合，无法"局部放弃低保真"。
- **本文方法的数学表达**：
  ① 全局多保真关系 $y_{HF}(\mathbf{x})=\rho\,y_{LF}(\mathbf{x})+Z(\mathbf{x})$；
  ② 用 DBSCAN 在样本空间中把"预测偏差大"的点聚成失真簇（需要 $\varepsilon$、$MinPts$，具体取值全文未获取）；
  ③ 在簇内另建局部 SFS：$\hat y_{SFS}(\mathbf{x})$；
  ④ 融合：$\hat y_{EMFS}(\mathbf{x})=w(\mathbf{x})\,\hat y_{MFS}(\mathbf{x})+\big(1-w(\mathbf{x})\big)\,\hat y_{SFS}(\mathbf{x})$，$w(\mathbf{x})$ 为**自适应权重**（其具体函数形式全文未获取，禁止臆造）。
- **算法控制流**：初始 HF+LF 采样 → 建 MFS → 每轮加点后以 DBSCAN 侦测失真区 → 失真区建局部 SFS → 自适应加权成 EMFS → 用 EMFS 上的采集准则选下一 HF 点 → 循环。

## 六、验证与基线

- **验证算例**：① GE-E3 气动叶片优化（3D 黏性 RANS，公开基准叶型）；② 涡轮端壁气膜孔排布优化。
- **对比基线**：SFS-based optimization（仅 HF）与常规 MFS-based optimization。
- **量化结果**：摘要只作定性陈述"effectiveness … well demonstrated"，未给数字；**任何具体 CFD 次数/误差降幅均不可写**，须标 `［原文未获取］`。

## 七、局限与可攻击点

1. **DBSCAN 参数敏感**：$\varepsilon$、$MinPts$ 决定"失真区"边界，摘要未言如何取值，更未给敏感性分析——本方法的可复现性瓶颈。
2. **失真判据来自"预测偏差"，本身依赖代理**：用 MFS 自己的误差去判定 MFS 不可信区域，存在循环论证风险。
3. **仅两层保真**：HF/MF/LF 三层以上的权重设计未讨论。
4. **局部 SFS 需额外 HF 样本**：在失真区建单保真代理要花 HF 预算，与"省 HF"的初衷相冲突，摘要未量化这一代价。
5. **无统计显著性**：两个算例各单次优化，未给多种子重复的分布，"更好"可能是随机性。

## 八、承前启后

- **继承于**：**论文 02（Filter-GEI）**——同为"多保真加点策略"的直接延续，02 管"在哪里加 HF"，本文管"何时干脆别信 LF"；**论文 01（CR-EI）**——共享"采集准则会被噪声/模型误差带偏"这一问题意识。
- **启发了**：**论文 16（TNO）/ 论文 12（SDNO）**——与其在代理层做真伪融合，不如让网络直接学全场，回避保真一致性问题；**论文 19（SHAP）**——把"哪些区域/变量不可信"从聚类升级为可解释归因。

---

> ⚠️ 全文未获取：本卡 §五④、§六为摘要粒度；自适应权重公式与全部数字均标 X，禁止写入白皮书正文。
