# Mr.GUO 仓库重建总纲：从"看起来很全"到"经得起查"

> **制定日期：** 2026-09-05
> **适用分支：** `arena/01a07173-mr-guo`
> **制定依据：** 对 `main`（85fe2ae）全量审计 + 20 篇论文的 DOI/元数据逐条核查（见 `corpus/doi_verification.md`）
> **核心原则：** **没有出处的话不写，跑不通的代码不交，对不上的数字不填。**

---

## 零、为什么需要重建（审计结论）

仓库上一版的自我描述与实际内容严重不符。以下每一条都可以用命令复现：

| # | 自我宣称 | 实际状况 | 严重度 |
| :--- | :--- | :--- | :--- |
| 1 | "篇篇俱全，绝不略写" 的 20 讲保姆级剖析 | 20 讲中 **9 讲无一个小节**（仅"文献+作者+一段话"，约 300 字），8 讲仅 1 个小节；唯一写全的是第 01 讲（1830 字 / 5 节）。全书 25,384 字 | 🔴 致命 |
| 2 | "完整代码库"、"工业级 Python 核心算法原型库" | 仓库内**零个** `.py` 交付文件；所谓代码是 4 段内嵌 markdown 片段。其中 5.1 引用 `scipy`（与"零外部依赖"矛盾），5.3 的 `optimize_step` 在 `self.rbf` 赋值前调用，**无法运行** | 🔴 致命 |
| 3 | "中英双语高清插图" | 9 张图仅 5 处引用，**2 处链接错图**（第 03 讲英文位用了 `ai4turbomachinery_evolution_map.png`，第 11 讲英文位用了叶片涡系图）；`ai4turbomachinery_evolution_map_zh.png` 从未被引用 | 🟠 高 |
| 4 | 20 篇论文的全景索引 | **3 篇元数据错误**：GMFoO 的 DOI 尾号错；ResUNet 的 DOI 指向一篇**完全无关**的论文；AI-PJP 的年份错（2024 → 应为 IEEE CEC 2025） | 🔴 致命 |
| 5 | "六步流水线科研超脑引擎" | `skills/omni_scholar/core.py` 所有方法返回硬编码占位串，含 `return f"...# 核心逻辑待注入..."` | 🟠 高 |
| 6 | 统一的论文编号体系 | README/全集用 01–20，白皮书按主题自行重排（白皮书"第10讲" = 全集 16 号），同一书内混用"15 篇 / 20 篇 / 15+5 篇" | 🟠 高 |
| 7 | 6.1 全景对比矩阵的量化指标 | 多处数字无出处（如 ResUNet"精度 98%"实际原文为 MRE 1.16% / MAE 0.26%；GAN-Endwall"消除 90% 畸变几何"无来源） | 🔴 致命 |

**一句话：上一版交付的是一份"结构与文案都很漂亮、但内容与事实都对不上"的骨架。**

---

## 一、重建目标与验收标准（可量化，逐条可验）

### 目标 A：内容达标
| 验收项 | 标准 | 验证方式 |
| :--- | :--- | :--- |
| 讲义完整性 | 20 讲**每讲**均含 6 个规定小节，单讲正文 ≥ 1200 字 | `tools/verify_whitepaper.py` 自动统计 |
| 全书篇幅 | 白皮书正文 ≥ 60,000 字（现 25,384 字） | 脚本统计 |
| 数字可溯 | 每一个量化指标后标注来源：`［原文 P07 §4.2］` / `［出版商页面］` / `［原文未获取］` | 正则扫描 + 人工抽检 |
| 零虚构 | 无任何无出处的百分比/倍数断言 | 脚本抽取所有 `%` 断言，逐条比对 `corpus/` |

### 目标 B：代码可用
| 验收项 | 标准 |
| :--- | :--- |
| 真实交付 | `code/` 目录含 ≥ 6 个独立可运行 `.py` |
| 可运行 | `python3 -m pytest tests/ -q` 全绿；每个脚本 `python3 code/xxx.py` 能在 < 60s 内跑出结果 |
| 依赖合规 | 第三方依赖限于 `numpy`；文中声明与实际 imports 严格一致 |
| 有输出 | 每个算法附带一个可复现的数值/图像验证（如 CR-EI 在 Branin 上的收敛曲线） |

### 目标 C：配图正确
| 验收项 | 标准 |
| :--- | :--- |
| 引用正确 | 每张被引用的图，其文件名与图注语义一致；无错链、无死链 |
| 覆盖均衡 | 四篇主体各有配图；中英双语成对，无孤儿文件 |
| 内容相符 | 图内元素与其所配讲义的物理内容一致（人工逐张核对） |

### 目标 D：口径统一
| 验收项 | 标准 |
| :--- | :--- |
| 唯一编号 | 全仓库统一使用 01–20 编号；白皮书"第 N 讲"与之并列标注（如「第 03 讲 · 论文 16」） |
| 篇数表述 | 统一为「全集 20 篇，其中本地有原文 15 篇」 |
| 元数据 | 20 篇的标题/作者/期刊/卷期页/DOI 全部与 `corpus/` 一致 |

### 目标 E：技能包诚实
| 验收项 | 标准 |
| :--- | :--- |
| 真能跑 | `skills/omni_scholar/` 提供真实可执行的抽取与分析能力，而非占位串 |
| 有演示 | 附 `README` + 一条可复制的端到端命令，输出真实结果 |

---

## 二、分阶段路线图（从大局到细节）

### ✅ P0 审计与事实基线 —— **已完成**
- [x] 全仓库结构与内容审计（上表 7 项）
- [x] 本文件随进度更新（上一轮未做，本轮补：见 §四、§五）
- [x] `tools/extract_corpus.py` 语料抽取管线（13 PDF + 2 HTML，共 ~80 万字原文）
- [x] 20 篇 DOI 逐条核查 → `corpus/doi_verification.md`（查出 3 处硬伤）
- [x] 5 篇无原文论文的网络补全 → `corpus/web_evidence/P{04,05,13,14,20}.md`

### ✅ P1 事实卡片 —— **已完成（2026-09-06）**
把 20 篇论文各自抽成一张**结构化事实卡**（`corpus/facts/PXX.md`），每张固定含：
1. 权威著录（标题/作者/期刊/卷期页/DOI/发表日期）
2. 摘要原文（英文逐字 + 中译）
3. **关键量化指标表**（指标名 / 数值 / 原文位置 / 证据等级）
4. 方法机理（数学公式、网络结构、算法流程图）
5. 验证算例与基线对比
6. 局限性与可攻击点（供"审稿人批判"小节使用）
7. 与其余 19 篇的承前启后关系

> 证据等级定义：**A**=本地原文可查 / **B**=出版商页面或摘要 / **C**=仅学者主页线索 / **X**=无来源（禁止写入正文）
>
> **完成情况（2026-09-06）**：20/20 张卡已依原文或 `corpus/web_evidence/` 填写，`corpus/facts/` 内 TODO 归零。
> 其中 7 张（P04/P05/P09/P10/P13/P14/P20）此前仍是骨架，本轮补齐；P17 缺"英文逐字摘要"与"验证与基线"两栏，一并补齐（现 20 张每张 ≥7 栏）。
>
> ⚠️ **本轮复核更正（2026-09-06 深夜）**：上一句"均为 7 栏"只说了栏数，掩盖了内容差距——当时 P08/P15/P16/P18/P19 的 §二仍是"中译要点"，**缺英文逐字摘要**，与 P1 规范"摘要原文（英文逐字 + 中译）"不符。本轮已从 `corpus/txt/` 逐字提取补齐（换行合并、文字逐字）。现 20 张：18 张英文逐字 + 中译齐全，P14/P20 诚实标注"未获取"（无源不写）。F1b 同步收紧为"7 类规定栏目名"检查。
> `tools/build_fact_cards.py` 现对**不含 TODO 的卡片一律跳过**（`[KEEP]`），避免重跑生成器冲掉人工成果；如需重建骨架显式加 `--force`。

### ✅ P2 白皮书重建（核心工作量）—— **已完成（2026-09-06）**
按统一模板重写 20 讲。每讲固定 6 节：
1. **生活与物理直觉引子**（初高中物理可懂）
2. **叶轮机械中的真实工程死穴**（为什么非做不可）
3. **数学公式从 0 到 1 推导**（关键式逐步推）
4. **算法控制流 / 网络架构**（伪代码或结构图）
5. **实验验证与量化突破**（**所有数字带出处标注**）
6. **审稿人批判与承前启后**（局限 + 下一篇如何接上）

每讲开头用统一元数据块标注：规范编号、讲次、本地原文文件名、证据等级。

> **完成情况（2026-09-06）**：20 讲均按 6 节模板成文（`docs/lectures/01–20.md`），白皮书由 `tools/build_whitepaper.py` 自动装配，
> 全书去代码正文 116,190 字（≥60,000；含第五篇嵌入代码则为 219,568 字——旧口径把代码计入篇幅，虚胖近一倍，A1 已收紧为去代码统计）。第六篇【对比、批判与前瞻篇】此前是**只有"🚧 待重建"的空壳**，本轮以 `docs/part6.md` 实作：
> 6.1 全景对比矩阵（四条主线分表，每个数字带 `［原文 PXX 位置］`）、6.2 横向批判、6.3 前瞻选题、6.4 旧版虚构数字公示、6.5 收口。
>
> **一处受控例外（如实登记，不掩盖）**：第 15 讲（论文 20，SPIE 会议）的"数学"小节最初写作"本讲不推导"——当时该文**无原文、无摘要、证据等级 C**。
> **2026-09-06 第三轮更新**：论文 14、20 已通过 Crossref 取得 DOI（`10.1115/GT2024-128792`、`10.1117/12.3117536`）与出版商摘要，
> 第 09、15 讲已按摘要重写为 B 级（数学小节只推导摘要明示的结构，摘要以外一律"原文未获取"）；**全集已无 C 级条目**。

### ✅ P3 真实代码库 —— **已完成（2026-09-06）**
`code/` 目录，NumPy-only，每个文件含 `if __name__ == "__main__":` 自检。
（下表为**实际落地**的模块清单；最初规划的 `gp_core.py / cr_ei.py / filter_gei.py / gsde.py / da_ego.py / sdno_superposition.py / shap_mining.py` 七个按论文命名的文件已改为按**算法功能**组织，以避免模块间互相 import——旧表已于 2026-09-06 第三轮删除。）

| 文件 | 对应论文 | 自检内容 |
| :--- | :--- | :--- |
| `gp.py` | 01/02/03/04/06/08/17 | 普通 Kriging + EI/GEI，插值性、留一误差、超参恢复 |
| `optimizer.py` | 01、07 | DE/PSO/EGO/**CR-EI**/GSDE 骨架，在基准函数上收敛 |
| `multifidelity.py` | 02、13、17、08 | AR1 Co-Kriging、Filter-GEI 的 ω/T 原文公式、DBSCAN、EMFS 集成 |
| `rbf.py` | 07 | 三次 RBF + 线性尾项（GSDE 的代理） |
| `decomposition.py` | 11 | 动态子空间分解 + 聚合（DA-EGO） |
| `sampling.py` | 04、05、11 | LHS、均匀随机、尺度变换、粗粒度敏感性 |
| `benchmarks.py` | 01/06/07/08/11 | 14 个基准函数（含论文 01 的 Camel6/Hartman6/Shekel4/Trid10） |
| `moo.py` | 16 | 非支配排序 / 拥挤距离（NSGA-II 组件） |
| `metrics.py` | 09/10/04/05/07 | 总压损失系数、气膜有效度、Nu、等熵效率等口径 |
| `transfer.py` | 08 | MMD²、源任务排序、负迁移判定、SW-VAE 加权采样、FFD 网格 |

配套 `tests/` 单元测试 + `requirements.txt`（仅 numpy）+ `Makefile`。详细的模块→论文对照与设计规则见 `code/README.md`。

> **完成情况（2026-09-06）**：`code/` 10 个模块，全部含 `__main__` 自检且单模块 <10s；`tests/` 9 个文件、**129 passed + 37 subtests**。
> 新增 `Makefile`（`venv/corpus/whitepaper/checks/verify/selftest` 等目标）。
> 依赖口径明确为两处：`code/requirements.txt` 只含 numpy（算法库需能在无外网节点直接跑）；
> `tools/requirements.txt` 收录 `pypdf`/`pytest`（仅服务于"把 PDF 变成可核验语料"与测试，不参与算法实现）。
> `corpus/txt/` 属派生产物、不入库，因此**新克隆必须先 `make corpus`**；技能包对缺失语料改为干净降级（退出码 0 + 降级横幅 + 修复命令），不再抛 traceback。
> （⚠️ 原文"退出码 2"系笔误：实现一直是退出码 0，本轮已把文档与实现统一；E4 同步改为分模式判定并如实报告"完整/降级"模式。）

### ✅ P4 配图修复 —— **已完成（2026-09-06）**
- 修 2 处错链，补 `ai4turbomachinery_evolution_map_zh` 的归属
- 补齐缺失的英文版配图（GSDE/DA-EGO、气膜叠加）
- 中英成对，图注与讲义内容逐张人工核对

> **完成情况（2026-09-06）**：12 张图（6 主题 × 中英）全部成对、全部被引用、零死链；旧版 2 处错链与 1 张孤儿图均已消除。
> 新增验收项 C4 强制"第一至第四篇各有配图"——第二篇（论文 06/08/17/14，生成式与迁移）此前**一篇无图**，
> 本轮在 `docs/lectures/06.md` 引用演进路线图（其第 3 格"生成式模型（GAN/VAE）—更省的表征"正是本篇主题）。
>
> ⚠️ **本轮复核（2026-09-06 深夜）**：目标 C 要求"图内元素与讲义内容一致（人工逐张核对）"，此前从未真正执行过。
> 本轮人工核对 6 张中文图：布雷顿循环方向、EI 加点逻辑、GSDE/DA-EGO 流程、气膜叠加公式、涡系结构、演进路线五阶段均正确，与所配讲义相符。
> 唯一瑕疵：演进路线图第 5 格并列了可压/不可压两组连续方程（示意"把守恒律写进网络"的两种典型形式）——已在前言图注中如实说明，不改图。

### ✅ P5 技能包"从假到真" —— **已完成（2026-09-06）**
重写 `skills/omni_scholar/`：真正调用 `tools/extract_corpus.py` 的抽取能力，实现
① 文献元数据与 DOI 核验 ② 四维骨架抽取 ③ 跨文献演进拓扑 ④ 事实卡生成。
附端到端演示命令与真实输出。

> **完成情况（2026-09-06）**：`core.py` 为真实检索实现（无占位串），`--demo` 在完整语料下输出 106 行、退出码 0，每条证据带 `(文件, 字符区间)` 可回溯；无语料时输出 58 行降级指引（E4 如实区分两种模式）。
> 本轮另修：演进拓扑曾用启发式 `year_hint` 排代（把 P02 误标为 2016、漏掉无原文的 5 篇），现改用人工裁定的 `curated_year`（20 篇全覆盖）。
> 补 `skills/omni_scholar/README.md`（一次性准备 + 可复制的 `--demo` + 命令表 + "刻意不做的事"）。
> 上一轮此处有**两处硬伤**：`--demo` 在缺语料时直接 `ValueError` traceback、README 根本不存在——均由 E3/E4 新检查项锁定。

### ✅ P6 索引与文档对齐 —— **已完成（2026-09-06）**
README / 全集汇总 / 公开论文整理 / 付费论文五篇整理 / 论文链接整理 五份文档
按 `corpus/doi_verification.md` 统一修正 DOI、年份、卷期、编号口径。

> **完成情况（2026-09-06）**：上一轮只改了白皮书、**索引文档没跟上**。本轮修正论文 15（据本地 PDF 版权页逐字印证：
> *2025 IEEE Congress on Evolutionary Computation (CEC)*、ISBN 979-8-3315-3431-8、DOI `10.1109/CEC65147.2025.11043110`）
> 在 README/全集汇总/论文链接整理中的 5 处 2024 残留；并消除论文 11 的 "(2025/2024)" 骑墙写法，统一为
> **"2024-04-18 在线发表；正式卷期 2025, 57(2): 514–542"**。
> **年份口径约定（此后统一执行）**：索引表"年份"列 = **首次正式发表年**（在线优先）；卷期年另在备注注明，二者不再混用。
>
> ⚠️ **本轮复核补遗（2026-09-06 深夜）**：P6 自称"五份文档已对齐"，实际漏掉以下硬伤，本轮一并修复并由 D4b/D2b/D5 锁定：
> 1. 全集 18 节 "Official Abstract" 中 14 节是**改写版冒充逐字**（P09/P10/P04 先修，其余 11 节本轮从事实卡/语料逐字替换；P09/P14/P15 三节补/诚实标注后，18 节逐字 + 2 节诚实无源）。
> 2. 全集 6 处"HTML 全文/全文文献"**夸大表述**（两 htm 仅为摘要页）→ 改为"HTML 摘要页"；README 同步修正。
> 3. 全集本地映射表 P11 仍写 "Eng Opt (2025)"**单年份旧口径** → 双标注；README/全集缩写文件名（`...enhanced...pdf`）→ 真实全名。
> 4. 全集/公开/付费自有序号**无规范编号对照** → 逐节追加"规范编号 **NN**"；公开文档明确收录口径（13 篇，IEEE 两篇不在收录范围）；公开 P11 条目补卷期双标注；"总压总效率"笔误 → "总-总等熵效率"。
> 5. 全集 L86 把 MA=0.8 单点最优的 14.0% **泛化到 0.6–0.95 全工况**（违反事实卡 P09 禁令）→ 补工况限定；README 两处 14.0% 补 (MA=0.8)。

### ✅ P7 自动验收 —— **已完成并收紧（2026-09-06）**
`tools/verify_all.py` 一键跑完目标 A–E 的全部机器可验项，输出 PASS/FAIL 报告。

---

## 三、写作与标注规范（重建版强制）

1. **数字必有出处**：正文中出现任何百分比/倍数/CFD 次数，紧跟 `［来源］` 标注。
2. **证据等级外显**：每讲元数据块标注该讲最高证据等级；C 级及以下的论文，正文明确写"原文未获取"。
3. **不写无法核实的套话**：禁止"显著提升""大幅降低"这类无量化、无出处的形容词。
4. **编号唯一**：论文一律用 01–20；讲次用"第 N 讲"。任何一处同时给出，如「第 12 讲 · 论文 18」。
5. **代码与文档同源**：白皮书第五篇的代码示例由 `code/` 中的真实文件**自动嵌入**，杜绝文档与代码脱节。

---

## 四、进度快照

| 阶段 | 状态 | 产出 |
| :--- | :---: | :--- |
| P0 审计与事实基线 | ✅ 完成 | `corpus/doi_verification.md`、`corpus/web_evidence/`、`tools/extract_corpus.py` |
| P1 事实卡片 | ✅ 完成（2026-09-06） | `corpus/facts/P01–P20.md`：20/20 已依原文填写、每张 7 栏、TODO 归零 |
| P2 白皮书重建 | ✅ 完成（2026-09-06） | 20 讲 ×6 节、去代码正文 116,190 字；`docs/part6.md` 补齐第六篇（6.1–6.5） |
| P3 真实代码库 | ✅ 完成（2026-09-06） | `code/` 10 模块自检全绿、`tests/` 129+37 用例、`Makefile`、双 requirements |
| P4 配图修复 | ✅ 完成（2026-09-06 第四轮，第十三轮口径修正） | 20/20 讲均至少 1 张配图（C5 检查锁定）；**31 张主题图（12 张中英成对旧图 + 5 对新成对 + 9 张仅中文版）+ 42 张手绘讲稿图，共 73 个文件**；77 处引用、零死链零孤儿（见 §八、§十一与 §十四） |
| P5 技能包 | ✅ 完成（2026-09-06） | `core.py` 真实实现 + `skills/omni_scholar/README.md`；缺语料时干净降级 |
| P6 索引与文档对齐 | ✅ 完成（2026-09-06） | 论文 15 年份/DOI 三文档统一；论文 11 骑墙口径消除；年份口径约定成文 |
| P7 自动验收 | ✅ 完成并再次收紧（2026-09-06 第四轮合并后） | `verify_all.py` **35 项**（第三轮的 D5-规范编号并入为 **D6**，与本仓 D5-工况限定撞号改称；D4a 扩至 20/20；D4b 增 4 条守卫）+ `verify_whitepaper.py` 逐讲明细 + `selftest_verifier.py` **16 类**反身测试 |

**审计发现的"上一轮遗留"与本轮处置**（原表 7 项硬伤的闭环情况）：

| 上一版审计条目 | 上一轮处置 | 本轮核查结果 |
| :--- | :--- | :--- |
| ①9 讲无小节 / 篇幅 25,384 字 | 已重写 | ✅ 20/20 讲齐 6 节，去代码正文 116,190 字 |
| ②零 .py 交付、内嵌代码跑不通 | 已建 `code/` | ✅ 10 模块可跑、129 测试通过 |
| ③2 处错链、1 张孤儿图 | 已修 | ✅ 已修；但第二篇当时仍无图 → 本轮补（C4） |
| ④3 篇元数据错误（P06/P18/P15） | 白皮书已改 | ⚠️ **只改了白皮书**，README/全集/链接整理未同步 → 本轮修正（D4a/D4b） |
| ⑤技能包占位串 | 已重写 | ⚠️ 实现为真，但 `--demo` 缺语料即 traceback、README 缺失 → 本轮修（E3/E4） |
| ⑥编号与篇数口径 | 已统一 | ✅ D2/D3 通过 |
| ⑦6.1 矩阵无源数字 | 未处理 | 🔴 **第六篇当时是空壳**，只留"🚧 待重建" → 本轮实作（A6） |

## 五、当前验收成绩单（由 `tools/verify_all.py` 自动判定）

```bash
make verify          # 等价于 python3 tools/verify_all.py
make selftest        # 反身测试：故意注入缺陷，断言裁判必须变红
```

| 轮次 | 结果 | 说明 |
| :--- | :--- | :--- |
| 2026-09-05 基线（重建开始前） | **4 / 14** | 旧验收器 |
| 2026-09-06 上一轮（自称"全面完美完成"） | 报 **18/18** | ⚠️ 实为放水，见下 |
| 2026-09-06 次轮（验收器收紧后） | **28 / 28** | 判定口径严格化后重测，但仍有漏网，见下 |
| 2026-09-06 本轮（复核审计 + 修内容 + 再收紧裁判） | **34 / 34** | 6 项新增检查 + 9 处口径收紧；反身测试 14/14 |
| 2026-09-06 第三轮（独立复审，见 §六） | 修正前 **34 / 34 但内容仍有 12 类实质缺陷** → 修正后（合并前） **29 / 29** | 新增 D5/D4a/D4b 扩展；验收器全绿≠内容为真 |
| 2026-09-06 第四轮（两分支合并，见 §七） | **35 / 35** | 采纳第三轮全部内容修正；D5-规范编号并入为 D6（与既有 D5-工况限定撞号）；反身测试 **16/16** |
| 2026-09-06 第四轮·配图补齐（见 §八） | **36 / 36** | 新增 **C5-每讲配图**（20/20 讲至少 1 图，35 处引用）；修 C3 后缀误删 bug；反身测试 **17/17** |
| 2026-09-06 第五轮（独立复核，见 §九） | 修正前 **36 / 36 但"逐字摘要"3 篇注水 + 13 处无源套话** → 修正后 **38 / 38** | 新增 A7-无源形容词、F5-逐字摘要属实；反身测试 **19/19** |
| 2026-09-06 第六轮（外推检查边界，见 §十） | **41 / 41** | 新增 D7-讲次论文映射、A8-算式自洽、A9-公式结构；反身测试 **22/22** |
| 2026-09-06 第七轮（手绘风配图重做，见 §十一） | **41 / 41** | 图 7/41 完成；C5 图引用 35→42；本轮因内容错误返工 3 次 |
| 2026-09-06 第八轮（配图续做，见 §十一.五） | **41 / 41** | 图 17/41；C5 引用 42→52；2 张待返工（英文杂字） |
| 2026-09-06 第九轮（返工＋续做，见 §十一.六） | **41 / 41** | 图 25/41；C5 引用 52→60；C 风格首秀；1 张待返工（中文乱码词） |
| 2026-09-06 第十轮（返工＋续做，见 §十一.七） | **41 / 41** | 图 33/41；C5 引用 60→68；1 张待返工、1 张生成失败待重试 |
| 2026-09-06 第十一轮（配图收尾，见 §十一.八） | **41 / 41** | **图 41/41 全部完成**；C5 引用 68→77；1 张标注重复遗留 |
| 2026-09-06 第十二轮（全量图文校对，见 §十二） | **42 / 42** | 新增 C6-图注数字有源；修 C5 只扫派生物的盲区；反身 23/23 |
| 2026-09-06 第十三轮（独立复审，见 §十四） | 修正前 **42 / 42 但图内 1 处文字残留 + 规范"次数"条目无裁判兜底** → 修正后 **42 / 42** | A5 扩至"N 次 CFD"类次数断言 + 03 讲补 1 处标注；L05_s2 图内"形形状"重制；语料去 NUL；README 三处过期口径同步；反身 **24/24** |

**"18/18 但仍有大量不达标"的根因，是裁判比标准松**。本轮对验收器做了如下收紧，使 PASS 重新有意义：

| 检查项 | 旧判定 | 现判定 |
| :--- | :--- | :--- |
| A4 小节完整 | 6 节中命中 **≥4** 即放行 | 必须 **6/6**，并列出每讲缺哪节 |
| A5 数字可溯 | 块内**出现任意"［"即算有标注**；且容忍 ≤5 处无源 | 只认 `［原文…／出版商…／§n／Table n／摘要／由…计算］`；**零容忍**；取消"短标题/引注豁免"后门 |
| E1 占位检测 | `"待注入" in l or "TODO" in l and "raise NotImplementedError" in src`（and 优先级使 TODO 检测整体短路） | 分别独立扫描占位串与 `NotImplementedError` |
| 第六篇 | **完全未检查**（空壳也 PASS） | 新增 **A6**：≥3000 字 + 必须含 6.1 矩阵 + 不得残留"待重建/待补/TODO" |
| 事实卡片 | **完全未检查** | 新增 **F1a/F1b**：20 张、TODO 归零、每张 ≥7 栏 |
| 技能包可用性 | 只做 `ast.parse` | 新增 **E3**（README + 端到端命令）、**E4**（真跑 `--demo`，须退出码 0、无 traceback、有实质输出） |
| 元数据对齐 | 只比对 `index.json` 内部字段（抓不到文档里的错年份） | 新增 **D4a**（已确证 DOI 必须落到索引文档）、**D4b**（已裁定错误串不得复现，且排除"辟谣式引用"） |
| 配图覆盖 | 只查死链/孤儿 | 新增 **C4**：第一至第四篇各有配图 |
| 交付物与依赖 | 未检查 | 新增 **F2**（Makefile、`verify_whitepaper.py`）、**F3**（抽取依赖 pypdf 必须在 requirements 声明） |

**本轮（2026-09-06 深夜）复核审计发现"28/28"仍有漏网**，根因依然是"裁判没查那一项"。本轮修内容的同时对验收器做第二轮收紧：

| 检查项 | 次轮判定（仍有放水） | 现判定 |
| :--- | :--- | :--- |
| A1 篇幅 | 代码计入正文（219,326 字，虚胖近一倍） | 去代码块统计（116,190 字，仍 ≥60,000） |
| A4 小节 | 关键词在块内出现即算（含正文顺口提及） | 必须出现在 `####`/`###` 标题行 |
| A5 数字可溯 | 只查百分比 | 百分比 + **倍数**（抓出 6 处无源倍数，已补出处/改写） |
| A6 第六篇 | 只认 6.1 | 6.1–6.5 小节齐全 |
| B4b/B5b 真跑 | 只数字符串（无 numpy 时 19 项全挂仍 5/5 PASS） | **真跑**模块自检 + pytest；缺依赖明确 FAIL 并提示 `make venv` |
| D1b 年份载体 | 只比 DOI | curated 年份/载体与裁定交叉核对（曾漏 P02→2016、P15 载体误标） |
| D2b 编号落地 | 只查 README 索引表 | 公开/付费/全集规范编号标注齐全 |
| D4b 禁用串 | 5 个 | 14 个（+P11 单年份旧口径、HTML 全文夸大、3 段改写摘要特征串、笔误） |
| D5 工况限定 | 未检查 | **新增**：每个 14.0% 的 ±300 字符窗口内必须有 MA=0.8 限定 |
| E4 演示 | 只数行数（58 行降级与 106 行完整都 PASS） | 分模式判定：完整模式须 ≥3 条正文回溯证据；降级模式须含修复指引 |
| F1b 事实卡 | 只数 `##` 个数 | 7 类规定栏目名齐全 |
| F4 源一致性 | 未检查 | **新增**：白皮书与 docs+code 源逐字节一致（build 改确定性内容哈希戳） |
| 技能包年份 | 用嗅探 hint（含 2016 错误、漏 5 篇） | 改用 curated（20 篇全覆盖） |

> 为确保**新红灯真的有效**（而不是又一次自我宣称），`tools/selftest_verifier.py` 会把 6 类已知缺陷
> 逐个注回仓库（删小节、插入无源百分比、掏空第六篇、把事实卡改回骨架、让错误年份回流、抹掉端到端命令），
> 断言验收器必须变红；跑完自动还原。当前结果：**14/14 全部捕获，还原后 34/34**（变异用例从 6 类扩到 14 类，覆盖本轮所有收紧项；非 B 用例用加速模式跳过 pytest 真跑，B 用例全量真跑）。

> 为确保**新红灯真的有效**（而不是又一次自我宣称），`tools/selftest_verifier.py` 会把 16 类已知缺陷
> 逐个注回仓库（删小节、插入无源百分比、掏空第六篇、把事实卡改回骨架、让错误年份回流、抹掉端到端命令等本仓 14 类，
> 加第三轮追加 2 类：无本地原文论文的 DOI 从索引消失、规范编号从付费五篇整理消失），
> 断言验收器必须变红；跑完自动还原。当前结果：**16/16 全部捕获，还原后 35/35**（非 B 用例用加速模式跳过 pytest 真跑，B 用例全量真跑）。

> 该成绩单是后续每一轮工作的验收入口：**任何一轮结束前必须重跑 `make verify` 与 `make selftest`，且通过项不得回退。**

## 六、第三轮独立复审（2026-09-06，不采信上一轮自报的 28/28）

上一轮自称"全面完美完成"。本轮**不看验收器输出、直接对照原文与 Crossref** 重新抽查，发现验收器 28/28 全绿之下仍有以下实质缺陷，均已修正：

| # | 缺陷（验收器为何没抓到） | 处置 |
| :--- | :--- | :--- |
| 1 | 论文 14、20 被标为"无 DOI、无摘要、C 级"——实为**假阴性**：Crossref 首次书目查询即命中 `10.1115/GT2024-128792`、`10.1117/12.3117536`，出版商摘要公开可读（验收器只检查"已确证 DOI 是否落到文档"，不会质疑"未确证"本身） | 重写 `corpus/web_evidence/P14,P20.md`、`corpus/facts/P14,P20.md`、第 09/15 讲（B 级）；DOI 同步到 README/全集/链接整理/付费五篇/`extract_corpus.py` |
| 2 | 第 09 讲代号 "GAN-Endwall"——摘要明确是 **VAE + NURBS 层**，全仓库 15 处旧代号 | 统一改为 "VAE-NURBS 端壁参数化"，6.4 表登记旧判定错误 |
| 3 | 6.4 曾把论文 20 的"毫秒级"判为"无来源删除"——SPIE 摘要原话正是 "millisecond level" 与 "relative errors … below 1%" | 恢复并加 ［出版商页面 摘要］ 标注，6.4 表如实登记"旧判定本身有误" |
| 4 | 论文 04 与 13 的**作者互换**（P13 `10.1115/1.4064228` = Wang/Song/Guo/Li/Feng；P04 `10.1115/1.4051416` = Tao/Guo/Song/Li）；论文 05 漏列第一作者 Hongyan Bu（与 Guo 并列一作） | 依 Crossref/ASME 页面修正全集汇总 §三 卡片、付费五篇、`doi_verification.md`、第 20 讲与 P05 事实卡（P04/P13 的事实卡与讲义元数据经核对本已正确，未动） |
| 5 | 第 08 讲把论文 08 的数字（4.22%、120→100 叶片、分离泡）写成论文 17 的结论（验收器 A5 只看有无"［原文…］"标签，不校验标签指向） | 按 P17 Table 1/Table 3 重写，并加注说明归属 |
| 6 | 论文 12 正式版 *Physics of Fluids* 36(12):126110（`10.1063/5.0239483`）未登记，仍写"正式卷期与 DOI 未知" | 事实卡、第 11 讲、公开论文整理、链接整理、`doi_verification.md` 补记；数字仍引自本地 SSRN 版并声明 |
| 7 | 全集汇总 §三 有 17 处 "Official Abstract" 实为中文转述；两篇 `.htm` 被称作 "HTML 全文" 实为**仅摘要页** | 改为"摘要转述"；README/front_matter/全集 §四 改为"ScienceDirect 页面，仅摘要级" |
| 8 | 索引文档编号不统一：全集 §三 卡片、公开/付费/链接整理均只有"文内顺序号"，无规范编号 | 所有条目标题加 **【论文 XX】** 规范编号并加编号说明；公开论文整理补 IEEE 两篇（06、15），使 15 + 5 = 20 |
| 9 | 无源形容词残留：全集汇总"显著提升/从根源上消除/全面超越/极强"等 9 处、讲义 4 处（04/12/16/18） | 全部替换为带出处的量化表述或明确标注"原文自评" |
| 10 | `code/gp.py` 文档串把论文 03/14/16/17/18 的方法写成 ASMO/MFSIK/PCGPR/GP-Hedge 等**原文不存在的名字**；`multifidelity.py` 声称 Co-Kriging 用于论文 03/05（原文无此结构） | 按 corpus 逐篇核对后重写模块头注释与 `code/README.md` 对照表 |
| 11 | 本文件 P3 表列出 7 个不存在的文件名 | 换成实际 10 模块清单 |
| 12 | 验收器盲区：D4a 只查"从 PDF 抽到的 DOI"，5 篇无本地原文的论文**从未被检查**；编号贯穿性无任何检查 | D4a 扩至 20/20（声明 DOI 兜底，且须同时落到链接整理与全集汇总）；新增 **D5** 规范编号贯穿（全集/链接各 20 个标签，公开∪付费=20 且不重叠）；D4b 新增 4 条守卫（GAN-Endwall / SPIE-AI / HTML 全文 / Official Abstract）；反身测试增至 8 类。**（已并入本仓：D5 改号 D6，用例同步改名，见 §七）** |

> 教训写进规范：**验收器只能证明"形式合规"，不能证明"内容为真"**。任何"未检索到 DOI / 无摘要"的结论都必须附带检索记录，且每轮至少人工重查一次。

> 本文件随进度更新，每完成一个阶段即在上方勾选并补写产出。

## 七、第四轮：两分支合并（2026-09-06，本轮）

上一节（§六）来自并行独立复审分支 `arena/01a0747e-mr-guo`。本轮将其 **44 文件改动全部合并入本分支**，并对合并结果做了三件事：

**1. 独立复核对方的全部关键事实主张**（不采信、重新查证）：

| 主张 | 独立核验方式 | 结论 |
| :--- | :--- | :--- |
| 论文 14 DOI `10.1115/GT2024-128792`、VAE+NURBS、9 变量、Cunxi Li 一作 | Crossref API `works/10.1115/gt2024-128792` 逐字段比对（含摘要全文） | ✅ 一致 |
| 论文 20 DOI `10.1117/12.3117536`、HARCT 2026、Cunzhong Gong 一作 | Crossref API 同上 | ✅ 一致 |
| 论文 04/13 作者互换（P04=Tao/Guo/Song/Li；P13=Wang/Song/Guo/Li/Feng） | Crossref API `works/10.1115/1.4051416`、`works/10.1115/1.4064228` | ✅ 互换属实 |

**2. 合并中发现并修正对方分支自身的 5 处新问题**：

| # | 问题（对方分支 29/29 全绿之下） | 处置 |
| :--- | :--- | :--- |
| 1 | 全集汇总两处卡片仍写 "(HTML 格式全文)"——与其自己 §六 第 7 条裁定（htm 仅摘要级）自相矛盾；其守卫串 "HTML 全文" 因中间夹"格式"二字而漏检 | 按其口径统一改为"（ScienceDirect 页面，**仅摘要级**）"；本仓既有守卫串 "HTML 格式全文" 恰好网住 |
| 2 | 全集汇总 17 条 "Official Abstract" 字段被改标"摘要转述"后，本仓已逐字化修复的 14 条**逐字英文摘要面临回退为转述版**（对方从基线复审，不知道本仓已修文本） | 统一为"**摘要原文（Abstract, verbatim）**"：保留并补全本仓逐字文本（P05/P13 原为截断版，已换事实卡完整逐字），标签注明逐字来源；P14/P20 新增同款字段（取自其证据卡 §二） |
| 3 | 对方 D5-规范编号贯穿与本仓 D5-工况限定**撞号** | 对方检查改号 **D6**，selftest 用例同步改名，两检查并存 |
| 4 | 公开论文整理编号说明写"本文档的 1–12 只是文内顺序号"，实际含 15 篇（11 主条目 + 4 补录） | 改为如实描述 |
| 5 | 本仓 D2b-编号落地仍按旧"规范编号 **NN**"字符串计数，与合并后的【论文 XX】体系不符 | 改为标题级【论文 XX】标签计数：公开 15 + 付费 5 + 全集 20 |

**3. 合并后全量重验**（成绩见上方成绩单第四行）：

> `make verify`：**35/35**（A1 去代码正文 120,234 字；全书 223,926 字）；
> `make selftest`：**16/16** 全部捕获，还原后 35/35；
> `verify_whitepaper --strict`：20 讲 **0 不达标**；
> 确定性保持：白皮书内容哈希戳 F4 逐字节一致，`index.json` `generated_at=src-7799a1c6e251`（touch 稳定）。

> 教训（续 §六）：**并行复审的价值在于互补盲区，但合并本身就是一次新审计**——两个分支对同一处"旧错误"给出了不同修法（改标签 vs 换文本）时，必须回到原始证据（本地 PDF / Crossref / 出版商页面）裁决，而不是简单二选一。

## 八、第四轮配图补齐：每讲的原理/难点讲解处配图（2026-09-06 起，进行中）

**新口径（本轮起）**：既有 6 讲（01/03/04/06/11/16）已有配图；用户要求**每一讲的"原理解释、难点解析"处都有图片配合讲解**。
做法：沿用仓库既有惯例——生图模型生成、中英成对（`xxx.png` 英文版 + `xxx_zh.png` 中文标注版）、
插在第 3 节（数学推导/原理）标题之后、图下配一段「一张图版本」解说引文；图中引用论文数字处一律在解说与图注中带 `［原文 PX…］` 出处。

**进度**（生图模型每轮限 10 张，跨轮补齐；已完成）：

| 讲次 | 图名（images/） | 状态 |
| :--- | :--- | :--- |
| 02 | filter_gei_multifidelity_parallel（中英成对） | ✅ 2026-09-06 |
| 05 | emfs_msfo_late_stage_trap（中英成对） | ✅ 2026-09-06 |
| 07 | swvae_ktaso_transfer（中英成对） | ✅ 2026-09-06 |
| 08 | gto_gradient_inversion（中英成对） | ✅ 2026-09-06 |
| 09 | vae_nurbs_endwall（中英成对） | ✅ 2026-09-06 |
| 10 | tno_panoramic_prediction（仅中文版） | ✅ 2026-09-06 |
| 12 | presunet_similarity_physics（仅中文版） | ✅ 2026-09-06 |
| 13 | shap_design_attribution（仅中文版） | ✅ 2026-09-06 |
| 14 | pjp_digital_twin（仅中文版） | ✅ 2026-09-06 |
| 15 | meridional_physics_ai（仅中文版） | ✅ 2026-09-06 |
| 17 | transonic_cascade_windtunnel（仅中文版） | ✅ 2026-09-06 |
| 18 | tip_leakage_ffd（仅中文版） | ✅ 2026-09-06 |
| 19 | uq_uncertainty_propagation（仅中文版） | ✅ 2026-09-06 |
| 20 | impingement_film_cooling（仅中文版） | ✅ 2026-09-06 |

> **2026-09-06 用户指示：自第四轮后半起，新增配图仅生成中文标注版、不再生成英文版**（既有英文版保留，不删）。
>
> 已新增验收项 **C5-每讲配图**（20/20 讲各至少 1 张图）并配套 selftest 反身用例「C5 某讲配图整体消失」，锁定成果不回退。
> 同轮修复验收器 C3 老 bug：`stem.replace("_en","")` 会误删文件名中间的 `_en…`（如 endwall），改为只剥行尾后缀。

## 九、第五轮独立复核（2026-09-06，不采信上一轮自报的 36/36）

上一轮再次自称"全面完美完成"。本轮**先不看验收器**，直接写脚本把交付物与 `corpus/txt/` 原文、
Crossref/出版商页面做**机器对账**，再回头审判裁判。结论仍是那句老话：**验收器全绿 ≠ 内容为真**。
36/36 全绿之下查出以下 6 类实质缺陷，均已修复，并新增 2 项检查 + 2 条反身用例锁定：

| # | 缺陷（验收器为何没抓到） | 处置 |
| :--- | :--- | :--- |
| 1 | **P17 的"逐字摘要"里混进了 markdown 引用符**：`…framework is > proposed with…`、`combined > with an sample-weighted…` 等 7 处 ` > ` 混入正文（事实卡 P17 §二与全集汇总同步中招）。号称 verbatim，实际连原文字符都不是 | 依本地 PDF 摘要重排为单行逐字文本 |
| 2 | **P12 的"逐字摘要"用 `…` 截断**，丢掉末尾两句（`…also shows remarkable better prediction accuracy. With the above, the effectiveness of the SDNO has been well demonstrated.`）；中译同样缺这两句 | 依 `corpus/txt/P12` 补全英文与中译 |
| 3 | **P13 的"逐字摘要"实为轻度改写**：漏 `the`（`To solve the turbine design…`）、擅自把 `(DBSCAN)` 插进原文没有该缩写的位置 | 依 Crossref `works/10.1115/1.4064228` 的 `abstract` 字段逐字replace（事实卡 / web_evidence / 全集三处） |
| 4 | **规范 §三.3「禁止无量化、无出处的形容词」从未被检查**：讲义与第六篇共 13 处"显著/大幅/完美"无任何出处（如 02 讲"需要显著更多的 HF 样本"、10 讲"显著超出训练样本分布"、08 讲"显著削弱压力梯度"） | 逐条回原文裁定：属原文自评的补引英文原话并标注"原文定性用语，未给数值"；属本讲推论的改写为量化或注明"本讲推论" |
| 5 | **抽取器把未映射字形吐成 `/u1D70E` 转义**（P01 一篇就有 264 处），导致语料里 `uncertainty /u1D70E(/u1D431)` 这种伪文本——**逐字比对与全文检索双双失效**，而 F1/F5 之前都不比对正文 | `tools/extract_corpus.py` 的 `normalize()` 增加 `/uXXXX → 真字符` 还原 |
| 6 | **验收器盲区**：全仓没有任何一项检查"自称逐字的摘要是否真逐字"，也没有任何一项检查文风规范 §三.3 | 新增 **A7-无源形容词**（禁用套话须带出处或已量化；代码块内断言/注释豁免）、**F5-逐字摘要属实**（凡含"逐字/verbatim"的英文块须整句命中语料；省略号截断直接判红） |

**A7 的自身盲区也由反身测试当场抓出**：初版只扫 `docs/`，反身用例把套话注入白皮书时只有 F4 变红、A7 无感——
说明"改派生物"这条路能绕过检查，已补扫白皮书本体。

> `make verify`：**38/38**（新增 A7、F5；A1 去代码正文 124,887 字）；
> `make selftest`：**19/19** 全部捕获（新增「A7 无源套话回流」「F5 逐字摘要被篡改」两类），还原后 38/38；
> `make checks`：10 模块自检 + **129 passed / 37 subtests**。

> 教训（续 §六、§七）：**"逐字"是最容易被悄悄注水的字段**——它看起来最权威，却几乎没人真的去和原文逐字符比对。
> 凡是文档里出现"逐字 / verbatim / 原文照录"的地方，都必须有一项机器检查去兑现这个承诺，否则它就只是一句形容词。

## 十、第六轮：把机器能管的边界继续外推（2026-09-06）

上一轮如实交代了一条底线：**验收器覆盖的是"形式合规"，讲义 1434 句陈述里只有 176 句（12%）带来源标注，
其余 88% 的讲解/推导/类比机器判定不了**。本轮的任务就是**把这条边界往外推**——找出那些"看似只能人工读、
其实可以机器判定"的类别，逐个变成硬检查。

### 本轮排查的 6 类（含 4 类误报，如实登记，不掩饰）

| # | 排查项 | 结果 |
| :--- | :--- | :--- |
| 1 | 全仓非图片内部链接 | ✅ 0 死链（讲义 `./images/` 是相对装配后根目录的，属设计如此） |
| 2 | 跨讲引用「第N讲↔论文XX」是否错位 | 初查报 3 处，**经核实全为误报**（正则跨越了表格分隔符）；收紧后 18 处并列标注**全对** |
| 3 | 每讲的英文缩写能否在其自称的论文原文中找到 | 查出 **2 处真问题**（见下表），其余为"承前启后"合法提及别篇 |
| 4 | 自陈算式（`a/b≈c`、`(a−b)/b=c%`）复算 | 初查报 2 处，**为误报**（脚本漏识别 `%`）；修正后 5 条**全部算对** |
| 5 | LaTeX 结构（`$$`/`$` 配对、花括号平衡） | ✅ 0 处损坏 |
| 6 | 无本地原文的 5 篇，讲义有没有写出摘要外的数字 | ✅ 无越界（唯一命中项标的是 P16，属合法跨篇对比） |

### 查出并修正的实质问题

| # | 问题 | 处置 |
| :--- | :--- | :--- |
| 1 | 第 14 讲把结构求解器写作 **ABAQUS**，而论文 15 原文 Table VII 实际拼作 **ABAQUES**（原文自身的拼写）。属"善意规范化"，但违反可回溯口径——读者按 ABAQUS 检索原文会落空 | 两处均补注"原文 Table VII 拼作 ABAQUES"；数字 1,022.35 s 经核对无误 |
| 2 | 第 08 讲跨论文对比表的 `FFD-BO / GAN-BO / VAE-W-BO` 在论文 17 原文中不存在 | **经核实不是错误**：这些命名出自论文 08（4.96/4.86/4.75/4.52 逐个精确命中 P08 Table），且讲义已明确警告"08 用 BO、17 用 EGO，非严格同条件对照"。判定为**正确且严谨**，不改 |

### 新增 3 项检查 + 3 条反身用例

| 检查项 | 判定内容 | 为什么旧裁判抓不到 |
| :--- | :--- | :--- |
| **D7-讲次论文映射** | 每讲**标题编号 / 元数据规范编号 / 元数据 DOI↔index.json** 三处互相交叉校验；外加全仓 18 处「第N讲（论文XX）」并列标注一致性 | D2/D6 只数编号"在不在"，从不问"指对没有"。历史上"白皮书第10讲=全集16号"式错位正属此类 |
| **A8-算式自洽** | 文中自行给出的除式/百分比换算全部复算（容差 3%） | A5 只看数字有没有来源标签，**对"标签在、算术错"完全无感** |
| **A9-公式结构** | `$$` 成对、行内 `$` 成对、公式内花括号平衡 | 公式崩成乱码不触发任何既有检查，却是读者直接看得见的硬伤 |

> **D7 的初版盲区又是被反身测试当场抓出来的**：第一版只读元数据块一处，
> 变异用例把元数据改掉时"标注"和"基准"一起变了，自然测不出错位——已改为三源交叉校验。
> 这已是连续第二轮出现"新检查自己带盲区、由反身测试兜住"的情况，**说明反身测试这道工序不能省**。

> `make verify`：**41/41**（新增 D7、A8、A9）；
> `make selftest`：**22/22** 全部捕获，还原后 41/41。

> 教训（续 §九）：上一轮说"88% 的正文机器管不了"，本轮证明**其中至少还能再切出三块硬地**——
> 编号指向、算式自洽、公式结构。**"机器管不了"往往只是"还没想清楚怎么管"**；
> 但同时也要诚实：本轮 6 类排查里有 4 类初查结论是误报，**每一条机器告警都必须回原文人工复核后才能下结论**。

## 十一、第七轮：手绘风配图重做（2026-09-06 起，进行中）

### 零、先记一条必须刻死的操作教训

> **用户说「我把 X 发到你的分支了」时，第一个动作必须是 `git fetch origin <本会话分支>` 并比对本地/远端 HEAD。**
> 本轮我先去翻 `/tmp`、翻别人的分支、翻 PR 列表，绕了一大圈才回到用户第一句就指明的位置，
> 期间还误合了另一个分支（已 `git merge --abort` 撤销）。**用户已经说明位置时，不要自己另找位置。**

### 一、patch 的处置结论（如实登记）

用户推送的 `01a07526-6bb0-7f22-9aae-507e1b11ed5d.patch` 已取得（提交 `0edaa3f`），但**不含任何像素数据**：
- 声明 41 张 PNG，全文 `GIT binary patch` 段数为 **0**，只有 `Binary files ... differ` 摘要行；
- 文件仅 8,518 字节（现有 31 张图共 25 MB，均值 ~800 KB/张）；
- 41 个 blob 哈希在仓库内全部 `Not a valid object name`；全部 15 个远端 ref 均无这些文件名。

→ **图无法从该 patch 还原**（导出时缺 `--binary`）。但**41 个文件名本身是有效的设计蓝图**，已据此重做。

### 二、从 patch 反推出的原设计意图

命名规律 `L{讲次}_s{小节}_{主题}_zh.png`，即**每讲第 2 节（工程死穴）与第 3 节（数学推导）各配一图**，
比既有"每讲至少 1 图"的口径更细。原作者做到 L12 后中断（L01–L12 + L16，共 41 个名）。本轮沿用该编号体系。

### 三、风格口径（用户 2026-09-06 定稿，此后统一执行）

**手写字体 + 实体物件 + 三味混用**，按内容类型分配，不全书一种风格：

| 味道 | 特征 | 适用 |
| :--- | :--- | :--- |
| **A 手账风** | 牛皮纸底、马克笔手写、卡通角色演比喻 | §1 生活引子、§3 数学原理 |
| **B 钢笔速写** | 笔记本纸、钢笔线稿+交叉排线、单色高亮 | §2 工程难点、真实硬件（压气机/风洞/叶尖） |
| **C 概念渲染** | 半写实 3D + 手写批注 | 流动物理（涡系/气膜/泄漏涡），讲次 16/17/18/20 |

> 硬性要求：图中**所有中文必须是手写体**，不得出现印刷体；提示词中的英文指令**不得**被渲染进画面。

### 四、本轮完成（7/41）

| 图 | 讲·节 | 味道 |
| :--- | :--- | :--- |
| `L01_s2_expensive_blackbox_zh` | 01·§2 | B |
| `L01_s3_z_two_diseases_zh` | 01·§3 | A |
| `L01_s4_cei_rei_roster_zh` | 01·§4 | A |
| `L02_s2_idle_cluster_zh` | 02·§2 | B |
| `L02_s3_filter_threshold_zh` | 02·§3 | A |
| `L03_s2_kriging_blind_zh` | 03·§2 | B |
| `L03_s4_gsde_factory_line_zh` | 03·§3 | B |

**本轮返工 3 次，均为内容错误而非风格问题**（图错了比没图更糟，一律重做不将就）：
1. `L01_s4` 柱状图 E3I(−242.20) 优于 PEI/GEI 却画得最矮 → 重做为柱高严格单调递增；
2. `L03_s4` 压气机上排标号漏 `R2`、RBF 公式重复出现两次 → 重做，七排 S0/R1/S1/R2/S2/R3/S3 完整且公式仅一处；
3. `L01_s4` 二版把提示词里的英文指令（`NEGATIVE, MORE NEGATIVE IS BETTER`、`TALLEST`）**当作图上文字画了进去**
   → 提示词改为显式约束"画面内只允许出现下列中文与数字，不得出现任何英文"。

> **教训**：生图提示词里的**元指令**（对模型说的话）与**画面文案**（要画上去的字）必须显式区分，否则会被当成文案画进图里。

> `make verify`：**41/41**（C5 现 42 处图引用；C3 曾因新图注中英文实词与文件名无交集而告警，
> 已按既有惯例在图注中补英文题名——**这是既有判据生效，不是放宽判据**）。

### 五、第八轮：续做 10 张（累计 17/41）

| 图 | 讲·节 | 味道 | 校对结论 |
| :--- | :--- | :--- | :--- |
| `L04_s2_rotor37_divide_zh` | 04·§2 | B | ✅ |
| `L04_s3_da_ego_loop_zh` | 04·§3 | A | ✅ |
| `L04_s5_rotor37_scoreboard_zh` | 04·§5 | B | ✅ 五档效率/提升/基准与［原文 P11 Table 3］逐字吻合；28 维算式正确 |
| `L05_s2_mfs_trap_zh` | 05·§2 | A | ⚠️ 混入英文杂字 `HIGH`/`LOWER` → **待返工** |
| `L05_s3_emfs_pipeline_zh` | 05·§3 | A | ✅ |
| `L06_s2_latent_dim_dilemma_zh` | 06·§2 | B | ✅ |
| `L06_s3_gmfoo_two_latents_zh` | 06·§3 | A | ✅ |
| `L07_s2_blade_120_to_100_zh` | 07·§2 | B | ⚠️ 英文杂字 `BUDGET`＋"算过就扔掉？"重复两次 → **待返工** |
| `L07_s3_swvae_weighting_zh` | 07·§3 | A | ✅ |
| `L08_s2_blueprint_points_only_zh` | 08·§2 | B | ✅ 11 个原文数字全部准确，出处齐全 |

**英文杂字问题复发**：§十一.四 已记录过一次（`TALLEST` 被画进图），本轮仍在 2 张上重演，
根因是提示词里用英文描述**画面属性**（如 `flattens HIGH`、gauge 标签）时，模型会把这些词当文案。
→ 已升级写法：提示词开头加**硬约束段**「the ONLY text allowed anywhere is the Chinese listed below；
no English words anywhere」，并对易重复的标注显式写「write this phrase only once on the whole page」。
两张返工因本轮生图额度（10 张/轮）用满，顺延至下轮首批。

> `make verify`：**41/41**；C5 图引用 42 → **52 处**。

### 六、第九轮：返工 2 张 + 续做 8 张（累计 25/41）

**返工验收（§十一.五 遗留）**：`L05_s2` 的 `HIGH`/`LOWER`、`L07_s2` 的 `BUDGET` 与重复标注均已清除。
→ **硬约束段写法有效**，此后所有提示词沿用：开头明写「画面内只允许出现下列中文，任何位置不得有英文」，
易重复的标注附加「整页只写一次」。

| 图 | 讲·节 | 味道 | 校对结论 |
| :--- | :--- | :--- | :--- |
| `L08_s3_gradient_reparam_zh` | 08·§3 | A | ✅ |
| `L09_s2_endwall_harder_zh` | 09·§2 | B | ✅ |
| `L09_s3_vae_nurbs_layer_zh` | 09·§3 | A | ✅ 含"本文无全文、不做推测"的警示条 |
| `L10_s2_one_model_per_metric_zh` | 10·§2 | B | ✅ |
| `L10_s3_tno_two_stage_cost_zh` | 10·§3 | A | ⚠️ **五个扇出标签生成乱码词**（"效率升针""绝对合通疲识"等）＋便签漏"10" → **待返工** |
| `L11_s2_film_cooling_render_zh` | 11·§2 | **C 首秀** | ✅ 冷热对比/射流/集气腔剖面到位，参数与［原文 §2.1］一致 |
| `L11_s3_superposition_net_zh` | 11·§3 | A | ✅ |
| `L12_s2_condition_change_breaks_zh` | 12·§2 | B | ✅ 冲角 −30°~+15°、马赫数 0.2~1.2 与原文一致 |

**新缺陷类型：中文乱码词**。此前两轮的图文缺陷都是"混入英文"，本轮首次出现**生成不存在的中文词**
（"绝对合通疲识""细羿输通帖"）。根因推测：单张图内**密集小字标签过多**（该图有 5 个扇出小标签＋2 个长句＋1 张便签），
模型在小字号处退化为"形似汉字"的乱涂。
→ 应对：**单图内的独立文字标签控制在 10 个以内**；扇出型标签改为「少而大」，或拆成两张图。

> `make verify`：**41/41**；C5 图引用 52 → **60 处**。C 概念渲染风格首次投入使用（第 11 讲）。

### 七、第十轮：返工 1 张 + 续做 8 张（累计 33/41）

**返工验收**：`L10_s3` 的五个乱码词与漏字已清除。**"标签少而大"规则确认有效**——
该图独立文字块由 14 个降至 8 个，扇出小标签取消、改为一句总括，全部清晰可读。
此规则此后固定执行：**单图独立文字标签 ≤ 10 个；宁可拆图，不可密排小字**。

| 图 | 讲·节 | 味道 | 校对结论 |
| :--- | :--- | :--- | :--- |
| `L12_s3_physics_enhanced_zh` | 12·§3 | A | ✅ 误差链 2.84/1.87/1.52/0.31~0.35/0.26 与［原文 P18 Table 1］一致 |
| `L13_s2_93dim_zh` | 13·§2 | B | ✅ 45+3+45=93 算式与命名规则正确 |
| `L14_s2_pjp_two_solvers_zh` | 14·§2 | B | ✅ |
| `L14_s3_pod_three_surrogates_zh` | 14·§3 | A | ✅ 三代理分工与 Pareto 前沿（已注明示意） |
| `L15_s2_meridional_dilemma_zh` | 15·§2 | B | ✅ |
| `L15_s3_meridional_two_stage_zh` | 15·§3 | C | ✅ 子午面流道/静子转子/两段式，误差 <1% 出处齐全 |
| `L16_endwall_vortex_render_zh` | 16·§2 | C | ⚠️ "分成压力侧腿与吸力侧腿"重复两次 → **待返工** |
| `L16_transonic_cascade_windtunnel_zh` | 16·§3 | C | ✅ |

**生成失败 1 张**：`L13_s3`（SHAP 蜂群图）模型返回空响应（`Response contains no images`），
非内容违规，属服务端偶发，顺延重试。

> `make verify`：**41/41**；C5 图引用 60 → **68 处**。C 概念渲染累计 4 张（11·§2、15·§3、16·§2、16·§3）。

### 八、第十一轮：收尾，41 张全部完成

**返工与重试均通过**：
- `L16_endwall_vortex` 的重复标注已消除，六项标注各一次，双腿改用分叉引线指向马蹄涡两侧（比上版更准确）；
- `L13_s3`（SHAP 蜂群图）上轮服务端空响应，本轮重试成功。

| 图 | 讲·节 | 味道 | 校对结论 |
| :--- | :--- | :--- | :--- |
| `L13_s3_shap_swarm_zh` | 13·§3 | A | ✅ 两条准则方向与［原文 P19 §5］一致 |
| `L17_s2_crossflow_gradient_zh` | 17·§2 | C | ✅ 压力面/吸力面压差与两个恶果 |
| `L17_s3_cascade_rig_zh` | 17·§3 | B | ✅ 含"只有摘要不做推测"警示条；14.0% 出处齐全 |
| `L18_s2_tip_leakage_render_zh` | 18·§2 | C | ⚠️ "泄漏涡""刮削涡"各重复两次 → 遗留 |
| `L18_s3_ffd_cage_zh` | 18·§3 | B | ✅ 0.42% 出处齐全 |
| `L19_s2_two_uncertainties_zh` | 19·§2 | A | ✅ 0.33%/45%/5.0% 与［原文 P04 摘要］一致 |
| `L19_s3_uq_propagation_zh` | 19·§3 | A | ✅ |
| `L20_s2_impingement_film_render_zh` | 20·§2 | C | ✅ 内攻外防双侧结构清晰 |
| `L20_s3_three_conflicts_zh` | 20·§3 | B | ✅ 三重矛盾与预混燃烧背景 |

### 九、关于"41 张"口径的澄清（重要）

原 patch 的 41 个文件名**并非 41 个不同主题**：其中 16 个是旧命名、25 个是 `L{讲}_s{节}` 新命名，
两套名存在**大量同题重复**（例如 `L01_cei_rei_shifts` 与 `L01_s4_cei_rei_roster` 是同一张图的两个名字）。
去重后原作者的实际主题约 26 个，且**只做到 L12（+L16）**。

本轮采用的口径：**沿用 `sN` 新命名体系，为 20 讲每讲的 §2（工程死穴）与 §3（数学推导）各配一图**，
共 **41 张**（第 01、03、04 讲因小节结构不同，分别落在 §2/§3/§4、§2/§4、§2/§3/§5）。
→ 结果是**主题覆盖比原蓝图更全**（补齐了原作者未做的 L13–L20 共 16 张）。

### 十、风格分配统计

| 味道 | 张数 | 分布 |
| :--- | :---: | :--- |
| **A 手账风**（数学原理/抽象机制） | 17 | L01·§3§4、L02·§3、L04·§3、L05·§2§3、L06·§3、L07·§3、L08·§3、L09·§3、L10·§3、L11·§3、L12·§3、L13·§3、L14·§3、L19·§2§3 |
| **B 钢笔速写**（工程难点/真实硬件） | 18 | L01·§2、L02·§2、L03·§2§4、L04·§2§5、L06·§2、L07·§2、L08·§2、L09·§2、L10·§2、L12·§2、L13·§2、L14·§2、L15·§2、L17·§3、L18·§3、L20·§3 |
| **C 概念渲染**（流动物理） | 6 | L11·§2、L15·§3、L16·§2§3、L17·§2、L18·§2、L20·§2 |

> `make verify`：**41/41**；C5 图引用 68 → **77 处**。

## 十二、第十二轮：全量图文校对（2026-09-06）

配图主线完成后，对图片部分补做一次与文本同强度的审计。

### 一、机器全量扫描结果

| 检查 | 结果 |
| :--- | :--- |
| 讲义图引用 ↔ `images/` 实体 | 73 个文件、77 处引用；**死链 0、孤儿图 0** |
| 图注数字回溯语料 | 带出处者 **3/3 命中**；**2 处含数字却无出处标签** → 已修 |

补出处的 2 处（均已回原文确认后再补，非臆断）：
- `L07_s2`「叶片数 120→100」→ P08 原文 "reducing the number of referenced blades **from 120 to 100**"，补标［原文 P08 §5］；
- `L13_s2`「93 维」→ P19 摘要 "the **93-dimensional** design space"，补标［原文 P19 摘要与 §3］。

### 二、新增 C6 检查：图注数字必须有源

> **图注长期是检查盲区**——A5 只扫正文句子，图注被当作"附属物"放过。
> 但读者是把图注里的「降低 14.0%」「93 维」当结论看的，**注水风险与正文等同**。

`C6-图注数字有源`：扫全部讲义的 Markdown 图片引用语法，图注中出现数据型数字（百分比、小数、≥2 位整数；
排除讲次号/论文号 01–20）时，必须带 `PXX` 出处标签，且该数字能在对应语料（`corpus/txt` + `corpus/facts`）中命中。

### 三、反身测试当场抓出 C5 的真实盲区（重要）

新增 C6 用例后跑 `make selftest`，**C5 漏检**。追查发现两件事：

1. **旧用例已失效**：原用例只删 1 张图，但每讲配图已增至 2~3 张，删 1 张该讲仍有图。
   → 改为 `__ALL_IMAGES__` 语义：正则删掉该讲**全部**图引用。
2. **改完仍漏检——暴露 C5 自身的盲区**：C5 只扫**白皮书**，而变异改的是**讲义源**。
   源里的图全删光，C5 依然 PASS（只有 F4 报"源与派生不一致"）。
   → 已改为**同时扫白皮书与 `docs/lectures/*.md` 源文件**。

> 这与 A7 当初的盲区**是同一类、方向相反**：A7 是"只扫源、改派生物可绕过"，C5 是"只扫派生物、改源可绕过"。
> **教训升级为通则：凡涉及源→派生的检查项，两端都必须扫；只查一端的检查等于没查。**

> 这已是**连续第三轮**出现"新检查/旧用例自带盲区、由反身测试兜住"。反身测试这道工序的价值再次被证实。

> `make verify`：**42/42**（新增 C6）；`make selftest`：**23/23** 全捕获，还原后 42/42。

> ⚠️ **本轮自身还犯了一个错，一并记下**：在本文档里写检查项说明时，正文中出现了形如
> 图片引用语法的示例串，**被 C1-无死链当成真实引用**，导致提交后 `verify` 掉到 41/42。
> → 教训：**文档里描述"引用语法"时不要写出可被解析的字面形式**；C1 不区分"真引用"与"举例"。
> 这也说明一件事——**提交前必须以 `build_whitepaper` + `verify_all` 的最终输出为准，
> 不能凭上一次的绿灯记忆就提交**（本轮正是先提交、后发现，已在同轮修正）。

## 十三、合并前自检（2026-09-06）

### 一、遗留清空

`L18_s2` 叶尖泄漏图的「泄漏涡」「刮削涡」重复标注已消除——提示词显式约束"只画一个泄漏涡、一个刮削涡，
每个短语整页只写一次"，七项标注各一次。**至此配图无已知缺陷。**

### 二、合并前补做的三项结构自检

| 检查 | 结果 |
| :--- | :--- |
| 20 讲六节结构（§1–§6 齐全且有序） | ✅ 不合规 0 |
| 20 讲元数据完整性（编号/文献/DOI/作者/本地原文/证据等级） | ✅ 不合规 0 |
| 无全文的 6 讲（09/15/17/18/19/20）是否都有"不做推测"警示 | ✅ 6/6 全有 |
| 图片讲次归属（`LNN_*.png` 是否被第 NN 讲引用） | ✅ 无错配 |

> ⚠️ 自检过程中我先写了一版**判据过死**的元数据检查（硬匹配"原始文献"四字），
> 误报 6 讲"缺字段"。实际这些讲用「标题 / 期刊 / 论文号」承载同样信息，**信息更全而非缺失**。
> 改为语义化判据后不合规为 0。
> → 记一笔：**机器告警必须回原文复核后才能下结论**（§十 已有同类教训，此处再次应验，未写入误判为"缺陷"）。

### 三、合并前最终成绩

| 项目 | 结果 |
| :--- | :--- |
| `make verify` | **42 / 42** |
| `make selftest` | **23 / 23** 全捕获，还原后 42/42 |
| `verify_whitepaper.py` | 20 讲 6/6 节全达标，全书 **232,428 字**，不达标 **0** 讲 |
| `run_checks.py` | 全部通过，单元测试 **129** 例 |
| 配图 | **41 / 41**，77 处引用，死链 0、孤儿 0 |

判定：**达到合并标准，合入 main。**

## 十四、第十三轮独立复审（2026-09-06，不采信上一轮"42/42 且配图无已知缺陷"）

**方法**：一切自报结果均从零复跑并独立对账——`make corpus → whitepaper → checks → verify → selftest`；167 条带标签百分比断言与 `corpus/txt` 机器对账（抽样回验：85.825/86.929/1.104 ∈ P07 Table 3、12.99/4.52/4.85/1400/400 ∈ P08、0.33/45/5.0 ∈ P04 摘要、P01 Table 2 五值 ∈ 语料，全部属实）；Crossref 复核论文 14/20 的题名、一作与载体（与 §六裁定一致）；人工目检 7 张手绘图（L01_s4、L05_s2、L07_s2、L10_s3、L13_s3、L16 端壁涡、L18_s2）；第 13 讲"转子 AE-R2 减小 / 静子 AE-S2 增大"的方向性逐句回 P19 原文 §4.2.1 复核——与原文一致（原文明说 "a **smaller** … AE-R2 … higher efficiency"）。**结论：数字层、代码层、元数据层经得起查；但有 4 处实打实的不符合项，本轮全部闭环。**

| # | 缺陷（42 项裁判为何抓不到） | 处置 |
| :--- | :--- | :--- |
| 1 | **L05_s2 图内文字残留 `形形状`**：左标注"前期 LF 撑全局形/形状"跨行重复一个"形"字。§十一.六"返工验收"只复验了英文杂字与乱码词两类已知缺陷，漏了跨行重复字；§十三.一"至此配图无已知缺陷"因此不成立 | 按手绘 A 风 + 既有提示词硬约束规范重制，目检确认 8 项标注各出现一次、无英文杂字、曲线方向（MFS 早快晚差、SFS 后来居上）与讲义一致 |
| 2 | **裁判比标准松（老毛病第 5 次）**：规范 §三.1 明列"百分比/倍数/**CFD 次数**"须带出处，A5 注释也引了这条，代码却只实现 `%|倍`——"1500 次 CFD"类断言一直裸奔（抽样中唯一未带标签的是第 03 讲"启后"里的回收引用，数值本身正确，属漏标非虚构造） | A5 增"次数断言"分支（`N 次 CFD/评估/仿真/求解/计算`，辟谣块按 D4b 同款逻辑豁免）；03 讲该句补 `［原文 P07 Table 3］`；新增反身用例「A5 无源次数」（注入 `7777 次 CFD` 必须报红），selftest 23→24 |
| 3 | **抽取器吐 NUL**：`normalize()` 修了 `/uXXXX` 转义（§九.5），但 pypdf 对另一类未映射字形吐 `\x00`——每个语料文件 ~700–1500 个，"k-ω"变 `k\0w`、"10⁻⁶"变 `10\06`、"[−30°]"变 `[\0 30°`，逐字比对与 grep 在相关片段静默失效。属同类缺陷未修全 | `extract_corpus.py` 的 `normalize()` 增 NUL 剔除；重跑 `make corpus`（index.json 字数随之净化，口径不变） |
| 4 | **README 三处过期口径**：写"验收共 29 项"（实为 42）、"37 张配图"（实为 73 个文件）、docs 树漏 `front_matter.md`/`chapter0.md`。D 系列只检查索引表本身，从不读这几行——"文档对齐"存在机器盲区 | README 三处按实数改写；另注：§十一.九"共 41 张"与其自身列举（01/04 讲各 3 图）相加为 **42**，本轮按 42 如实登记 |

> 一处过程教训（不改仓库，写进纪律）：`make verify` 与 `make selftest` **不得并行**——selftest 会物理改写仓库文件注入缺陷，并行的 verify 会把"变异中"的仓库判红（本轮亲测：并行时误报 39/42，串行即 42/42）。两者自带 try/finally 还原，中断亦安全，但**报告以串行复跑为准**。

**教训（续 §六–§十二）**：① "人工逐张核对"这种验收标准**永远无法被验收器替代**——本轮仅目检 7/42 张就抓出一处残留；没做完"逐张"之前不得写"无已知缺陷"。② 规范里每写一个"必须"，裁判必须有一条检查兑现，否则该条就是空头支票——A5 的注释引用了"CFD 次数"却只实现 % 与倍数，**引用规范≠执行规范**。③ 文档的"现状口径"（README/进度表）与历史流水账不同，前者过期即失实，改内容必须连带改口径。

> 本轮成绩：`make verify` **42/42**（A5 收紧后全库次数断言零违例——唯一需要改的 1 处已补标）；`make selftest` **24/24** 全捕获、还原后 42/42；`make checks` 10 模块自检 + 129+37 用例全绿；白皮书与源逐字节一致（F4）。

## 十五、第十四轮打磨：L 图全量目检 + 索引文档纳入裁判（2026-09-06）

**范围**：按用户指令"打磨至无可挑剔"，清 §十四 遗留队列中可闭环的部分：42 张 L 图逐张目检全部完成；root 五份索引文档补溯源并让验收器覆盖它们；Crossref 剩余 4 篇与 P08 ≥50% 宣称、31 张主题图目检列入下一轮。

### 一、L 图 42/42 目检结果（本轮全部看完，不再抽样）

**实锤缺陷 10 张**（判定法沿用 §十四：逐标签查重复、扫非词乱码、公式框与讲义 boxed 逐字对账）：

| 图 | 缺陷 | 权威文本 |
| :--- | :--- | :--- |
| L02_s3 | 框内公式为捏造式 | `02.md` L89–96：ω = 1/(1+(σ²_DF/σ²_LF)^0.5) |
| L05_s3 | "聚出"跨行重复 | — |
| L08_s3 | 公式框尾部乱码字"财" | `08.md` §3 |
| L11_s3 | Σ×Σ 伪式（把 Sellers 合成写成求和） | `11.md`：T_AB = η⁻¹[η(T_A)·η(T_B)] |
| L12_s3 | 乱码"无量纲纳"+ 同义双框 | `12.md` Fig.6 数值本身无误 |
| L13_s2 | 底部图例整行重复 ×2 | `13.md` L46 仅一条 |
| L15_s2 | "代理模型"两字乱码 | `15.md` |
| L17_s3 | "静压孔""油流显示"各 ×2（第二对无引线） | `17.md` |
| L18_s3 | 上下两组标签重复 + "盘钹二局部坐标"乱码 | `18.md` |
| L20_s3 | "省冷气就有热斑" ×2 | `20.md` |

合格 32 张，图内数值均已对过讲义/语料（5.8×、超一个月、毫秒级/<1%、14.0%、0.33%/45%/5.0%、1500 次 CFD 等）。

### 二、10 张全部重制并逐张复验

生图 prompt 逐字内嵌讲义公式 + 白名单硬约束（"每个短语只出现一次、禁非词乱码"）。复验结论：**10 张的语义缺陷全部消除**（公式与讲义逐字一致、乱码清除、重复标签减半、L17_s3 完全合格）；但本轮 prompt 用"N. 文本"编号列白名单，模型把序号**当成正文画进图里**（L02/L05/L08/L11/L12/L15/L18/L20 出现 "1. 2. …" 前缀，L13_s2 出现无设计含义的 ②③⑤⑩⑪），另 L11/L12 两张标题行漏画。序号泄漏属"白名单外文字"，与乱码同类，不达标。**处置**：9 张登记二次返工（下一轮以引号列举式 prompt + "禁止渲染任何序号/引号"重生成）；本条同时立规：**今后生图 prompt 内不得用编号列表枚举图内文字，只能用引号列举**。

### 三、索引文档纳入裁判：D8（新）

- **补标 6 处**：全集 L337 `14.0%［原文 P09 摘要，见上引］`、L351 `0.42%［出版商页面 摘要，见上引］`；公开 L79 `0.42%［出版商页面 摘要中译，见事实卡 P10］`；付费 L49 `45%［原文 P04 摘要，见上引］`、L113 `<1%［出版商页面 摘要，见上引］`；README 表格 `14.0%（MA=0.8，见事实卡 P09）`。
- **D8-索引数字可溯**：五份 root 文档（README/全集/公开/付费/链接）按块扫 `%、倍、N 次 CFD/评估/…`，豁免 = 块内含 `原文/出版商页面/…` 标注或 `事实卡 PXX/见上引/摘要中译` 指针、`>` 逐字块、标题块、`### Abstract` 惯例下的纯英文逐字摘要、表格块允许相邻块带标。A5 的 root 盲区从此有裁判。
- **反身用例 +2**：含"D8 全集无源百分比"。第一次注入点落在带"见上引"的大列表块内部被块级豁免掩盖、用例捕获失败——教训：**块级豁免自身也是盲区**，注入用例必须构造独立无源块。selftest 24→**25**。

**本轮成绩**：`make verify` **43/43**；`make selftest` **25/25** 全捕获、还原后 43/43。

### 四、第十五轮待办（按序）

1. 9 张序号泄漏图二次返工 + 复验；2. 31 张主题图逐张目检（发现缺陷即返工）；3. Crossref 复核 P06/P12/P15/P18；4. 核 `05.md`"论文08 ≥50%"（grep corpus P08）；5. 全链复跑绿 → commit/push。**在 1–4 完成前，任何文档不得写"配图无已知缺陷"。**

---

## 十六、第十五轮：9 图返工闭环 + 31 主题图全量目检（2026-09-07，部分完成）

### 一、队列① 9 张序号泄漏 L 图二次返工：完成 ✅

9 张全部以「引号列举图内文字、禁用编号列表」的提示词重制并同名覆盖（`docs/lectures/images/`）。逐张目检复验**一次通过，零再返工**：
L02_s3「滤波门槛由相关性定」、L05_s3「EMFS：全局打底 局部纠偏」、L08_s3「把旧最优翻译回潜空间」、L11_s3「叠加写进网络结构」、L12_s3「物理增强把误差压到底」（柱值 2.84/1.87/1.52/0.26、×5.8 注记与 12.md Table 1 一致）、L13_s2「93 维从哪来」、L15_s2「快而不准 还是 准而太贵」、L18_s3「给叶尖罩一层控制点笼子」（+0.42% 注记）、L20_s3「端壁冷却的三重矛盾」。
**登记**：L08_s3 生成时多出「迭代次数」「‖G(z)−x*‖²」两个白名单外坐标轴标注——语义正确，判定合格保留。

### 二、队列② 31 张主题图逐张目检：完成 ✅，发现并返工 7 处缺陷

24 张直接合格（含 `shap_design_attribution_zh` 的「变量 Z37…」占位行——13.md 已明示「机制示意，非论文原图」，依约不算事实缺陷；`ai4turbomachinery_evolution_map` 可压/不可压连续方程并列为 §P4 已登记例外）。**本轮推翻 §P4 两处「人工核对通过」的旧结论**：

| # | 缺陷 | 位置 | 依据 |
|---|---|---|---|
| 1 | 布雷顿 p-V 循环箭头逆时针（1→4→3→2→1，制冷机方向） | `gas_turbine_working_principle` **zh+en 双张** | 热机须顺时针 1→2 压缩↑、2→3 燃烧→、3→4 膨胀↓、4→1 排气←；§P4「布雷顿循环方向正确」为误判 |
| 2 | MFS 橙虚线右端终点在 SFS 蓝实线**下方**＝后期更好，与图内红标「后期：MFS 反而更差」及讲义矛盾 | `emfs_msfo_late_stage_trap` **zh+en 双张** | 05.md:59 白纸黑字「最终解比 SFS 更差」；§P4/初检「曲线合格」为误判（终点高低须放大细读） |
| 3 | 标注换行重复「最小化 最小化 ‖G(z) − x*‖」 | `gto_gradient_inversion_zh` | 生图模型行折叠重词缺陷（同 L05_s2「形形状」家族） |
| 4 | 右面板说明行「控制点移动 → 叶尖几何光滑变形」整体重复两次 | `tip_leakage_ffd_zh` | 同上 |
| 5 | 左图对数轴最大刻度 10³，但「小时级」CFD 柱应 ≈3.6×10⁶ ms，刻度装不下 | `tno_panoramic_prediction_zh` | 03.md 的 0.915 ms/小时级数字本身无误，轴刻度与数据不符 |

**返工进度（截至本轮）**：7 张已全部同名重制/定向编辑；其中 3 张已放大复验合格——`gas_turbine_working_principle_zh`（四箭头全顺时针、五个中文标注无重复）、`gas_turbine_working_principle`（en，同左且七个英文标注完好）、`emfs_msfo_late_stage_trap_zh`（交叉点后橙上蓝下、红标指向正确、右面板未动、无黑边）。**其余 4 张（en emfs、gto_zh、tip_zh、tno_zh）待下一轮放大复验；复验合格前，本仓库任何文档不得写「配图无已知缺陷」。**

### 三、队列③ Crossref 复核 P06/P12/P15/P18：完成 ✅ 4/4

api.crossref.org 逐条实证：P06 `10.1109/TCYB.2022.3165044`（IEEE Trans. Cybern. 53(7):4347–4360, "Generative Multiform Bayesian Optimization"）✓；P12 `10.1063/5.0239483`（Phys. Fluids 36(12), 2024-12-04 在线）✓；P15 `10.1109/CEC65147.2025.11043110`（pp.1–8）✓；P18 `10.1016/j.ast.2026.112351`（AST 177:112351, 2026-10, Cheng/Song/Ouyang/Zeng/Guo）✓；旧错误 DOI `10.1016/j.ast.2026.112440` 查得为 Lim et al. 串列叶型 GCN 论文——「112440 张冠李戴」辟谣成立。

### 四、队列④ 「≥50%」主张核实：完成 ✅（队列标签勘误）

「论文 08 ≥50%」主张实际位于 **07.md:47/186 与 08.md:158/191/193**（队列原写「05.md」系标签错位；05.md 无此主张，未动）。corpus 逐行实证（PDF 换行折叠后 grep）：P08 摘要原文「the proposed KT-ASO is shown to reduce at least 50% function calls when achieving similar optimal solutions」✓；P17（GTO）「at least 50%」同型句 ✓；16.md 的 P03 关联主张 ✓。无需改文档。

### 五、队列⑤ 全链复跑 + 提交：verify 43/43 ✅、selftest 25/25 全捕获且还原后 43/43 ✅（本轮串行跑毕）；README「共 43 项」表述上轮已改毕，本轮无遗留。**唯一未闭环项：4 张返工图的放大复验（下一轮），复验通过前 §十六.二 的「待复验」标注不得清除。**

### 六、追加登记（同日）

- F4 曾短暂 42/43：白皮书头部 `src-` 内容戳哈希范围含 `docs/**`（`docs/lectures/images/` 的 9 张返工图在其中），按既定流程 `make whitepaper` 重装配（新戳 `src-f3e36dd0f71b`）后恢复 43/43。此非缺陷，是「源变→派生物须重建」的设计行为。
- 重装配后复跑 `selftest`：**25/25 全捕获，还原后 43/43**。
- 状态提醒：§十六.二 表中 4 张已重制但**放大复验未完成**（emfs en、gto zh、tip zh、tno zh）——留待下一轮，复验通过前维持「存在已知缺陷不得清零」约束。
