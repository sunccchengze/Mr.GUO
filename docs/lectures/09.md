### 【第09讲】VAE-NURBS 端壁参数化：把生成式参数化从叶型推广到非轴对称端壁（**论文 14**）

#### 本讲开场 90 秒

**上讲闪回：** 叶型生成后推到端壁：更脏、更3D、更易废。

**本讲反派：** 「轴对称偷懒」

**只要带走 3 句口诀：**

1. **轴对称省事但挡不住通道涡**
2. **VAE提案+NURBS托底**
3. **废品极多，生成式先滤合法**

**主比喻：** 端壁难捏的泥——轴对称挡不住横向流

![第09讲心智图：合法 / 光顺 / 物理三关](./images/L09_h_mindmap_zh.png)

> **看图三秒：** VAE 提案、NURBS 托底、物理挡横向流；轴对称是偷懒。
**本讲赌注：** 端壁参数化若不过三关，生成的是合法废品。

---

#### 1. 生活与物理直觉引子：为什么端壁比叶型更难"捏形状"


![axisym lie 轴对称假声明：看不见的二次流](./images/L09_d_axisym_lie_zh.png)

> **指读：** 周向平均好看，通道涡不吃你的平均。

前面几讲一直在处理**叶型**（叶片的横截面）。现在换一个更麻烦的对象：**端壁（endwall）**——叶片根部所依附的那个环形壁面。

传统端壁是**轴对称**的：绕发动机轴线旋转一圈，形状处处一样，像一张被旋转扫掠出来的圆盘面。它便宜、好加工，但气动上并不好——因为流道里的压力分布是**周向不均匀**的：压力面一侧压力高、吸力面一侧压力低，气流会本能地从高压侧"横着"往低压侧跑，形成**横向二次流**，卷出马蹄涡、通道涡、角涡，造成损失。

**非轴对称端壁造型（Non-axisymmetric Endwall Contouring, NEC）**的做法是：把端壁做成**周向起伏的三维曲面**——压力高的一侧"鼓起来"一点，压力低的一侧"凹下去"一点，用几何本身去抵消横向压力梯度。摘要第一句就把这一点当作已被证明的前提：NEC "has been proven effective in reducing the secondary losses of turbine cascades"［出版商页面 摘要］。

听起来很美，但工程上有一个硬骨头，摘要把它说成了一个**三角权衡**：样条类参数化在"**设计空间维度**"、"**样本多样性**"、"**畸形几何比例**"三者之间顾此失彼［出版商页面 摘要］。想让形状够丰富，控制点就得多，维度就上去了；控制点一多，随机组合出来的曲面里"长得不像端壁"的比例就飙升，白白浪费 CFD。

这正是第 06、07、08 讲那套**生成式参数化**要解决的同类问题——只不过对象从二维叶型换成了三维端壁。

**为什么端壁更烦：**  
叶型是「一条曲线的艺术」；端壁是「一圈环面还要跟叶片根接上、还不能让二次流乱窜」。  
设计空间里废品率极高——这就是 VAE 先学「什么算人样」、NURBS 层再保光顺的原因。

**三层难：** 合法、敏感、可造。


> **先猜后揭：** 若本讲反派「轴对称偷懒」赢了，你的下一笔 CFD/实验预算会怎样被浪费？揭晓贯穿 §3–§5。


| 生活元素 | 工程元素 | 符号 |
| :--- | :--- | :--- |
| 主比喻侧 | 生成提案 | VAE |
| 主比喻侧 | 光顺层 | NURBS |
| 主比喻侧 | 轮毂/机匣 | 端壁 |


**端壁更难捏：** 叶型有截面直觉；端壁缠着横向流与角区损失。轴对称等于假声明「周向无差异」。

**端壁比叶型难捏的三层原因：**  
1. 三维壁面，缺少「中弧线」那种低维经典语言。  
2. 横向流/通道涡/角区耦合，轴对称是假声明。  
3. 废品率高：刀脊、对穿、陡坎随时出现。  
所以 VAE 提案 + NURBS 托底，是「先合法再光顺」，不是「生成炫技」。

**泥与刀：**  
端壁是泥（难捏），轴对称是偷懒刀法。  
非轴对称不是为了「看起来高级」，是为了挡横向流。

---

#### 2. 叶轮机械中的真实工程死穴：设计空间里绝大部分形状是"废品"


![scrap museum 端壁废品三件展：刀脊对穿陡坎](./images/L09_d_scrap_museum_zh.png)

> **指读：** 生成式要把概率质量挪出这三间展厅；NURBS 是出门安检。

![three gates 端壁合法/光顺/物理三关](./images/L09_d_three_gates_zh.png)

> **指读：** VAE 过合法关，NURBS 过光顺关，物理关才挡得住横向流。


![端壁设计空间的高维、窄流形与多参数化合流（endwall harder，手绘笔记版）](./images/L09_s2_endwall_harder_zh.png)

> **指读：** 这张图是本讲工程死穴的一张图版本。


把摘要的三角权衡翻译成设计师每天面对的三件事：

1. **维度高**：三维光滑曲面若用 B 样条/NURBS 控制网格描述，设计变量轻易几十上百个；
2. **流形窄**：在如此高维的空间里，"看起来像真实、可加工端壁"的形状只占极小一部分，绝大部分随机采样都是废品；
3. **评估贵**：每个候选形状都要跑一次三维 RANS 才能知道好坏。

而且端壁参数化还有一个叶型没有的麻烦：**同一个端壁可以被多种完全不同的参数化方法描述**（正弦幅值×轴向包络、Bézier 控制网、直接的 B 样条曲面……），每种方法各有自己能表达和不能表达的形状。摘要给出的处理很有工程味：**把一组经典 NEC 参数化方法各自生成的样本都收集起来，统一表示成散点坐标（scatter coordinates），拼成一个"覆盖充分"的设计空间**［出版商页面 摘要］。也就是说，训练数据不是靠一种参数化"撒网"得到的，而是几种参数化"合流"的产物——这样学到的低维流形不会被单一方法的表达能力锁死。

于是问题变成：**能不能先学出一个"只生成合法端壁"的低维潜空间，再在这个潜空间里优化？**——这就是本文标题所指的"生成模型参数化"。

**现场感：** 周五下班前组长要数——你面对的不是可以慢慢推的练习题，而是**算错一次就不可回收**的预算。


**废品率：** 随机控制点过陡、相交、不可铣。VAE 把概率放在曾合法区域；NURBS 托底光顺。

**废品博物馆导览加长：**  
刀脊：曲率爆表，铣刀与网格双杀。  
对穿：拓扑与叶片/圆角打架。  
陡坎：看起来很「非轴对称」，分离与热斑一起爆。  
VAE 把概率挪出展厅；NURBS 安检；物理关看横向流是否真被挡。

---

#### 3. 数学公式：摘要给了骨架，细节不推导

先把公式关掉，用主比喻说一遍：

> **端壁难捏的泥——轴对称挡不住横向流**  
> 本讲反派是「轴对称偷懒」。下面公式是这句话的展开；每条核心式尽量带**回译**。知识总量不删——深潜只是阅读路径，不是垃圾桶。

![VAE 解码器内嵌 NURBS 层保障光顺性（vae nurbs layer，手绘手账版）](./images/L09_s3_vae_nurbs_layer_zh.png)

![端壁生成式参数化与内嵌 NURBS 层（VAE NURBS endwall，中文标注版）](./images/vae_nurbs_endwall_zh.png)

> 上图是本讲全部数学的「一张图版本」（据出版商摘要示意，正文未获取）：**左图**是压缩——多种经典 NEC 参数化方法的样本统一为散点坐标后，VAE 把端壁设计空间压进 **9 个潜变量**［出版商页面 摘要］；**右图**是合法性保障——解码器内嵌一层 NURBS，没有它生成的型面可能畸形（红线），有了它才光顺（绿线）。

> 🚫 本文无全文，**网络结构、损失函数、训练策略一概未获取，本讲不做任何推测性推导**。以下只把摘要给出的三个"骨架事实"写清楚。

##### 3B. 本讲符号卡（建议截图留存）

| 符号 | 人话 | 备注 |
| :--- | :--- | :--- |
| VAE | 生成提案 |  |
| NURBS | 光顺层 |  |
| 端壁 | 轮毂/机匣 |  |

> 🧭 先认符号再读公式；主文核心式有「回译」，装不下的细节仍在正文完整保留（不删知识）。

##### 3C. 主路径公式与推导（知识全保留）

**骨架一：生成模型是 VAE。** 摘要原话："the Variational Autoencoder (VAE) is used to learn the intrinsic dimensions of the parameterized NEC design space"［出版商页面 摘要］。这与第 07 讲论文 08 的 SW-VAE 同属一族，而与第 08 讲论文 17 的 GAN 路线不同——旧版 "GAN-Endwall" 的错名很可能就是把 17 混进来了。

**骨架二：光顺性靠结构约束，不靠事后过滤。** 摘要："a layer of NURBS is incorporated into the neural network architecture of VAE"［出版商页面 摘要］。直觉上，解码器输出的不是裸的散点坐标，而是经过一层 NURBS 映射后的曲面——只要 NURBS 层在，输出天然是光滑曲面，畸形几何在生成源头就被压掉。这一点和第 06 讲 GMFoO"生成后再筛"的思路形成对照。**但 NURBS 层的节点向量、控制点数量、是否可训练，摘要一概没说，本讲不猜。**

**骨架三：潜空间维度为 9。** 摘要："NEC parameterization of a high-pressure turbine stage testcase with **9 variables**"［出版商页面 摘要］。对比第 16 讲论文 03 手工 NEC 参数化的 13 个参数（优化 11 个）［原文 P03 §3.3］，9 维并非数量级的压缩——本文的卖点更在于**这 9 维覆盖了多种参数化方法的并集**，而不是把某一种方法压得更小。

想看完整的数学骨架，请读有全文支撑的相邻讲次：

| 想理解的内容 | 请读 |
| :--- | :--- |
| 潜空间优化的基本框架、GAN/VAE 如何学出设计流形 | **第 06 讲（论文 06 GMFoO）** |
| VAE 参数化 + 样本加权训练、多保真迁移 | **第 07 讲（论文 08 SW-VAE / KT-ASO）** |
| 梯度重参数化（把坐标点形状映射回潜空间） | **第 08 讲（论文 17 GTO）** |
| 非轴对称端壁的气动原理与手工参数化（13 参数） | **第 16 讲（论文 03 NAE-Purge）**、**第 17 讲（论文 09 NAE-Exp）** |

---

#### 4. 算法控制流

> 🧭 **值班手册：** 先看「什么情况下走哪条分支」，公式是对白速记不是主角。

摘要只给了粗粒度流程，按原文顺序整理如下（每步都能在摘要中找到对应句子；步内细节未获取）：

1. **收集样本**：用一组经典 NEC 参数化方法各自生成端壁样本［出版商页面 摘要］；
2. **统一表示**：把所有样本转换为统一格式的散点坐标，合并成一个设计空间［出版商页面 摘要］；
3. **训练 VAE**：学习该设计空间的内在维度（结果为 9 维），解码端含 NURBS 层［出版商页面 摘要］；
4. **潜空间优化**：以 9 个潜变量为设计变量做高压涡轮级 NEC 气动优化，并与其他 NEC 参数化方法做同样的优化作对比［出版商页面 摘要］。

> 🚫 步骤 3 的损失函数（重构项 / KL 项权重）、步骤 4 用的优化器（EGO？DE？）与评估次数上限的具体值，**均未获取**。

> **一句话记住：** 轴对称省事但挡不住通道涡；进阶：VAE提案+NURBS托底。


**主比喻回扣：** 把上面的分支再翻译成「端壁难捏的泥——轴对称挡不住横向流」——若某步在生活里说不通，多半是符号没对齐。


**生成式端壁流水线（概念）：**  
VAE 提案（合法关）→ NURBS 托底（光顺关）→ CFD/指标（物理关）。  
任一关失败都不得进入「可签字几何」库。

**流水线门禁：**  
提案出门必须持「合法牌」；入 CFD 必须持「光顺牌」；入报告必须持「物理牌」。  
缺牌者不得进入可签字库——这是配置管理，不是学术洁癖。

---

#### 5. 实验验证与量化突破

**先报胜负：** 先读结论句与最大那一格，再读全表；表是证据架不是背诵清单。

> ⚠️ **摘要只给定性结论，没有任何数字。** 本讲据此**不列出任何百分比、降幅、CFD 次数**。

摘要可确认的验证事实：

- **算例**：一个高压涡轮级（high-pressure turbine stage）的非轴对称端壁气动优化［出版商页面 摘要］；
- **对比对象**："other NEC parameterization approaches"（具体哪几种、各自变量数未获取）［出版商页面 摘要］；
- **结论原话**："the generative model based parameterization method can optimize aerodynamic performance more effectively while maintaining a lower limit on the number of performance evaluations"［出版商页面 摘要］——即在**更低的性能评估次数上限**下取得**更好的气动优化效果**。

> 🗑️ 再次明确：旧版白皮书 6.1 对比矩阵中"GAN-Endwall（2024）……**消除九成畸变几何，设计空间极度紧凑**"这一行属于**无源填充**，摘要亦不支持，继续从重建版删除。

**体感换算：** 把关键数字换成「工程师的周/月」或「一次签字的风险」——数字才进长期记忆。若原文给了墙钟时间或成本比，优先用原文换算。


---

**桥到 16–17：** 本讲先合法参数化；风洞封口在后。

#### 6. 审稿人批判与承前启后

把软肋放上**可信度刻度**（0=完全不信，5=可当默认工具）：

| 维度 | 打分 | 人话 |
| :--- | :---: | :--- |
| 问题 | 5 | |
| 骨架 | 4 | |
| 细节 | 2 | |
| 实验 | 3 | |

**可以攻击的点（基于摘要即可提出）：**

1. **收益未量化**。与第 07、08 讲（有 Table 3/Table 4 硬数据）相比，本文公开摘要只有"more effectively"，读者无法判断"更有效"是 0.1 个百分点还是 1 个百分点。
2. **流形覆盖上限被训练集锁定**。设计空间由既有参数化方法的样本"拼"成，VAE 学到的 9 维流形原则上**只能覆盖这些方法能表达的形状的并集**——若最优端壁不在任何一种经典方法的表达范围内，本方法会系统性漏掉它。摘要未讨论此风险。
3. **NURBS 层的约束强度未知**。"光顺"不等于"可加工"（还有最小曲率半径、与叶根倒圆的相容性等约束）；一句 "a layer of NURBS" 不足以判断可制造性是否被真正保证。
4. **会议论文篇幅有限**。ASME Turbo Expo 论文通常单算例、无消融实验，这本身也是本讲只能到 B 级的成因。

**承前：**
- 直接继承自**论文 06（GMFoO）**确立的"生成模型 + 潜空间优化"范式，以及**论文 08（SW-VAE）**的 VAE 参数化思路；
- 应用对象（非轴对称端壁）承接**论文 03（NAE-Purge）**与**论文 09（NAE-Exp）**所研究的端壁气动问题。

**启后：**
- **论文 17（GTO）**在参考文献 [17] 中引用本文（这也是本仓库最初取得其著录的线索），并把"生成式参数化"推进为"生成式**迁移**优化"；
- 与**论文 10（FFD-Tip）**共同构成"复杂三维几何的高效参数化"这一工程子方向：一个用生成模型，一个用自由变形（FFD）。

---

> 📎 **升级路径**：若能取得 GT2024-128792 全文 PDF，本讲可升级为 A 级证据，并补全：VAE 编码/解码结构、NURBS 层定义、训练样本数、对比的参数化方法清单、气动收益数值与评估次数上限。
> 届时请先更新 `corpus/facts/P14.md` §三/§五，再同步本讲。

---

**读端壁生成结果的三问：**  
1. 合法率相对随机采样提升了多少（若原文有表，用表；无则只定性）？  
2. 光顺后 CFD 网格是否还在「修洞」？  
3. 横向流/角区指标有没有动——没有物理关，合法形只是博物馆藏品。

**读生成端壁结果的验收序：**  
合法率 → 光顺/网格友好 → 横向流或角区指标是否动。  
第三关不过，前两关只是「合法废品生产线」。

---

#### 附：非轴对称端壁的「三关」

1. **合法关：** 不自交、不过陡、与叶片/通道拓扑兼容——VAE 提案。  
2. **光顺关：** 可铣、可铸、网格好生成——NURBS 托底。  
3. **物理关：** 真能改横向流与角区损失——最终要 16–17 的机理与实验。

**反派轴对称偷懒的具体嘴脸：**  
图纸好看、网格好画、周向平均也「说得通」，可通道涡不吃你的平均。二次流是三维的，端壁却被降成二维旋转体——这是建模偷懒，不是物理结论。

**和叶型生成的差别：**  
叶型有厚度、中弧线等经典语言；端壁更像「地形塑形」。废品率更高，所以生成式过滤合法形的价值更大。

---

#### 附：端壁参数化选型会（15 分钟版）

**议程三问：**

1. **我们有没有成对的「合法端壁」数据？**  
   - 有 → VAE/生成式可以学流形；  
   - 无 → 先别幻想端到端生成，用 NURBS/FFD 加硬约束更稳。

2. **废品率现在多少？**  
   - 随机抽样里非法/过陡/干涉占比若非法/过陡/干涉占比已经高到「随机抽样经常中招」，生成式过滤的价值立刻超过「多几个控制点」。

3. **下游谁接盘？**  
   - 只要 CFD：光顺与网格友好优先；  
   - 要进 16–17 的机理与实验：几何幅度必须落在可加工公差。

**轴对称偷懒的三种伪装：**

| 伪装 | 听起来很对 | 物理上错在哪 |
| :--- | :--- | :--- |
| 「周向平均够了」 | 通流设计常用 | 二次流是三维的 |
| 「网格好画」 | 项目经理喜欢 | 好画 ≠ 流场对 |
| 「以前型号也轴对称」 | 经验 | 负荷一变，经验过期 |

**三关检查单（合法 / 光顺 / 物理）** 过两关不够：没有物理关，参数化只是「合法废品生成器」。

---

#### 附：端壁「废品博物馆」（概念展板）

进馆三件展品（均为非法/高风险形，生成式要学会避开）：

1. **刀脊：** 局部曲率爆表，铣刀抬不起头，CFD 网格先哭。  
2. **对穿：** 端壁与叶片/圆角拓扑打架。  
3. **陡坎：** 看起来「很非轴对称」，横向流被猛打，但分离与热斑一起爆。

VAE 的价值不是「更花哨」，是把概率质量从这三间展厅挪走；NURBS 是出门安检。

---

#### 附：轴对称偷懒的三个伪装（再钉）

周向平均够了 / 网格好画 / 以前型号也这样。  
三条都能通过项目经理，三条都可能在二次流上破产。

---

#### 附：非轴对称端壁评审看板

| 关卡 | 绿 | 黄 | 红 |
| :--- | :--- | :--- | :--- |
| 合法 | 自交/干涉≈0 | 偶发可修 | 频发 |
| 光顺 | 网格一次过 | 需手工修 | 无法出网 |
| 物理 | 横向流/角区指标改善 | 持平 | 恶化 |

黄灯可带风险继续；红灯禁止入详设库。轴对称「省事」若物理红灯，仍算偷懒。

---

#### 图卡：VAE 内嵌 NURBS

潜码经解码器后过 NURBS 层，输出端壁默认合法/光顺/可加工；刀脊、对穿、陡坎进废品堆。生成式参数化的物理保险丝。

![NURBS embed VAE：解码器内嵌光顺层过合法三关](./images/L09_d_nurbs_embed_zh.png)

---

#### 章末：30 秒复述 · 自测 · 元数据

**30 秒复述稿（可朗读）：** 端壁比叶型难捏。VAE 提案、NURBS 光顺、物理挡横向流，三关都过才算数。轴对称是偷懒假声明，不是物理最优。

**自测（先做再对答案）：**

1. 轴对称代价？
2. NURBS？
3. 废品？

<details>
<summary>点开看参考答案</summary>

1. 二次流/角区损失。
2. 光顺可造。
3. 随机型面大量非法。

</details>

> **本讲元数据**（读完再看，不挡故事）
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **14** |
> | 原始文献 | **ASME Turbo Expo 2024（GT2024，London, UK，2024-06-24～28）**，Vol. 12D，论文号 **GT2024-128792**，文章号 **V12DT34A025** |
> | 作者 | Cunxi Li, Liming Song, Zhendong Guo, Zhao Yang, Jun Li, Zhenping Feng（郭老师列第三） |
> | DOI | `10.1115/GT2024-128792`（Crossref 2026-09-06 核验；在线 2024-08-28） |
> | 本地原文 | **无**（出版商摘要已获取；正文/图表需订阅） |
> | 证据等级 | **B（权威著录 + 摘要全文可查；正文方法细节与数值未获取）** |

> ⚠️ **本讲的写作边界（务必先读）**
> 本文**没有本地全文**。可核验的信息 = Crossref 著录 + 出版商页面摘要（逐字存于 `corpus/web_evidence/P14.md`）。
> 因此本讲：✅ 只写摘要明确给出的**方法定位、模型类型、变量数、验证对象与定性结论**；❌ **不写**任何百分比、损失降幅、CFD 次数、网络层数、损失函数——这些摘要没有。
>
> 🗑️ **两轮纠错记录**（都保留，便于审计）：
> ① 旧版白皮书把本文称作 "**GAN**-Endwall"，并写入"消除九成以上的无效几何生成"——**GAN 是错的，九成是编的**。
> ② 上一轮重建版虽删掉了数字，却又写成"DOI 未检索到、摘要未获取、证据等级 C"——**这也是错的**：Crossref 按题名一查即中，摘要公开。摘要显示模型是 **VAE + NURBS 层**。本讲据此重写，标题代号改为 VAE-NURBS。
> | 反派绰号 | 轴对称偷懒 |
> | 主比喻 | 端壁比叶型更难捏 |

