### 【第10讲】TNO：不优化，而是让"一次评估"变得几乎免费（**论文 16**）

#### 本讲开场 90 秒

**上讲闪回：** 前九讲聪明采样；本讲切换：单次评估变便宜。

**本讲反派：** 「一指标一模型」

**只要带走 3 句口诀：**

1. **先场f_s后积分W**
2. **毫秒前向，多指标复用**
3. **抓论文矛盾数字当侦探**

**主比喻：** 与其省打车钱，不如评估近乎免费


> **看图三秒（心智锚点）：** 几何→场→积分；「一指标一模型」划掉。
---

#### 1. 生活与物理直觉引子：与其省着打车的钱，不如把打车变免费

前面九讲，我们一直在琢磨同一件事：**CFD 太贵，怎么把"打车的次数"省下来？**

- 第 01 讲：让每一次采样都更有价值（CR-EI）；
- 第 02 讲：便宜的粗网格先探路（Filter-GEI）；
- 第 03、04 讲：换个能扛高维的搜索策略（GSDE / DA-EGO）；
- 第 07、08 讲：把上一个型号的经验搬过来（KT-ASO / GTO）。

这些都属于**"省着花"**的路线。

本文换了一个完全不同的思路：

> **能不能干脆让"一次评估"本身变得几乎不要钱？**

这就是**代理模型（surrogate model）**最朴素的想法——但本文的做法远比"训一个效率预测网络"更进一步，它叫**全景式（panoramic）预测**。

**范式切换口号：**  
前九讲：让「下一次评估」更聪明。  
本讲起：让「一次评估」本身变便宜，甚至近乎免费。  
TNO 的野心是：基本场一次前向，性能指标用公式积分出来——**一指标一模型**的仓库，可以收起来了。

**赛道切换：** 采样智慧 → 评估成本。


> **先猜后揭：** 若本讲反派「一指标一模型」赢了，你的下一笔 CFD/实验预算会怎样被浪费？揭晓贯穿 §3–§5。


| 生活元素 | 工程元素 | 符号 |
| :--- | :--- | :--- |
| 主比喻侧 | 基本场 | f_s |
| 主比喻侧 | 性能 | W |
| 主比喻侧 | 神经算子 | TNO |


**打车隐喻加长：** 旧策略少打车（少 CFD，前九讲）；新策略把车费降到可随便打（代理场，本讲）。可叠加：便宜评估 + 聪明采样。

---

#### 2. 叶轮机械中的真实工程死穴：一个代理模型只能服务一个目标

![一指标一模型的困境与临时起意的常态（one model per metric，手绘笔记版）](./images/L10_s2_one_model_per_metric_zh.png)

> **指读：** 这张图是本讲工程死穴的一张图版本。


先说常规做法的问题。假设你在设计叶片，关心三个指标：效率、压比、失速裕度。

常规代理建模会怎么做？**每个指标各训一个模型。** 代价是：

- 每增加一个关心的指标，就要多训一个模型、多花一份数据；
- 更麻烦的是，**中途临时想看一个新指标**（比如"我想看看某个叶高截面处的熵增分布"），对不起——**模型没训过，只能回去补 CFD**。

设计师的真实工作方式是**边看边想**的：看一眼流场，觉得尾缘分离可疑，就想看尾缘的熵分布；发现激波位置不对，又想看等熵马赫数分布。**"临时起意"是设计过程的常态。**

本文的洞察是：既然这些量**本质上都能从基本流场算出来**，那为什么不——

> **先预测流场的基本物理量，再按公式算出任何你想看的性能指标？**

原文的表述是：

> 与以往大多数直接预测所关心目标函数的模型不同，该方法**首先预测 Navier–Stokes 方程的基本参数**（温度、压力、密度等），再利用这些基本物理量**预测涡轮级子午面上的关键性能参数**。如此，该框架可**像 CFD 求解器一样工作**。［原文 P16 摘要］

**现场感：** 周五下班前组长要数——你面对的不是可以慢慢推的练习题，而是**算错一次就不可回收**的预算。


---

#### 3. 数学公式从 0 到 1 推导

先把公式关掉，用主比喻说一遍：

> **与其省打车钱，不如评估近乎免费**  
> 本讲反派是「一指标一模型」。下面公式是这句话的展开；每条核心式尽量带**回译**。知识总量不删——深潜只是阅读路径，不是垃圾桶。

![TNO 两段映射与单次前向代价（tno two stage cost，数值见［原文 P16 Table 3］，手绘手账版）](./images/L10_s3_tno_two_stage_cost_zh.png)

![TNO 全景预测：架构与单次评估代价（TNO panoramic prediction，中文标注版）](./images/tno_panoramic_prediction_zh.png)

> 上图是本讲全部内容的「一张图版本」：**左图**是 TNO 的结构——几何与工况经编码器、Transformer 注意力、解码器，**一次前向就输出整个流场**（u、v、w、p、T…）；**右图**是「一次评估」的代价——高保真 CFD 是小时级，TNO 前向只要 **0.915 ms**［原文 P16 Table 3］（原文 §4.1.1 正文写约 9 ms、与 Table 3 相差 10 倍，本讲以 Table 3 为准并已如实登记该矛盾）。

##### 3B. 本讲符号卡（建议截图留存）

| 符号 | 人话 | 备注 |
| :--- | :--- | :--- |
| f_s | 基本场 |  |
| W | 性能 |  |
| TNO | 神经算子 |  |

> 🧭 先认符号再读公式；主文核心式有「回译」，装不下的细节仍在正文完整保留（不删知识）。

##### 3C. 主路径公式与推导（知识全保留）

**第一步：把"全景框架"写成两段映射**
**〔式〕** x — 基本场预测器 fₛ → fₛ — 全景预测器 → W

**回译：** 上式是主路径构件——先记住它在算法里负责哪一步，再抠每一个符号。


- x：设计变量；
- f_s：基本物理场；
- W：性能量。

**第二步：选哪些"基本物理量"？**

原文选了 5 个［§3.3］：**静压 p、静温 t、绝对速度 v、相对速度 w、质量流 q_vm**。

为了计算方便，速度取平方，于是网络要预测的是：
**〔式〕** (p, t, v², w², q_vm)


子午面被离散成 M=4096 个点。

**第三步：TNO 的网络结构（branch–trunk 框架）**
**〔式〕** F(x)(r, z) = S(Bₖ(x;h_b) ⊙ Tₖ(r, z;h_t); h_s)


- **分支网络 B（branch）**：把设计变量 x 编码成特征，用堆叠的 MLP；
- **主干网络 T（trunk）**：把查询位置 (r,z) 编码成特征，由 1 个 embedding block + 若干 **Galerkin 注意力块**串联而成：

**〔式〕** T(r, z) = P∘ Gₜₘ∘⋯∘ Gₜ₁∘ E(r, z)

- 每个 Galerkin 注意力块 = 注意力层 + 前馈网络（FFN），**均带残差连接**；
- 注意力输出：
**〔式〕** Z = Attn(P^t) = (QKᵀV)/(m), Q = P^tW^Q

**为什么用 Galerkin 注意力而不是原生 softmax 注意力？**

原文说明：采用基于 Galerkin 的**简化自注意力**替代常规注意力，**省掉了 softmax 的计算，从而提升计算速度**［§3.1］。

**为什么自注意力在这个任务上特别有效？** 原文给了一个非常有物理感的解释：

> 叶型的**局部几何改动只影响特定的流动区域**——比如尾缘的改动会影响下游的分离涡与叶尖流动。自注意力模型能捕捉这种**复杂、非均匀的空间依赖**，把几何变化映射到对应的流动区域，从而提高整场预测精度。［§3.1］

最后，TNO 接一个**共享输出网络 S**，用于**同时处理多个物理场**——这正是"全景"二字的技术实现。

**第四步：从基本场算出性能量（全景预测器）**

原文 Table 6 给出 9 个常用性能指标的**显式计算公式**，全部只依赖那 5 个基本场，例如：

| 性能量 | 计算方式 |
| :--- | :--- |
| 绝对总温 | t_v=t+(v²)/(2c_p) |
| 相对总压 | p_w=p(1+(w²)/(2c_p t))^γ/(γ-1) |
| 等熵效率 | η_is = [(p_v2/p_v1)^((γ−1)/γ) − 1] / [t_v2/t_v1 − 1] 形式的总参数之比 |
| 质量流 | ṁ = ∫_{r_h}^{r_s} ρ v_m z r dr  /  ∫_{r_h}^{r_s} z r dr（周向平均后再展向积分） |

整体量的计算被分解为**周向积分 + 展向积分**两步，可分别在流场预测之前或之后执行。

**训练目标**：MSE 损失，Adam 优化器，900 epochs。

---

#### 4. 算法控制流

> 🧭 **值班手册：** 先看「什么情况下走哪条分支」，公式是对白速记不是主角。

```
离线（一次性投入）：
  ① 生成 2500 个叶片几何样本 + 对应 CFD 解
  ② 把每个子午面离散为 4096 个点，提取 5 个基本场
  ③ 训练 TNO：分支网络吃设计变量，主干网络吃查询坐标，输出 5 个基本场

在线（几乎免费，可反复执行）：
  ④ 给定新设计 x → TNO 预测子午面基本场
  ⑤ 按 Table 6 的显式公式算出任意性能指标
  ⑥ 接下游任务：敏感性分析 / GA 单目标优化 / NSGA-II 多目标优化
  ⑦ 关键结论仍送回 CFD 复核
```

> **一句话记住：** 先场f_s后积分W；进阶：毫秒前向，多指标复用。


**主比喻回扣：** 把上面的分支再翻译成「与其省打车钱，不如评估近乎免费」——若某步在生活里说不通，多半是符号没对齐。


---

#### 5. 实验验证与量化突破

**先报胜负：** 先读结论句与最大那一格，再读全表；表是证据架不是背诵清单。

**算例**：Rotor 37 跨声速压气机叶片，**28 个设计变量**。数据集 **2500 训练 / 400 验证**［§3.3］。

**① 基本物理场预测精度（FRE，%）**［Table 4］

| 网络 | 平均误差 |
| :--- | :---: |
| MLP | 0.820 |
| UNet | 0.792 |
| DeepONet | 1.888 |
| FNO | 0.572 |
| **TNO** | **0.083** |

> 原文结论：**TNO 相对 FNO 的平均误差降低 85.5%**［§4.1.1］。验算：(0.572-0.083)/0.572=85.5% ✅

**② 网络规模与速度**［Table 3］

| 网络 | 前向显存 (MB) | 训练时间 (min) | 预测时间 (ms) |
| :--- | :---: | :---: | :---: |
| MLP | 768.0 | 19.98 | 2.198 |
| UNet | 624.5 | 114.0 | 5.075 |
| DeepONet | 822.5 | 52.9 | 0.970 |
| FNO | 558.9 | 106.5 | 1.448 |
| **TNO** | **147.13** | **40.5** | **0.915** |

> ⚠️ **原文此处自相矛盾，必须如实指出**：§4.1.1 正文写的是"TNO 的预测时间约为 **9 ms**，比 CFD 仿真低 5 个数量级"，而 Table 3 给出的是 **0.915 ms**，相差 10 倍［由正文 9 ms 与 Table 3 的 0.915 ms 计算：9/0.915≈10］。可能是排版把 "0.9 ms" 印成了 "9 ms"，也可能正文另有所指。**本讲以 Table 3 为准，并标注该处待核实。**

**③ 与传统代理模型的对比（等熵效率 PRE，‰）**［Table 8］

| 训练样本量 | 500 | 1000 | 1500 | 2000 | 2500 |
| :--- | :---: | :---: | :---: | :---: | :---: |
| FNO | 1.213 | 1.188 | 1.057 | 0.938 | 0.810 |
| GPR（传统代理最佳） | 1.425 | 1.273 | 1.156 | 1.098 | 1.075 |
| ANN | 3.697 | 3.775 | 3.799 | 3.699 | 3.706 |
| **TNO** | **0.823** | **0.460** | **0.448** | **0.439** | **0.486** |

原文结论：**15 个场景中 TNO 有 13 个最优；在训练样本超过 1500 的所有场景中，TNO 全部最优且优势显著**［§4.1.3］。

**④ 单目标优化（GA，种群 20、代数 100；所有最优解都送回 CFD 复核）**［Table 9］

| 性能量 | 方向 | 参考值 | 模型预测 | CFD 复核 | 预测相对误差 | 相对改善 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **等熵效率** | max | 0.854 | 0.871 | **0.871** | 0.001% | **+1.95%** |
| 总压损失 | min | 0.124 | 0.115 | 0.103 | 11.87% | +17.24% |
| 静熵 | min | 44.83 | 41.44 | 38.94 | 6.42% | +13.13% |
| 总压比 | max | 2.046 | 2.073 | 2.071 | 0.11% | +1.21% |
| 温比 | max | 1.265 | 1.270 | 1.271 | 0.03% | +0.44% |

［原文 P16 Table 9，数值由原文给出］
- 效率的实际改善为 **1.70%**，与作者先前在同一优化任务上的工作一致［§4.3］。
- **这张表最值得细看的一行是"总压损失"和"静熵"**：它们的预测相对误差高达 **11.87%** 与 **6.42%**，远高于效率的 0.001%。原因见下一节的批判。

**⑤ 多目标优化（NSGA-II，种群 30、代数 100）**［Table 10，节选］

| 指标 | 参考设计 | Opt-A | Opt-B | Opt-C |
| :--- | :---: | :---: | :---: | :---: |
| 等熵效率 | 0.854 | **0.871** | 0.869 | 0.863 |
| 多变效率 | 0.872 | 0.883 | 0.882 | 0.876 |
| 总压损失 | 0.124 | 0.116 | 0.116 | 0.119 |
| 总压比 | 2.046 | 2.069 | 2.045 | 2.040 |
| 质量流 | 20.266 | 20.615 | 20.439 | 20.262 |
| 熵 | 44.832 | 41.196 | 41.273 | 43.462 |

**⑥ 最有冲击力的一句话**［§4.3］

> 全景式预测框架把一个**原本需要 1000 组 CFD 计算**的优化任务，压缩到 **121 秒**内完成，**优化效率提升四个数量级**。

**⑦ 敏感性分析的额外发现**［§4.2］

28 个设计变量按叶高分组做方差敏感性分析（蒙特卡洛法）。结果显示：**span 25% 组与 span 50% 组对整体效率的影响幅度相近**，但借助全景流场预测进一步分析会发现——**两组对流场的作用方式完全不同**：span 25% 组主要影响叶高约 5%–50% 区域与 90% 附近区域，前者是同叶高位置的几何直接影响，后者是局部型线变化通过分离涡**传播到全场**的表现。
［原文 P16 §4.2］

> 这正是"全景"框架的独有优势：**传统代理只能告诉你"哪个变量重要"，而流场预测能告诉你"它通过什么流动机制起作用"。**

**体感换算：** 把关键数字换成「工程师的周/月」或「一次签字的风险」——数字才进长期记忆。若原文给了墙钟时间或成本比，优先用原文换算。


**侦探框：** 原文若出现互相矛盾的耗时数字，记录网格/后处理/硬件/batch，写「待核实」比装懂更专业。

---

**篇内坐标：** 本讲开场预测篇；11–15 从「有场」生长。

#### 6. 审稿人批判与承前启后

把软肋放上**可信度刻度**（0=完全不信，5=可当默认工具）：

| 维度 | 打分 | 人话 |
| :--- | :---: | :--- |
| 范式 | 5 | |
| 速度 | 4 | |
| 多指标 | 5 | |
| 数据 | 2 | |

**可以攻击的点：**

1. **相对误差指标在"小量纲"指标上失真**。总压损失的预测相对误差 **11.87%**、静熵 **6.42%**——也就是说，**越是接近零的损失类指标，模型越不准**。原文用同一个相对误差指标衡量不同量纲的量，这是方法论上的瑕疵。
2. **最优解落在训练集分布之外**。原文自己承认：识别出的最优解性能"significantly exceeds the distribution range of the corresponding performance indicators"（原文自评用语，未量化超出幅度）［原文 P16 §4.2］。这意味着模型在**外推**，而外推的可靠性没有任何保证。
3. **只有一个算例**（Rotor 37，一种叶片、一个子午面），泛化性未验证。
4. **预测时间自相矛盾**（正文 9 ms vs Table 3 的 0.915 ms），见上文。
5. **训练成本没有被算进"四个数量级"**。2500 个 CFD 样本是实打实的投入；本文没有与"用同样 2500 个样本做常规代理 + 优化"做**端到端的总账对比**。
6. **优化器很朴素**：GA / NSGA-II，没有用前面各讲的贝叶斯或代理辅助方法。也就是说，**TNO 的收益来自"评估便宜"，而不是"搜索聪明"**——这两条路线其实是**可以叠加**的，本文没有做这个实验。
［原文 P16 Table 9 与 §4.3，数值由原文给出］

**承前：**
- 方法论上继承神经算子（DeepONet、FNO）与 Transformer/Galerkin 注意力。
- 与第 01–09 讲的关系是**互补而非替代**：前面九讲解决"**如何用尽量少的 CFD 找到最优点**"，本文解决"**如何让一次评估本身变得几乎免费**"。**两条路线可以叠加**——用 TNO 做"廉价评估"，再用 CR-EI/DA-EGO 做"聪明搜索"，这是很自然的下一步。

**启后：**
- **论文 12（SDNO）**：同为流场预测的深度算子方向；
- **论文 18（ResUNet-Sim）**：同为"用深度学习替代/加速仿真"的工程化落地；
- **论文 19（SHAP-Turbine）**：本文的敏感性分析只是**为设计服务的副产品**，而 SHAP 那篇把"变量重要性与可解释性"本身作为研究对象。

---

---

#### 章末：30 秒复述 · 自测 · 元数据

**30 秒复述稿（可朗读）：**  
TNO先场后积分，评估近免费。反派一指标一模型。连矛盾数字一起审。

**自测（先做再对答案）：**

1. 两段式为何香？
2. 多指标省在哪？
3. 矛盾耗时怎么做？

<details>
<summary>点开看参考答案</summary>

1. 场可迁移；指标是积分。
2. 一场多用。
3. 查设置写入待核实。

</details>

> **本讲元数据**（读完再看，不挡故事）
> | 项 | 值 |
> | :--- | :--- |
> | 规范编号 | **16** |
> | 原始文献 | *Chinese Journal of Aeronautics*, **2025, 38(7): 103473** |
> | DOI | `10.1016/j.cja.2025.103473`（开放获取 CC BY） |
> | 作者 | Qineng Wang, Zhendong Guo, Liming Song\*, Tianyuan Liu\* |
> | 本地原文 | `1-s2.0-S1000936125000792-main.pdf` |
> | 证据等级 | **A（本地原文逐句可查）** |
> | 反派绰号 | 一指标一模型 |
> | 主比喻 | 与其省打车钱，不如把打车变免费 |

